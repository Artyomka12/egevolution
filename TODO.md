# TODO.md — Задачи и планы EGEvolution

> **v2.5.2 (2026-07-28):** hero-блок главной — убрана кнопка «Решить вариант», оставшаяся кнопка отцентрована; плюс маркетинговые изображения (вне кода сайта).
> `.hero-cta` — удалена ссылка «Решить вариант» (`/variants`), осталась только «Начать подготовку» ✅ · добавлен `justify-content: center` в десктопное правило `.hero-cta` — без него одиночная кнопка была прижата к левому краю ✅ · backend и остальная разметка `start.html` не менялись ✅ · отдельно (не код сайта, не влияет на приложение) — в `static/` сложены маркетинговые изображения для Авито/Instagram, собранные HTML-макетами и отрендеренные через headless Edge: QR-код, 3 фото-обложки для Авито, 3 сторис с отзывами, 3 сторис с форматом/ценами/расписанием занятий
>
> **v2.5.1 (2026-07-26):** редизайн интерактивного блока «Попробуйте задачу прямо сейчас» на главной + правка урока №1.
> Блок расширен с 1 вопроса + калькулятора готовности + маршрута до полной последовательности: 6 простых вопросов из разных тем (единицы измерения информации, системы счисления, алгебра логики, кодирование изображений, целочисленное деление в Python, единицы памяти) ✅ → калькулятор готовности, без изменений ✅ → 1 сложный теоретический вопрос (проблема остановки Тьюринга, без вычислений) ✅ → финальная страница с планом подготовки и встроенной формой заявки рядом (те же поля, что в `register.html`, обычный `POST` на `/register`, бэкенд не менялся) ✅ · индикатор — 9 точек, по одной на каждый содержательный шаг (по решению пользователя), подписи у каждой точки убраны (не влезли бы), вместо них одна динамическая подпись под рядом точек ✅ · логика перехода: верный ответ — короткое сообщение и автопереход через 0.75с (по отдельной правке — сама анимация перехода сделана медленнее и плавнее: 0.35–0.48с с `ease-in-out`/`ease-out` вместо прежних 0.18–0.32с с `ease`) ✅; неверный ответ — прежняя логика, кнопка «Далее» ✅ · переходы между «страницами» блока переведены с двух дублирующихся функций (`goToStep2`/`goToStep3`) на общие переиспользуемые хелперы (`izSlideOut`/`izSlideIn`/`izPrepareIncoming`) ✅ · удалён неиспользуемый `#izCta`-блок и мёртвый CSS (`.iz-step-label`, `.iz-cta-title/-sub/-buttons/-btn-apply`) ✅ · независимо — в уроке №1 «Установка Python и PyCharm» убрана тестовая задача-заглушка «Итог»/«Да», вместо неё карточка-ссылка на этот же опрос на главной (`/#tryTaskSection`, с `scroll-margin-top` под sticky-navbar) — только для `lesson_id == 1`, остальные уроки не затронуты ✅ · каталог `/preparation` больше не показывает «0 / 0» для уроков без практических задач ✅ · всё проверено `Flask test_client()` (включая перекрёстную сверку всех `getElementById`/`querySelector` в JS с реальными id/классами в HTML) и лично пользователем в браузере
>
> **v2.5.0 (2026-07-22):** новый урок №13 «Контрольная проверка» — 13-й урок сверх согласованного 12-урочного плана, по решению пользователя.
> Теория — один короткий абзац, без нового материала ✅ · практика — 20 задач, по 2 на каждый из уроков №2–8/№10–12 (уроки №1 и №9 исключены пользователем) ✅ · формат принципиально другой, чем у практики уроков №2–12: там всегда готовый код и вопрос «что выведет», здесь — только текстовое условие без кода, ответ получить можно только реально написав и прогнав программу ✅ · урок №8 — исключение, условие содержит код с намеренной ошибкой (тот же паттерн `range(1, len(A))`, что и в его собственной теории) — индекс намеренно `k`, не `i`, чтобы не словить уже дважды встречавшуюся `[i]`-коллизию (v2.4.2) ✅ · урок №12 — задачи про формулы Excel, не про Python-код, согласованно с самим уроком №12 ✅ · все 20 ответов посчитаны прогоном эталонного кода (файлы — через `io.StringIO()`) перед показом, не подобраны на глаз ✅ · сохранено через `/api/uroki/save` (папка `uroki/urok_13/` создана автоматически), рендер `/preparation/13` и появление в каталоге проверены `Flask test_client()`, без `[i]`-коллизии и двойного экранирования ✅ · доступ к уроку по умолчанию закрыт всем — требует ручного включения в админке
>
> **v2.4.7 (2026-07-22):** урок №12 — добавлена функция СЧЁТЕСЛИ в раздел «Основные функции» по просьбе пользователя.
> `СЧЁТЕСЛИ(диапазон; условие)` добавлена в список функций (после `СЧЁТ`) + строка `=СЧЁТЕСЛИ(A1:A10; ">100")` в пример-`<pre>` ✅ · изменены точным поиском только эти 2 абзаца, остальные 20 + все 12 задач практики не тронуты ✅ · рендер `/preparation/12` чист — без `[i]`-коллизии и без двойного экранирования `&gt;` ✅
>
> **v2.4.6 (2026-07-22):** теория + практика урока №12 «Работа в Excel» — последний урок плана «Python с нуля».
> Теория (23 абзаца, без презентации): адресация, формулы/протягивание, `СУММ`/`СРЗНАЧ`/`МАКС`/`МИН`/`СЧЁТ`/`ЕСЛИ`, ВПР (тот же синтаксис, что в теории задания №3 — ссылка туда за подробностями, не дублирует), фильтры, сортировка ✅ · впервые в уроках использован список `<ul>/<li>` — проверено, рендерится как список, не escaped-текст ✅ · практика — 12 задач: арифметика формул (3), адресация/диапазон (2), по одной на 4 функции, `ЕСЛИ` в обе стороны (2), ВПР с игрушечной таблицей в условии (1) ✅ · найден и исправлен живой баг: `&gt;` вручную вписан в 2 задачах на `ЕСЛИ» — задвоилось экранированием в `&amp;gt;`, на странице видно буквально «&gt;» вместо «>»; исправлено обычным символом `>` ✅ · сохранено через `/api/uroki/save`, рендер `/preparation/12` чист — без `[i]`-коллизии и двойного экранирования ✅ · план «Python с нуля» (12 уроков) полностью укомплектован теорией; практика есть везде кроме урока №9 (решение пользователя)
>
> **v2.4.5 (2026-07-22):** практика урока №11 — 12 задач под текущий текст теории (после ручной правки пользователя).
> `read()` (1), последовательные `readline()` (2), `readlines()` количество/индекс (2), `for line in f` подсчёт/накопление (2), `strip()` отдельно и со `split()` (2), сборка двумерного списка из файла — 3 варианта включая сумму (мост к уроку №10) ✅ · содержимое каждого файла дано текстом в условии (`<pre>`), ответы получены прогоном реального кода поверх `io.StringIO()` с тем же содержимым — не подбирались вручную ✅ · сохранено через `/api/uroki/save` (только `practice.tasks`, теория не тронута), рендер `/preparation/11` чист, без `[i]`-коллизии ✅
>
> **v2.4.4 (2026-07-22):** теория урока №11 «Чтение файлов кодом» — без практики, без презентации.
> 28 абзацев: `open()`, `read()`/`readline()`/`readlines()`, `for line in f`, `f.close()`, `strip()` (впервые в курсе — в уроке №3 намеренно отсутствует), сборка двумерного списка из файла (мост к уроку №10) ✅ · `with open(...) as f:` сознательно не основной стиль примеров — визуализатор кода (урок №9) запрещает `ast.With`, поддерживает только `f = open(...)`/`f.close()`; упомянут одним абзацем как факт из реального Python, без противоречия с тем, что можно запустить в визуализаторе ✅ · сохранено через `/api/uroki/save`, рендер `/preparation/11` чист, без `[i]`-коллизии ✅
>
> **v2.4.3 (2026-07-22):** практика урока №10 — 12 задач под текущий текст теории (после ручной правки пользователя).
> Индексация `A[i][j]` (2), размер таблицы (2), ловушка `[[0]*m]*n` (1), перебор с индексами/без (2), построчный ввод (1), суммы всей таблицы/строки/столбца (3), изменение элемента (1) ✅ · диагонали и обе фразы-мостика пользователь убрал из теории сам до этого шага — задач на диагонали нет ✅ · все ответы проверены запуском кода, сохранено через `/api/uroki/save` (только `practice.tasks`, теория не тронута), рендер `/preparation/10` чист, без `[i]`-коллизии ✅
>
> **v2.4.2 (2026-07-22):** теория урока №10 «Двумерные списки и таблицы-матрицы» — без практики, симметрично уроку №9.
> 34 абзаца без презентации: создание/индексация, ловушка `[[0]*m]*n` (общая ссылка на строку), оба способа перебора, построчный ввод (только через `split()`/`int()` — `map()`/генераторы списков ещё не встречались в курсе), суммы таблицы/строки/столбца, диагонали ✅ · найден и исправлен на лету живой баг: `A[i][i]`-индексация (4 места) совпадала с BBCode-маркером курсива `[i]`, съедалась в рендере — переименовано `i → k` только в задетых местах ✅ · сохранено и проверено через `/api/uroki/save` + рендер `/preparation/10`, оба `Flask test_client()` ✅ · отдельно пользователь сам добавил через боевой конструктор скриншот блок-схемы (`factorial()`) к уроку №9 — вне этой сессии ✅
>
> **v2.4.1 (2026-07-22):** теория урока №9 «Работа с визуализатором» — без практики, по прямому решению пользователя.
> 23 абзаца без презентации-первоисточника: «Классический вид» (Memory-блоки по областям видимости, переменные-«шарики», Output) и «Блок-схема» (7 типов фигур + анимация вызова/возврата функций, включая рекурсию) ✅ · практических задач нет — урок только объясняет инструмент ✅ · мост от `print()`-отладки урока №8 к уроку №10 (двумерные списки) ✅ · сохранено через `/api/uroki/save` и проверено рендером `/preparation/9` — оба через `Flask test_client()` ✅
>
> **v2.4.0 (2026-07-22):** сразу после v2.3.0 пользователь сообщил «некоторые программы перестали запускаться» (3 приложенных примера, все реальные).
> Причина в `tracer.py`, независимо от Block Scheme: `MAX_STEPS` ограничивал только `'line'`-события, `'call'`/`'return'` дописывались в трассировку безусловно — безобидно до v2.1.0, но генераторные выражения внутри `any()/all()` (разблокированы тогда же) создают собственный Python-фрейм на каждое вычисление, и внутри цикла это давало call/return-событие на каждой итерации в полный обход лимита ✅ · замерено напрямую: пример пользователя (`combinations`+`all(genexpr)`) — 198.6 сек и 81.6 млн шагов вместо ожидаемых ≤1500 (`line: 1000`, `call`/`return`: по ~40.8 млн); изолированный синтетический повтор подтвердил механизм точно ✅ · пользователь выбрал общий счётчик на все типы событий + компенсирующее повышение `MAX_STEPS` с 1000 до 1500 — реализовано проверкой лимита в начале `tracer()` до диспетчеризации по `event`; при достижении лимита колбэк теперь поднимает `_StepLimitReached` (тихо перехватывается в `trace_code()`) и реально обрывает выполнение трассируемого кода через `sys.settrace()`, а не только прекращает запись шагов (подтверждено изолированным тестом на raise из колбэка) ✅ · побочный эффект — ранее документированное зависание `itertools.count()`/`cycle()` без `islice()` внутри `all()/any()` тоже теперь корректно обрывается на лимите вместо настоящего хана ✅ · обновлены 3 упоминания «1000 шагов» в UI на «1500» ✅ · все 3 примера пользователя — 0.01–0.02 сек вместо 41–198 сек, `truncated=True`, без ошибок; регрессия — простой код без обрезки, реальная ошибка (`ZeroDivisionError`) не путается с обрывом лимита, видимая глубина рекурсии сопоставима с прежней (~499 уровней), `open()`/`re`/`itertools` без регрессий, тестовый набор 35/35 без изменений; сквозной прогон через `Flask test_client()`/`/api/visualizer/trace`
>
> **v2.3.0 (2026-07-22):** 7 багов визуализатора, найденных пользователем при живом тестировании (плюс 1 не-баг ошибка в его же коде, 1 идея на будущее) — все исправлены.
> `from module import *` — `imported_names` в валидаторе получал буквальный `'*'` вместо реальных имён, `product`/`combinations`/`findall` отклонялись; исправлено подстановкой набора имён нужного модуля вместо `'*'` ✅ · `.add()` для `set` добавлен в `ALLOWED_METHODS` (тот же пробел, что был найден и отложен при v2.1.0) ✅ · `map`/`filter` добавлены в whitelist (обычный недосмотр, не сознательное решение) ✅ · нет стрелки цикл→следующее условие — ветка `condition` в `layoutSeq()`/`linSeq()` не проверяла `lastLoop`, исправлено общим хелпером `loopExitArrow()` (заодно закрыт непроверенный случай «цикл сразу за циклом») ✅ · дух-стрелка между диаграммами функций — регрессия v2.2.0, правка «закрыть висящий join» не проверяла реальную достижимость точки; `layoutCond()` теперь возвращает `{y, reachable}`, закрывающая заглушка рисуется только при `reachable: true` ✅ · `break`/`continue` не имели выхода из цикла на диаграмме — `continue` переиспользует существующий `backArrow`, `break` — новый отложенный коннектор (`registerBreak()`/`resolveBreaks()`), разрешается в момент, когда известна точка естественного выхода из того же цикла ✅ · доработка уже после первой версии: типичный случай `if k==5: break` последним в цикле давал некрасивое наложение линии break на «Нет»-стрелку того же условия и обратную стрелку цикла — `resolveBreaks()` теперь держит вертикаль на исходной колонке break до низа рамки цикла и поворачивает к цели только там; одно короткое пересечение вместо пучка линий, проверено рендером SVG на примере пользователя + 4 регрессиях ✅ · серый шарик анимации долетал позже, чем стартовал следующий шаг (длительность растёт с длиной стрелки до 1400мс, но не входила в ожидание перед следующим шагом) — пользователь выбрал полную синхронизацию: `bsAnimateFlowCursor()` получил `onComplete`, `bsStepNext()` учитывает его в общем `ballCount` ✅ · вся работа проверена 55 автоматическими проверками (35 validator/tracer + 20 block-scheme на реальных сценариях) + `Flask test_client()` на всех примерах пользователя + полный конвейер рендера без единого падения
>
> **v2.2.0 (2026-07-21):** три бага блок-схемы визуализатора — найдены пользователем на реальном рекурсивном коде, все исправлены.
> Однострочные `if/elif/else/for/while/def` (`if x: return y`) искажали диаграмму (весь текст «условие: тело» в подписи блока, пустая ветка «Да») — баг общий для всех 5 конструкций, не только `if` ✅ · исправлено препроцессором `expandInlineBodies()`/`findTopLevelColon()` в `block-scheme.js`, разворачивающим однострочную форму в стандартную двухстрочную ещё до разбора — сам парсер не менялся, тернарник в `return` сознательно не тронут (отдельный случай, вне scope по решению пользователя) ✅ · пропадающая стрелка «Нет» у последнего условия функции без `else` (диаграммы функций не имеют «Конец», join не с чем связать) — исправлено в `layoutSeq()`, дорисовывается отрезок со стрелкой при незакрытом join ✅ · шарик анимации летел от блока условия вместо `return` (оба делят номер строки, поиск узла брал первый результат) — `bsFindNodeByLine()` теперь предпочитает содержательный узел, `bsSetActiveBlock()` получил признак `isReturn` (`step.event === 'return'`) для точного переключения подсветки; `bsFindEdgePath()` сознательно не тронут ✅ · все три правки проверены отдельно (массив рёбер раскладки, реальный DOM из настоящего `renderSVG()` через самописный минимальный XML-парсер без внешних зависимостей, данные раскладки напрямую) + сквозной прогон исходного кода пользователя через `Flask test_client()` + регрессия 30/30 по прежнему набору визуализатора
>
> **v2.1.1 (2026-07-21):** MAX_STEPS 600→1000 по запросу пользователя + фиксация ранее не задокументированных правок.
> `tracer.py`: `MAX_STEPS` поднят с 600 до 1000 line-событий — глубина пошаговой рекурсии выросла примерно с 250–300 до 400–500 уровней; синхронно обновлены 3 захардкоженных упоминания «600» в UI (`visualizer.html`, `editor.js`, `block-scheme.js`) и справочные значения в CLAUDE.md ✅ · проверено — ровно 1000 line-событий на линейной рекурсии, регрессия 30/30 без изменений ✅ · задокументированы правки пользователя, сделанные параллельно с работой над визуализатором: телефон в футере `start.html`, сокращённая формулировка практической задачи урока №7 (`result`/`None`), перенос части предложений на отдельные абзацы + правка формулировки моста к уроку №9 в теории урока №8 ✅
>
> **v2.1.0 (2026-07-21):** расширение whitelist визуализатора Python-кода — `import` (re/itertools), `sys.setrecursionlimit()`, `open()`, `any()`/`all()`+GeneratorExp.
> Планирование заняло несколько раундов по каждому из 5 пунктов до начала реализации; пункт «turtle» отменён пользователем в процессе — слишком объёмная фича, нет реальной потребности в разборе задания №6 через визуализатор ✅ · `import re`/`itertools` — настоящий импорт, кастомный `__import__` в `tracer.py` (namespace изначально без builtins, иначе `import` падает), `math` сознательно не включён ✅ · `re`-методы (`group/groups/span/start/end/finditer` и др.) добавлены в общий `ALLOWED_METHODS` ✅ · в блок-схеме — новый тип блока «предопределённый процесс» для `import`-строк ✅ · `sys.setrecursionlimit()` — фейковая заглушка `_FakeSys` (не настоящий модуль), строка `import sys` разрешена, но физически вырезается перед компиляцией, потолок 10 000 (эмпирически проверено на деве до 3 000 000 без падения процесса), реальный лимит процесса сохраняется/восстанавливается вокруг каждой трассировки ✅ · `open()` — новый блок реальной загрузки файла на странице визуализатора, роут `/api/visualizer/trace` переведён на `multipart/form-data`, фейковый `open()` возвращает `io.StringIO()` от реально загруженного содержимого, `with open(...)` по-прежнему запрещён ✅ · `any()`/`all()` + разблокированный `ast.GeneratorExp` (структурно совпадает с уже разрешённым `ListComp`) ✅ · побочно найден (не исправлен, вне scope) предсуществующий пробел — `.add()` для `set` отсутствует в `ALLOWED_METHODS` ✅ · вся реализация проверена тестовым набором из 30 сниппетов + реальным `Flask test_client()` на `/api/visualizer/trace`, включая комбинированный сниппет со всеми 4 фичами и рендер блок-схемы через Node
>
> **v1.16.7–v1.16.9 (2026-07-21):** практика урока №7 + теория и практика урока №8 «Понимание и исправление ошибок».
> Урок №7 — 16 практических задач по актуальному тексту теории (после правки пользователя): вызов функций, `return`, функции без `return` (`None`), локальные/глобальные переменные, рекурсия, `import` ✅ · Урок №8 — впервые без презентации-первоисточника (как и уроки №9–12): синтаксические ошибки, ошибки отступов, чтение traceback, частые типы ошибок выполнения, логические ошибки на примере off-by-one в `range()`, приёмы отладки через `print()`; `try/except` сознательно не включён по решению пользователя ✅ · практика урока №8 — 15 задач ✅ · все ответы проверены запуском кода, рендер `/preparation/7` и `/preparation/8` проверен `Flask test_client()`
>
> **v2.0.0 (2026-07-20):** главная страница к рекламной кампании — карусель скриншотов вместо видео (обе секции) + патчи.
> Секция «Видеоразборы и уроки» (`start.html`): `<video>` + 2 постера → карусель из 8 реальных скриншотов (`static/images/slideshow/slide-1..8.jpg`), 3 DOM-узла крутят роли лево/центр/право по кругу (бесконечная очередь любой длины) ✅ · по запросу пользователя — карточки крупнее (720→900px), ротация быстрее (4.5с→3.2с) ✅ · на мобильном (≤640px) боковые карточки скрыты, одна фотография кроссфейдит на всю ширину (без изменений в JS — чисто CSS) ✅ · тот же механизм применён к секции «Визуализатор Python-кода» — JS вынесен в переиспользуемую `initSlideshow(stageId, images, intervalMs)`, 6 реальных скриншотов (`visualizer-1..6.jpg`), старые `.visualizer-*`/осиротевшие `.video-side*` CSS-классы удалены ✅ · обе карусели проверены вживую headless-скриншотами (Edge, десктоп+мобильный, до/после тика) ✅ · независимо — переведены последние англоязычные надписи визуализатора («Classic View»/«Block Scheme» → «Классический вид»/«Блок-схема», «Memory» → «переменными») ✅ · независимо — убран битый `<video>`-блок теории задания №1 (ссылался на несуществующий `Priroda.mp4`), гостю больше не виден сломанный плеер ✅ · все правки временные по замыслу — при появлении реального видео/новых скриншотов легко заменить содержимое без структурных изменений
>
> **v1.16.6 (2026-07-18):** практика уроков №4, №5, №6 — уроки №1–6 полностью укомплектованы.
> Урок №4 (условия) — 14 задач: `if`/`elif`/`else` на границах строгого/нестрогого сравнения (новые значения, не из теории), цепочка из нескольких `elif`, `and`/`or` ✅ · Урок №5 (циклы) — 13 задач: `while`, все 5 комбинаций параметров `range(a,b,k)` из теории на новых числах, `break`/`continue`; при составлении контрольного вопроса найдена и исправлена собственная ловушка — порядок проверки `continue` раньше `break` по пересекающемуся условию мог погасить срабатывание `break` ✅ · Урок №6 (списки/множества) — 19 задач: индексация, все 7 методов списков, множества (включая проверку уникальности `add()`), `sum()`, `in`, дедупликация через `set()` ✅ · формат ответов на изменяющие список методы спроектирован устойчивым к точечным правкам пользователя (подтвердилось на правке `A.pop()` → `A.pop(1)`, ответ не потребовал пересчёта) ✅ · заголовки задач — по установленному в v1.16.3 соглашению (тема раздела, не подсказка на ответ) ✅ · все ответы проверены запуском кода, рендер всех трёх уроков проверен `Flask test_client()` ✅
>
> **v1.16.3 (2026-07-18):** мобильный баг картинок + автоссылки на URL + практика уроков №2–3.
> Патч 1: картинки в теории/задачах/уроках вылезали за карточку на телефоне — `.img-small/medium/large` задавали фиксированный `px` `max-width` через `!important`, не учитывающий ширину контейнера; исправлено переводом на `max-width: min(px, 100%)` в `teoria.css`/`tasks.css`/`reshenie.css` (`exam.html` — та же проблема, но защищённый шаблон, не тронут) ✅ · Патч 2: голые URL в тексте теперь автоматически становятся кликабельными ссылками — `_linkify_urls()`, отдельный финальный шаг `replace_markers()`, не пересекающийся с логикой 12 разрешённых тегов; ссылки внутри `<code>`/`<pre>` сознательно пропускаются — при проверке найден реальный случай в теории задания №13 (фиктивный адрес в примере не должен быть кликабельным) ✅ · добавлен стиль ссылок в 4 CSS-файлах (раньше правила для `a` не было вовсе) ✅ · Практика: урок №2 — 19 задач, урок №3 — 18 задач, по актуальному (обновлённому пользователем) тексту теории каждого урока, все ответы проверены запуском кода перед добавлением ✅ · установлено соглашение — заголовки задач называют тему теории, а не конкретный ответ (по итогам ручной правки пользователя) ✅ · урок №1 — ссылка на PyCharm через Яндекс.Диск заменена на прямую (правка пользователя, включена в релиз по подтверждению) ✅
>
> **v1.16.1 (2026-07-17):** наполнен теорией урок №7 «Python: функции» + сгенерированная иллюстрация передачи аргументов/возврата.
> Теория по разделу 5 презентации пользователя (`def`/`return`, с параметрами и без, с результатом и без) — все примеры проверены запуском ✅ · два содержательных дополнения сверх презентации, каждое подтверждено пользователем отдельным вопросом: локальные/глобальные переменные и краткое введение в рекурсию со ссылкой на теорию задания №16 ✅ · раздел про `import`/подключение библиотек — по поручению пользователя из планирования v1.14.0 ✅ · впервые для урока начальной подготовки сгенерирована собственная иллюстрация (Pillow, по образцу теории заданий №4/№6) — схема передачи аргументов/возврата на примере `sos(4, 8)`, координаты стрелок вычислены точно по ширине шрифта, а не на глаз; палитра и Darcula-стиль карточек кода — в тон дизайн-системы сайта ✅ · черновик текста и иллюстрация показаны и утверждены пользователем по отдельности, перед переносом на сайт ✅ · перенос в `uroki/urok_07/urok_07.json` проверен рендером `/preparation/7` через `Flask test_client()` — `<pre>`-блоки и картинка на месте, живых BBCode-коллизий не найдено ✅
>
> **v1.16.0 (2026-07-17):** наполнены теорией уроки №2–6 «Python с нуля» + код-блок `<pre>` (Darcula) для многострочных примеров.
> Уроки №2 (ввод/вывод, типы, переменные), №4 (условия), №5 (циклы) — по презентации пользователя; урок №3 (строки) — полностью самостоятельно, в презентации раздела про строки нет вовсе (перескакивает с переменных на условия); урок №6 (списки/множества) — по презентации ✅ · в каждом уроке presentация проверена на смысловые пробелы против практики и по подтверждению пользователя дополнена: урок 3 — срез с шагом/`s[::-1]`/`.join()`/перебор строки в `for`/`isdigit()`/`isalpha()`; урок 4 — `and`/`or`; урок 5 — `range()` с одним аргументом и `break`/`continue`; урок 6 — `sum()`/`in`/`set()`-дедупликация + исправлена неточность презентации про «конец множества» ✅ · попутно найден и исправлен живой баг в собственном контенте урока №6 — `print(A[i])` совпадал с BBCode-маркером `[i]` (тот же баг класса, что в v1.12.18), переименовано в `A[k]` ✅ · новая фича — блок кода `<pre>` в whitelist `replace_markers()` (по образцу остальных 11 тегов), шаблон расширен на пропуск `<p>`-обёртки для целых `<pre>`-абзацев, 7 многострочных примеров в уроках 4–6 переведены на единый блок вместо построчных `<code>`+NBSP ✅ · визуальный стиль — тёмная Darcula-тема (`#2b2b2b`/`#a9b7c6`, в тон PyCharm из урока №1) с декоративной шапкой из трёх кружков через `pre::before` ✅ · попутно — стиль текстовых полей абзацев в конструкторе уроков (`.paragraph-textarea`, раньше голый `<textarea>` без CSS), применён только в `constructor_uroki.html` ✅ · полный прогон тестов (юнит-тесты фильтра, скан BBCode-коллизий по всем урокам 2–6, изолированный Jinja-рендер, smoke-тест гостевых страниц) — зелёный
>
> **v1.15.1 (2026-07-16):** патч — картинки не грузились у гостя (база заданий, тема теории №1, урок №1).
> Пользователь протестировал сайт глазами гостя и нашёл битые картинки сразу в трёх местах ✅ · причина — `before_request`-гейт открывал гостю страницы, но не файловые маршруты (`task_images`/`theory_images`/`theory_videos`/`theory_files`/`preparation_images`/`preparation_videos`/`preparation_files`), баг жил с v1.10.0, но был незаметен без реального контента с картинками ✅ · исправлено точечно в `check_auth()`: `task_images`/`task_files` открыты безусловно (база открыта для всех 27 номеров), файлы теории/уроков — только для `task_num==1`/`lesson_id==1` (как и сами страницы, не шире) ✅ · `variant_images`/`variant_files` не тронуты — их страницы гостю не открыты вообще ✅ · 15 проверок через `Flask test_client()`, все зелёные, подтверждено пользователем вживую ✅
>
> **v1.15.0 (2026-07-16):** первое реальное содержимое урока (Установка Python и PyCharm) + фикс живого бага `.reveal`.
> Пользователь самостоятельно через конструктор наполнил урок №1 — 33 абзаца + 17 скриншотов ✅ · независимый обзор контента по запросу пользователя (без правок) — найдены 5 опечаток/грамматических ошибок и содержательный пробел (абзац-анонс без самого объяснения) ✅ · опечатки исправлены, добавлены 4 абзаца с описанием чекбоксов установщика PyCharm (по общим знаниям, с пометкой сверить со скриншотом) + абзац про установку через Microsoft Store ✅ · найден и исправлен живой баг вёрстки — карточки теории/практики урока никогда не проявлялись из-за анимации `.reveal` (`IntersectionObserver`, порог 12% никогда не достигался для длинной карточки), убран класс `reveal` по прецеденту `teoria_zadanie.html` ✅ · подтверждено пользователем вживую в браузере ✅
>
> **v1.14.0 (2026-07-15):** согласован план из 12 уроков «Python с нуля» + построен конструктор уроков начальной подготовки.
> Учебный план (по презентации пользователя + собственному брейнстормингу): установка Python/PyCharm → ввод/вывод+типы+переменные → строки → условия → циклы → списки и множества → функции → понимание/исправление ошибок → визуализатор кода → двумерные списки → чтение файлов кодом → Excel напрямую ✅ · старое содержимое `urok_01`/`urok_02` («Системы счисления», одна — тестовая заглушка) удалено, заменено на 12 пустых уроков-скелетов (только заголовки, наполнение контентом — следующий шаг) ✅ · новый `templates/constructor_uroki.html` — самостоятельный клон конструктора теории (не единый мультирежимный инструмент), полная функциональная параллель: выбор урока, массовый ввод с разбиением на абзацы, тулбар форматирования, картинки, редактор практики с переключателем типа ответа ✅ · схема урока расширена: `video` → `videos` (массив, как у теории), новое поле `description` (для каталога `/preparation`), новая папка `practice_files/` (раньше не существовала) ✅ · `preparation_lesson.html` дополнен рендером нескольких видео, заголовка и файла практической задачи (раньше не отображались) ✅ · backend: `GET/POST /api/uroki/*`, `GET /preparation_files/<id>/<file>`, `GET /constructor_uroki`, 3 новых save-хелпера ✅ · 37 проверок через `Flask test_client()`, все зелёные ✅
>
> **v1.13.0 (2026-07-15):** заявка вместо регистрации + ручное создание пользователей + Telegram-уведомления, по итогам гостевого UX-аудита.
> Гостевой UX-аудит (весь путь незарегистрированного посетителя через `Flask test_client()`) выявил главный барьер конверсии: самостоятельная регистрация требовала пароль сайта, узнаваемый только лично у владельца — стоп для органического посетителя с рекламы/соцсетей ✅ · `/register` переписан из формы регистрации в форму заявки (Фамилия/Имя/Телефон/способ связи Telegram-VK-MAX) — пишет в новую таблицу `site_applications`, не создаёт аккаунт напрямую ✅ · новый роут `/admin/create_user` — админ вручную вводит логин/пароль (пароль текстовым полем, сразу виден), с префиллом имени из заявки ✅ · новый блок «Заявки» в `/admin` — необработанные таблицей с кнопками «Создать аккаунт»/«Обработана», обработанные в свёрнутом списке с кнопкой возврата (переключатель в обе стороны, ничего не удаляется) ✅ · Telegram-уведомления о новой заявке через Bot API (`urllib`, без новых зависимостей), не ломают сохранение заявки при сбое — подтверждено пользователем на реальном боте ✅ · главная страница обновлена под новую логику в 4 местах (навбар, гостевой блок, карточка «Запишись на занятие», плавающий виджет) ✅ · попутно: постатейная детализация прогресса ученика в админке (`/admin/user/<id>/lesson|theory/<id>`) ✅, фикс кэширования картинок на главной (`?v={{ app_version }}`) ✅ · **`SITE_PASSWORD` сознательно не тронут** по решению пользователя, хотя больше не используется
>
> **v1.12.27 (2026-07-14):** наполнена теория+практика заданий №19–27 — весь диапазон теории 1–27 закрыт.
> Задания №19–21 (выигрышная стратегия, связанная тройка): по решению пользователя ОДИН общий текст на всех трёх страницах теории; презентация проверена кодом — найдены и исправлены лишний ход «+2» и путаница двух формулировок Вопроса №19 (эталонные ответы презентации 34/пустой список были неверны, правильно 18/21) ✅ · Задание №22 (процессы): доминирующий тип «K процессов одновременно» (31% базы) требует не обычного расписания, а намеренных задержек (2 подтипа, метод критического пути) — не было в презентации, добавлен и проверен на реальных файлах; найдена ловушка «минимальное время выполнения» = момент завершения, не длительность ✅ · Задание №23 (перебор программ): все примеры презентации проверены + решены 5 незаполненных слайдов + новый подтип «команды над цифрами числа» ✅ · Задание №24 (regex): найдена и исправлена ловушка порядка альтернатив (короткий префикс после длинного даёт заниженный ответ) + добавлен целый недостающий тип «окно с N вхождениями» (~25% базы) с исправленной ошибкой границы окна ✅ · Задание №25 (делители/маски): все примеры проверены; попутно найден и исправлен живой баг сайта — `loop.parent` не существует в Jinja2, ломал рендер `answer_grid` в практике теории ✅ · Задание №26 (жадные алгоритмы) — впервые переработано: старая заглушка (2 абзаца + 8 тестовых задач) заменена на 28 абзацев (7 типов, 5 проверены на реальных файлах) + 20 новых задач ✅ · Задание №27 (кластеризация звёзд) — впервые без презентации: метод восстановлен с нуля (MST-кластеризация + медиана-центр + таблица классификации звёзд) и проверен на 4 парах реальных файлов (16 чисел, все совпали); шаблон теории расширен на несколько файлов на задачу (`task.files`) ✅ · практика всех 9 заданий — по 20 задач возрастающей сложности ✅ · **итого теория реально наполнена по всем 27 темам**
>
> **v1.12.18 (2026-07-13):** наполнена теория+практика заданий №11–18.
> Задание №11 (кол-во информации): формулы презентации проверены + новые составной алфавит и обратные задачи (перебор N/K/G/P), 5 примеров перепроверены ✅ · Задание №12 (Машина Тьюринга): из презентации только последние 3 слайда (устаревший формат остальных) — метод «обратный подсчёт» покрывал лишь ~28% базы, добавлены прямое преобразование и разворот результата (полный симулятор МТ) ✅ · Задание №13 (адресация): 6 методов презентации проверены + широковещательный адрес и подсчёт по чётности бит (закономерность «ровно половина» подтверждена полным перебором) ✅ · Задание №14 (СС): 4 метода презентации + доминирующий тип «выражение минус x с границей» (~77% базы, не было в презентации) ✅ · Задание №15 (логика): 4 типа презентации + `div()` + критичная находка про диапазон перебора (маленький диапазон давал неверный ответ 9802 вместо 1176126) ✅ · Задание №16 (рекурсия): `sys.setrecursionlimit()` независимо проверен (без него реальный `RecursionError`) + взаимная рекурсия F(n)/G(n) ✅ · Задание №17 (обработка последовательности): произвольные/соседние пары презентации + новые тройки/симметричные пары/локальные экстремумы; попутно найден и исправлен живой баг `replace_markers()` — коллизия `a[i]` с BBCode-маркером `[i]`, затронувшая уже запущенное задание №9 (исправлено ретроактивно) ✅ · Задание №18 (Робот на поле) — впервые без презентации, собственные парсеры ODS/XLSX для чтения стен по границам ячеек, DP-метод, обнаружены 3 подтипа реальной базы (стандартный/фиксированный угол/другая механика — последний исключён из практики) ✅ · практика всех 8 заданий — по 20 задач возрастающей сложности ✅ · итого реально наполнены темы №1–18 (задание №26 наполнено ранее, но требует переработки)
>
> **v1.12.10 (2026-07-09):** наполнена теория+практика заданий №6–10.
> Задание №6 (Черепаха): теория по презентации + 2 сгенерированные иллюстрации (ловушка «повторы≠стороны», объединение прямоугольников, проверено на реальной задаче базы id=8) ✅ · практика — 20 задач (6 одна фигура → 14 две фигуры, возрастающей сложности) ✅ · Задание №7 (кодирование/передача изображений и звука): теория по презентации + найденный на реальной базе раздел «осторожно с округлением» (направление зависит от формулировки, проверено на паре контрастных задач) ✅ · практика — 20 задач (8 изображение + 6 звук + 6 передача, round-robin) ✅ · Задание №8 (перебор слов и СС): сверка с базой (127 задач) выявила пробел — презентация не покрывает системы счисления (28% базы); по решению пользователя написан и дважды перепроверен на реальных задачах (5100, 3625) новый раздел (метод «цифра-разделитель») ✅ · практика — 20 задач (14 слова + 6 СС, пропорция как в базе) ✅ · Задание №9 (большие данные) и №10 (поиск в тексте) — впервые без презентации, целиком по изучению реальной базы; для №9 разобран реальный `.ods`-файл вручную (zip+XML, `pandas`/`odfpy` недоступны из-за отсутствия pip-доступа), 4 примера перепроверены на настоящих файлах (6322, 138, 27987, 2354) ✅ · для №10 обнаружено, что файл почти всегда содержит больше текста, чем нужно (найдено превышение в ~24 раза при поиске по всему файлу вместо нужных 2 глав) и что в заголовках глав встречаются опечатки-омоглифы (кириллическая «Х» вместо «X», «X0» вместо «X»), ломающие наивный поиск границ; 2 примера перепроверены на реальных файлах (46, 57) ✅ · практика №9–10 — по 20 задач с копированием приложенных файлов ✅ · итого реально наполнены темы №1–10 (задание №26 наполнено ранее, но требует переработки)
>
> **v1.12.5 (2026-07-08):** наполнена теория+практика заданий №4 и №5 по презентациям пользователя.
> Задание №4 («Условие Фано»): теория по методу двоичного дерева из презентации + 2 случая (ограниченный/весь алфавит, во втором — резерв узла, проверено на реальной задаче базы) ✅ · 4 сгенерированных иллюстрации дерева (Pillow) вместо сканов слайдов ✅ · практика — 20 задач возрастающей сложности из базы (122 задачи) ✅ · Задание №5 (алгоритмы для исполнителей): справочник функций Python + разобранный пример с кодом (перепроверен запуском) ✅ · переводы в СС 3–9 и 11–36 по коду пользователя, найдена и исправлена опечатка `a //= 3` → `a //= 11` ✅ · второй вид задания переписан по рисунку пользователя (метод общей цифры b) + сгенерированная иллюстрация ✅ · практика — 20 задач возрастающей сложности из базы (125 задач) ✅ · итого реально наполнены темы №1–5 (задание №26 наполнено ранее, но требует переработки)
>
> **v1.12.3 (2026-07-08):** наполнена теория+практика заданий №2 и №3 + фикс списков/кода в теории + единый стиль `<code>` + реальный прогресс на главной.
> Фикс `<ul>/<ol>/<li>/<code>` в теории — расширен `replace_markers()`, убрана лишняя `<p>`-обёртка вокруг абзацев-списков в 4 шаблонах, `exam.html` не тронут ✅ · единый синий стиль `<code>` в `design-system.css` (все 16 страниц) + кнопка `</>` в конструкторе теории ✅ · блок «Ваш прогресс» на главной считает реальные данные (`load_homepage_progress()`) вместо захардкоженных чисел, пустое состояние для новых пользователей ✅ · заменены 2 фото-превью на главной на реальные скриншоты сайта ✅ · Задание №2: практика пересобрана на 20 задач возрастающей сложности ✅ · Задание №3: теория написана с нуля (разбор реального `.xlsx`, структура БД, функция ВПР) по правкам пользователя ✅, практика пересобрана на 20 задач с приложенными файлами БД ✅ · итого реально наполнены темы №1–3 (задание №26 наполнено ранее, но требует переработки)
>
> **v1.12.0 (2026-07-07):** конструктор теории доведён до полной параллели со старым конструктором + наполнена теория задания №1.
> 4-этапная доработка `constructor_theory.html`: сетка выбора задания + массовая вставка ✅ · тулбар форматирования (+ фикс `replace_markers()` для `<strong>/<em>/<u>`) ✅ · картинки в теории (`/api/theory/save` → FormData/multipart) ✅ · полный редактор практических задач (абзацы+тулбар, картинки, файл, переключатель типа ответа с взаимоисключаемостью полей, сворачиваемые карточки) ✅ · несколько видео на тему (`theory.videos`, автоконвертация старого формата, попутно исправлен баг падения при сохранении) ✅ · увеличен размер полей абзаца ✅ · Задание №1: исправлено 5 опечаток в тексте теории ✅, практика пересобрана на 20 задач возрастающей сложности из базы (старые 2 тестовые удалены, у одной был конфликт полей ответа) ✅
>
> **v1.11.0 (2026-07-06):** подготовка сайта к роли «визитной карточки» — 3 из 4 пунктов.
> Убрана версия сайта из публичного футера (техника cache-busting не тронута) ✅ · `reshenie.html` переведён на общий дизайн сайта (`base.html`/`design-system.css`), функциональность сохранена бит-в-бит, `exam.html` не тронут ✅ · Яндекс.Метрика + Open Graph/twitter:card теги (без exam.html и админки) ✅ · Пункт 4 (наполнение теории/уроков) — перенесён на следующую сессию
>
> **v1.10.2 (2026-07-06):** патч — кнопка перехода к заданию + cache-busting статики.
> Обнаружено на живом сайте: диаграмма статистики без стилей из-за 45-дневного кэша CSS без версионирования — добавлен `?v={{ app_version }}` на все 20 ссылок CSS/JS в 16 шаблонах ✅ · эмодзи-ссылка «🔗» на задание в разборе попытки заменена кнопкой «Перейти к заданию» в отдельном столбце ✅
>
> **v1.10.1 (2026-07-06):** патч — исправлен баг списка тем теории.
> `theory()` дублировал прямой SQL-запрос вместо вызова `check_theory_access()` — тема №1 ошибочно показывалась «Закрыто» залогиненным пользователям без разрешения админа (гостей баг не касался) ✅ · заменено на вызов `check_theory_access()` ✅
>
> **v1.10.0 (2026-07-06):** 6 точечных доработок платформы — завершено.
> Пересборка вариантов (20 новых вместо 14 старых) ✅ · Связь статистики попытки с базой заданий ✅ · Диаграмма решаемости по 27 номерам заданий (на /stats и в админке) ✅ · База заданий/каталоги/первый урок+тема открыты гостям, найден и переработан глобальный `check_auth()` ✅ · Регистрация: обязательное «Имя» + подсказка про пароль сайта ✅ · Версия сайта в футере + замена эмодзи-замков на эмодзи+текст ✅ · Итоговый смоук ~25 маршрутов без 5xx ✅
>
> **v1.3.0 (2026-06-16):** аудит безопасности и целостности данных завершён.
> D-1 ✅ D-2 ✅ D-3 ✅ · P0–P2 ✅ · XSS ✅ · SQL ✅ · P3 Этап 1 ✅
>
> **v1.4.0 (2026-06-16):** расширенный редактор задач — завершён.
> Этап 1 ✅ (форматирование B/I/sup/sub) · Этап 2 ✅ (изображения) · Этап 3 ✅ (файлы)
>
> **v1.5.0 (2026-06-16):** редизайн главной страницы (`start.html`) — завершён.
> Dropdown navbar ✅ · Мобильная версия ≤480px ✅ · Backend не менялся · Logout POST+CSRF сохранены · 21/21 OK
>
> **v1.5.3 (2026-06-16):** визуальная иерархия главной страницы (`start.html`) — завершён.
> Этап 1 ✅ (Hero двухколоночный + mockup) · Этап 2 ✅ (stats-row полоса, start-flow маршрут, синяя мини-задача, фиолетовый калькулятор, advantages-panel) · Этап 3.1 ✅ (feature grid 2×2, порядок карточек) · Этап 3.2 ✅ (контраст #64748b → #94a3b8, WCAG AA) · Этап 3.3 ✅ (interactive-zone) · Backend не менялся · последняя проверка: 9/9 OK
>
> **v1.5.4 (2026-06-17):** UX-доработка главной страницы (`start.html`) — завершён.
> Этап 1 ✅ (убран блок «Новые варианты», усилена navbar) · Этап 2 ✅ (последовательная зона, 24/24 OK) · Этап 3 ✅ (roadmap-карточка split-card; убраны фон и рамки .interactive-zone; JS/backend не менялись)
>
> **v1.5.5 (2026-06-17):** улучшение структуры и конверсии главной страницы (`start.html`) — завершён.
> Этап 1 ✅ (устранение дублирования stats-row, 10/10 OK) · Этап 2 ✅ (блок преимуществ — боли ученика, 7/7 OK) · Этап 3 ✅ (именованные шаги индикатора, 12/12 OK) · Backend не менялся · 29/29 OK
>
> **v1.5.6 (2026-06-17):** персонализация воронки главной страницы (`start.html`) — Этап 1 выполнен.
> Этап 1 ✅ (перенос progress-section/guest-cta-section после features-section, 13/13 OK) · Jinja2-условия сохранены · CSS/JS/backend не менялся · следующие этапы — позже
>
> **v1.6.0 (2026-06-17):** визуальный редизайн главной страницы под светло-голубую тему — выполнен.
> Этап 1 ✅ (глобальные токены: body, headings, btn-secondary) · Этап 2 ✅ (hero-mockup, мини-статы) · Этап 3 ✅ (start-flow карточки) · Этап 4 ✅ (interactive zone: minitask/calculator/roadmap/iz-cta) · Этап 5 ✅ (advantages/features/updates/news) · Этап 6 ✅ (progress-card, guest-cta-card) · Этап 7 ✅ (глобальная проверка, ~55 CSS-селекторов) · HTML/JS/backend/navbar/CSRF не тронуты
>
> **v1.6.1 (2026-06-18):** живой интерфейс главной страницы — выполнен.
> Этап 1 ✅ (hover translateY для start-card/update-card/progress-card/guest-cta-card/advantage-item + WCAG-контраст level-цветов) · Этап 2 ✅ (уплотнение интерактивной зоны: padding 64→36px, margin 44→22px, dot 10→14px, connector 52→72px, max-width 580→680px) · Этап 3 ✅ (scroll reveal IntersectionObserver + CSS transitions + stagger-delays, 19 элементов) · Этап 4 ✅ (count-up анимации: hero-mini-stat-num + progress-num, easeOutCubic 900ms) · 19/19 OK · JS-бизнес-логика/backend/navbar/CSRF не тронуты
>
> **v1.6.2 (2026-06-18):** горизонтальная динамика интерактивной зоны — выполнен.
> slide+fade 1→2 ✅ (minitask влево, calculator справа) · slide+fade 2→3 ✅ (calculator влево, roadmap+CTA справа) · коннекторы stepper ✅ (scaleX fill) · overflow:hidden ✅ · minHeight-фикс ✅ · reduced-motion ✅ · 12/12 OK · checkMiniTask/checkReadiness/backend/navbar/CSRF не тронуты
>
> **v1.6.3 (2026-06-18):** усиление ценностного предложения главной страницы — выполнен.
> Этап 1 ✅ (Hero: +4-я метрика «100+ видеоразборов», repeat(4,1fr), адаптив 480px 2×2) · Этап 2 ✅ (удалён блок новостей: HTML + 7 CSS-правил, 0 упоминаний news) · Этап 3 ✅ (рефрейминг 3 карточек обновлений на ценность для ученика) · Этап 4 ✅ (новый блок .video-preview-section: 3 карточки, reveal, hover, адаптив 3→2→1 колонки, без iframe/video/YouTube) · Этап 5 ✅ (ease → ease-out в reveal, скорость и stagger не изменены) · 17/17 OK · JS/backend/маршруты/navbar/CSRF не тронуты
>
> **v1.6.4 (2026-06-18):** визуальные и конверсионные улучшения главной страницы — выполнен.
> HTML5-видеоплеер ✅ · Боковые постеры видео ✅ · Исправление clipping (overflow-x) ✅ · CTA-секция записи на занятие ✅ · Плавающая карточка связи ✅ · Система .card-stripe для update-card ✅ · 22/22 OK · JS/backend/маршруты/navbar/CSRF не тронуты
>
> **v1.6.5 (2026-06-19):** полный редизайн пользовательских страниц — завершён.
> Этап 0 ✅ (base.html, компоненты, design-system.css, scroll-reveal.js) · Этап 1 ✅ (login/register/csrf_error + auth.css) · Этап 2 ✅ (variant_list/teoria/preparation/tasks_list + catalogs.css) · Этап 3 ✅ (profile + profile.css; stats/attempt_detail + stats.css) · Этап 4 ✅ (choose_mode + variants.css; tasks_view + tasks.css; teoria_zadanie + teoria.css; preparation_lesson + preparation_lesson.css) · 15/15 страниц ✅ · Аудит 246/246 OK · Backend/маршруты/CSRF/схема БД не тронуты
>
> **v1.7.0 (2026-07-01):** интеграция визуализатора Python-кода (visual-code) — завершена.
> `validator.py`/`tracer.py` перенесены без изменений ✅ · роуты `GET /visualizer` + `POST /api/visualizer/trace` в app.py, доступ только авторизованным ✅ · `templates/visualizer.html` автономный шаблон ✅ · `static/visualizer/` (CSS+JS, `api.js` адаптирован под относительный путь + CSRF) ✅ · кнопка в `components/navbar.html` и продублирована в `start.html` (десктоп рядом с dropdown, мобильная — внутри dropdown) ✅ · wall-clock timeout и rate limiting на трассировку — сознательно отложены
>
> **v1.7.1 (2026-07-01):** визуальная унификация визуализатора Python-кода с сайтом — завершена.
> Собственный header визуализатора заменён на общий `components/navbar.html` ✅ · светлая тема визуализатора перекрашена под палитру сайта (`--primary: #2563EB`, общий градиентный фон) ✅ · тёмная тема сохранена, `.dark .navbar` инвертирован в светлый вариант ✅ · контраст новой навбар-палитры проверен численно (4.63:1–17.85:1, WCAG AA+) ✅ · подтверждено пользователем визуально в браузере ✅ · Backend не менялся
>
> **v1.7.2 (2026-07-01):** диаграммы функций в Block Scheme + витрина визуализатора на главной — завершена.
> `def`/`return` в Block Scheme: отдельная диаграмма на каждую функцию, анимация вызова/возврата (в т.ч. рекурсия) ✅ · исправлен источник шарика Memory/Output после `return` ✅ · исправлено наложение диаграмм друг на друга (измерение реальной левой границы вместо фиксированного отступа) ✅ · `.visualizer-preview-section` на `start.html` (видео + 2 фото, по образцу `.video-preview-section`) ✅ · донастроены отступы/ширина области под диаграмму ✅ · вся логика проверена прогонами в Node на реальных данных `tracer.py`, без браузера · Backend не менялся
>
> **v1.8.0 (2026-07-02):** наполнение базы задач для Заданий №1–3 реальным контентом с kompege.ru — завершено.
> Задание 1 ✅ (113 задач, графы дорог, картинки + автоклассификация подтипов ~80/20) · Задание 2 ✅ (116 задач, таблицы истинности — рендер в PNG через matplotlib, формулы LaTeX/HTML-сущности → юникод) · Задание 3 ✅ (88 задач, базы данных — несколько картинок на задачу + приложенный файл с приоритетом форматов xlsx→ods→xls) · старые тестовые задачи и orphan-файлы удалены · полная проверка целостности после каждой партии (дубли id, потерянные файлы, битые ответы) · код проекта не менялся, только содержимое `tasks/task_01–03/`
>
> **v1.8.1 (2026-07-02):** продолжение наполнения базы задач для Заданий №4–10 реальным контентом с kompege.ru — завершено.
> Задание 4 ✅ (122, кодирование текста — таблицы буква→код рендерятся в PNG через Pillow) · Задание 5 ✅ (125, без таблиц/файлов) · Задание 6 ✅ (78, исполнители — исправлена потеря контента в `<div>`/`<pre>` + разбалансировка тегов при `<br/>`) · Задание 7 ✅ (122, кодирование звука — найден и исправлен пропуск дубля при сравнении по сырому тексту) · Задание 8 ✅ (127, комбинаторика слов — правило «только именованный источник» подтверждено без исключений даже для difficulty=«гроб») · Задание 9 ✅ (91, электронные таблицы — добавлена загрузка файлов xlsx→ods→xls→csv, исправлена потеря контента в `<ol>`/`<ul>`/`<li>`, уточнена дедупликация по паре текст+ответ) · Задание 10 ✅ (115, анализ текстовых файлов — приоритет docx→doc→odt→txt→rtf→pdf) · ретроактивно убраны авторские подписи из заданий 1–4 (79 шт.) · убрано скругление углов картинок (`tasks.css`, `reshenie.html`, `exam.html`) · код `app.py` не менялся, только содержимое `tasks/task_04–10/`
>
> **v1.8.2 (2026-07-02):** продолжение наполнения базы задач для Заданий №11–18 + сплошной аудит базы 1–18 — завершено.
> Задание 11 ✅ (120) · Задание 12 ✅ (60, Машина Тьюринга — новый рендерер таблиц команд, пул расширен сверх именованного по решению пользователя) · Задание 13 ✅ (100, IP-адресация — пул тоже расширен, исправлен баг regex `<pre>`/`<p>`) · Задание 14 ✅ (128, системы счисления) · Задание 15 ✅ (130, логические выражения — разработан конвертер LaTeX→юникод) · Задание 16 ✅ (118, рекурсивные функции — конвертер LaTeX расширен, исправлен разбор Quill/KaTeX-спанов) · Задание 17 ✅ (85, первое использование формата `answers: [a,b]`) · Задание 18 ✅ (110, Робот на сетке — картинка везде лишь иллюстрация, реальные данные в файле) · сплошной аудит всех 2029 задач 1–18 по запросу пользователя: исправлены необработанный LaTeX (51+7 задач в 14/13), 1 пропущенная авторская подпись (задание 8), 1 настоящий дубль (задание 2), 40 пустых тегов-обёрток по всей базе · код `app.py` не менялся, только содержимое `tasks/task_11–18/` и точечные правки `tasks/task_02,08,13,14/`
>
> **v1.8.3 (2026-07-03):** продолжение наполнения базы задач для Заданий №19–25 — завершено.
> Задания 19–21 ✅ (по 113 — «связанная тройка», параллельные id + `LINKED_TRIO` в конструкторе) · Задание 22 ✅ (110, пул расширен сверх именованного, исправлен баг «жирный текст» — протёкшие формулировки-заглушки) · Задание 23 ✅ (119, конвертер LaTeX ограничен рамками `\(...\)` — иначе портил URL) · Задание 24 ✅ (75 из ~148 доступных — единственное урезание по объёму, файлы ~8 МБ каждый) · Задание 25 ✅ (120, первое полностью на `answer_grid`, декодирована встроенная base64-картинка) · исправлен критический баг коллизии XSS-плейсхолдеров в `replace_markers()` (`app.py`) — единственное изменение кода в этой версии, помимо точечной правки конструктора
>
> **v1.9.0 (2026-07-03):** завершение наполнения базы задач — Задания №26–27 + финальный аудит 1–27 — завершено.
> Задание 26 ✅ (124, обработка чисел из файла — заглушка использовала несовместимый с частичным зачётом формат `answers`, заменена на `answer_grid`) · Задание 27 ✅ (90 = 23 именованных + 67 анонимных, кластеризация звёзд — расширена схема задачи под несколько файлов, новое поле `"files"`, правки в `tasks_view.html`/`reshenie.html`/`exam.html`) · весь диапазон 1–27 закрыт (2924 задачи суммарно) · финальный сплошной аудит 1–27: 23/27 чисто, исправлен последний реальный дефект (`\left|…\right|` в одной задаче задания 27), 4 находки — подтверждённые безопасные ложные срабатывания · смоук-тест всех 27 страниц — 200 везде

## Следующий этап

### Возможные доработки

- [ ] **Визуализатор: декомпозиция `print(..., [comprehension])` в реальный цикл на блок-схеме** — сейчас сложные `print` с list comprehension внутри (частый паттерн в реальных решениях ЕГЭ, напр. задания №19-21) рисуются одним плоским блоком с текстом comprehension внутри, а не как настоящая структура цикл+условие+накопление. Отмечено пользователем при живом тестировании (2026-07-22) как «не баг, но на подумать» — заметно крупнее по объёму, чем обычная точечная правка, требует отдельного планирования.
- [ ] **Кастомная/стилизованная страница 404** — сейчас у залогиненных пользователей на несуществующей ссылке голая страница Werkzeug без брендинга сайта; отмечена пользователем как желаемая будущая доработка (2026-07-15).
- [ ] **Вернуть видео на главную вместо карусели скриншотов** — обе секции («Видеоразборы и уроки», «Визуализатор Python-кода») временно переведены на карусель фото в v2.0.0 из-за нехватки времени на съёмку перед рекламной кампанией; когда видео будут готовы — заменить разметку `.slideshow-stage`/`initSlideshow(...)` обратно на `<video>`-плеер (старые файлы `platform-overview.mp4`/`visualizer-overview.mp4` и постеры `video-preview-*.jpg`/`visualizer-preview-*.jpg` на диске не удалялись, лежат неиспользуемыми).
- [ ] **Теория задания №1 — видео отсутствует** — блок `<video>` убран в v2.0.0 (ссылался на несуществующий `Priroda.mp4`); при появлении реального видео вернуть поле `video`/`videos` в `theory/task_01/theory.json`.
- [ ] **Наполнение уроков реальным контентом** — уроки №1–8 полностью укомплектованы теорией и практикой (урок №1 — пользователем самостоятельно, v1.15.0; уроки №2–6 — теория по презентации/самостоятельно + практика Claude Code, v1.16.0–v1.16.6; урок №7 — теория v1.16.1, практика v1.16.7; урок №8 — теория и практика впервые без презентации, v1.16.8–v1.16.9). Остальные 4 урока (№9–12: визуализатор, двумерные списки, файлы, Excel) — ещё пустые скелеты, без presentation-материала, писать с нуля. Наполнять по одному за раз. Урок №9 («Работа с визуализатором кода») теперь может опираться на расширенный whitelist визуализатора из v2.1.0 (`import re/itertools`, `sys.setrecursionlimit()`, `open()`, `any()/all()`).
- [ ] **Визуализатор: `.add()` для `set` отсутствует в `ALLOWED_METHODS`** — найдено побочно при работе над v2.1.0, предсуществующий пробел вне scope того плана; `s.add(x)` из теории урока №6 сейчас не выполнится в визуализаторе. Чинится одной строкой в `validator.py`, но требует отдельного согласования, т.к. не входил в утверждённый список из 4 пунктов.
- [ ] **Визуализатор: `itertools.count()`/`cycle()` внутри `all()`/`any()` без `islice()`** — бесконечный цикл, если условие никогда не станет `False`/`True`; принято как есть при согласовании v2.1.0, но стоит вернуться при первом реальном инциденте зависшего запроса.
- [ ] **Задание №18 — сценарий «Робот разрушается»/ВЕРХ-НИЗ** — ~17% реальной базы использует другую механику (не тупиковая клетка с накопленной суммой, а гибель робота при столкновении со стеной, либо два соревнующихся исполнителя), не описанную в текущей теории; сознательно исключено из практики v1.12.18, требует отдельного раздела теории при следующем возврате к заданию.
- [ ] **Задание №26 — два типа без сверки с реальным файлом** — «минимальное число групп» и «два окна с лимитом очереди» проверены только по логике на собственных тестовых данных (v1.12.27); при обнаружении в реальной базе задачи с точно такой механикой стоит сверить численно.
- [ ] **Задание №27 — таблица классификации звёзд не проверена целиком** — подтверждены численно только G/K/M/O (цвета) и III/V/VI/VII (типы светимости); остальные буквы (B, A, F) и типы светимости (I, II, IV) заполнены по стандартной астрономической логике, но не перепроверены на реальной задаче с этими конкретными обозначениями — стоит перепроверить при первой встрече такой задачи.

### v1.16.6 — Практика уроков №4, №5, №6 — уроки №1–6 полностью укомплектованы (выполнен, 2026-07-18)

- [x] Урок №4 (условия) — 14 задач: `if`/`elif`/`else` на границах строгого/нестрогого сравнения (новые значения, не из теории), цепочка из нескольких `elif`, `and`/`or`
- [x] Урок №5 (циклы) — 13 задач: `while`, все 5 комбинаций параметров `range(a,b,k)` на новых числах, `break`/`continue`
- [x] При составлении контрольного вопроса урока №5 найдена и исправлена собственная ловушка — порядок проверки `continue` раньше `break` по пересекающемуся условию мог погасить срабатывание `break`
- [x] Урок №6 (списки/множества) — 19 задач: индексация, 7 методов списков, множества (включая проверку уникальности `add()`), `sum()`, `in`, дедупликация через `set()`
- [x] Формат ответов на изменяющие список методы устойчив к точечным правкам пользователя (подтвердилось на `A.pop()` → `A.pop(1)`)
- [x] Заголовки задач — по установленному в v1.16.3 соглашению (тема раздела, не подсказка на ответ)
- [x] Все ответы проверены запуском кода, рендер `/preparation/4`, `/preparation/5`, `/preparation/6` проверен `Flask test_client()`

### v1.16.3 — Мобильный баг картинок + автоссылки на URL + практика уроков №2–3 (выполнен, 2026-07-18)

- [x] Патч: `.img-small/medium/large` переведены на `max-width: min(px, 100%)` в `teoria.css`/`tasks.css`/`reshenie.css` — картинка больше не может быть шире контейнера на телефоне
- [x] `exam.html` (та же проблема во встроенном `<style>`) сознательно не тронут — защищённый шаблон, ждёт отдельного решения
- [x] Патч: автоссылки на голые `https://...` в тексте — `_linkify_urls()`, отдельный финальный шаг `replace_markers()`, не пересекается с логикой 12 существующих тегов
- [x] Ссылки внутри `<code>`/`<pre>` пропускаются — найден и подтверждён реальный случай (фиктивный URL в примере теории задания №13)
- [x] Стиль ссылок добавлен в `teoria.css`/`tasks.css`/`reshenie.css`/`preparation_lesson.css` (раньше правила для `a` не было)
- [x] Урок №2 — 19 практических задач по актуальному тексту теории, все ответы проверены запуском кода
- [x] Урок №3 — 18 практических задач; первая версия (21 задача) пересобрана после того, как пользователь уточнил, что теория была пересохранена без раздела `.upper()/.lower()/.strip()`
- [x] Установлено соглашение: заголовки задач называют тему теории, а не конкретный ответ (по итогам ручной правки пользователя в уроке №2)
- [x] Урок №1 — ссылка на Яндекс.Диск с PyCharm заменена на прямую (правка пользователя, включена в релиз по подтверждению)
- [x] 24 проверки на автоссылки (юнит + рендер страниц) + JSON-целостность и рендер `/preparation/2`, `/preparation/3` для практики — все зелёные

### v1.16.1 — Урок №7 «Python: функции» + иллюстрация передачи аргументов/возврата (выполнен, 2026-07-17)

- [x] Теория по разделу 5 презентации пользователя (`def`/`return`, с параметрами и без, с результатом и без) — все примеры проверены запуском (`add`, `sos`, `greet`, `change_x`, `factorial`, `math.sqrt`)
- [x] Дополнение: локальные и глобальные переменные (нет в презентации) — подтверждено пользователем через `AskUserQuestion`
- [x] Дополнение: краткое введение в рекурсию (базовый случай, `factorial(n)`) со ссылкой на теорию задания №16 — подтверждено пользователем через `AskUserQuestion`
- [x] Раздел про `import`/подключение библиотек — по поручению пользователя из планирования v1.14.0
- [x] Черновик текста показан пользователю HTML-превью в стиле сайта (Artifact), утверждён до переноса
- [x] Впервые для урока начальной подготовки сгенерирована собственная иллюстрация (Pillow) — схема передачи аргументов/возврата на примере `sos(4, 8)`, координаты по ширине шрифта, палитра и Darcula-стиль карточек кода — в тон дизайн-системы сайта; показана и утверждена пользователем отдельно от текста
- [x] Перенос в `uroki/urok_07/urok_07.json` + `uroki/urok_07/images/`, проверен рендером `/preparation/7` через `Flask test_client()` — `<pre>`-блоки и картинка на месте, живых BBCode-коллизий нет

### v1.16.0 — Уроки №2–6 «Python с нуля» + код-блок `<pre>` (Darcula) (выполнен, 2026-07-17)

- [x] Урок №2 (ввод/вывод, типы, переменные) — теория по презентации пользователя (раздел 1)
- [x] Урок №3 (строки) — теория написана полностью самостоятельно (в презентации нет раздела про строки), дополнена по подтверждению пользователя: срез с шагом/`s[::-1]`, `.join()`, перебор строки в `for`, `isdigit()`/`isalpha()`
- [x] Урок №4 (условия) — теория по презентации (раздел 2), дополнена `and`/`or`
- [x] Урок №5 (циклы) — теория по презентации (раздел 3), дополнена `range()` с одним аргументом и `break`/`continue`
- [x] Урок №6 (списки и множества) — теория по презентации (раздел 4), дополнена `sum()`/`in`/`set()`-дедупликацией; исправлена неточность презентации про «конец множества» у `set`
- [x] Найден и исправлен живой баг в контенте урока №6 — `print(A[i])` совпадал с BBCode-маркером `[i]` в `replace_markers()` (баг того же класса, что в v1.12.18), переименовано в `A[k]`
- [x] Новая фича: блок кода `<pre>` — whitelist в `replace_markers()` (`\x00PH23\x00`/`\x00PH24\x00`), в `preparation_lesson.html` пропуск `<p>`-обёртки расширен на целые `<pre>`-абзацы
- [x] 7 многострочных примеров в уроках 4–6 переведены с построчных `<code>`+NBSP на единый `<pre>`-блок с настоящими `\n`
- [x] Визуальный стиль блока — тёмная Darcula-тема (`#2b2b2b`/`#a9b7c6`) с декоративной шапкой из трёх кружков (`pre::before`/`box-shadow`), по согласованию с пользователем (превью трёх палитр)
- [x] Стиль текстовых полей абзацев в конструкторе уроков — новый класс `.paragraph-textarea` (раньше голый `<textarea>` без CSS), применён только в `constructor_uroki.html`
- [x] Полный регресс: юнит-тесты `replace_markers()` на `<pre>` (11/12 сразу, 1 — уже известный `[i]`-баг, не регрессия), сплошной скан BBCode-коллизий по урокам 2–6 (чисто), изолированный Jinja-рендер (количество `<pre>` в JSON = в HTML), smoke-тест гостевых страниц (все 200)

### v1.15.1 — Патч: картинки не грузились у гостя (выполнен, 2026-07-16)

- [x] Пользователь протестировал сайт как гость, нашёл битые картинки в базе заданий, теме теории №1 и уроке №1
- [x] Найдена причина — `before_request`-гейт не пропускал гостя на файловые маршруты картинок/видео/файлов, хотя сами страницы уже были открыты
- [x] Исправлено точечно в `check_auth()`: `task_images`/`task_files` — безусловно, файлы теории/уроков — только для открытой темы/урока (`task_num==1`/`lesson_id==1`)
- [x] 15 проверок через `Flask test_client()`, регрессия для залогиненных пользователей не найдена
- [x] Подтверждено пользователем вживую в браузере

### v1.15.0 — Урок №1 наполнен + фикс `.reveal` (выполнен, 2026-07-16)

- [x] Пользователь самостоятельно наполнил урок №1 через конструктор (33 абзаца, 17 скриншотов)
- [x] Независимый обзор контента по запросу пользователя, без правок
- [x] Исправлены 5 опечаток/грамматических ошибок по согласованному списку
- [x] Добавлены 4 абзаца с описанием чекбоксов установщика PyCharm (содержательный пробел, найденный при обзоре)
- [x] Добавлен абзац про установку Python через Microsoft Store (текст — да, картинка — нет, см. выше)
- [x] Найден и исправлен живой баг вёрстки — карточки теории/практики урока не проявлялись из-за анимации `.reveal`, никогда не достигавшей порога видимости 12% для длинной карточки; убран класс по прецеденту `teoria_zadanie.html`
- [x] Подтверждено пользователем вживую в браузере после фикса

### v1.14.0 — План уроков «Python с нуля» + конструктор уроков (выполнен, 2026-07-15)

- [x] Согласован план из 12 уроков (установка → ввод/вывод → строки → условия → циклы → списки → функции → ошибки → визуализатор → двумерные списки → файлы кодом → Excel), по презентации пользователя (разделы 1–5, проверены запуском) + собственный анализ недостающих тем
- [x] Старое содержимое `urok_01`/`urok_02` удалено, создано 12 пустых уроков-скелетов по утверждённому плану
- [x] Новый конструктор уроков `templates/constructor_uroki.html` — клон конструктора теории с полной функциональной параллелью
- [x] Схема урока расширена: `videos` (массив вместо одиночного `video`), новое поле `description`, новая папка `practice_files/`
- [x] `preparation_lesson.html` дополнен рендером нескольких видео, заголовка и файла практической задачи
- [x] Backend: `GET/POST /api/uroki/*`, `GET /preparation_files/<id>/<file>`, `GET /constructor_uroki`, 3 save-хелпера
- [x] 37 проверок через `Flask test_client()` — гейтинг, полный цикл сохранения/чтения/отображения, скачивание файла

### v1.13.0 — Заявка вместо регистрации, ручное создание пользователей, Telegram-уведомления (выполнен, 2026-07-15)

- [x] Гостевой UX-аудит всего сайта через `Flask test_client()` (публичные/гейтед роуты, регистрация, вход, выход, CSRF, обработка ошибок) — отчёт обсуждён и разобран по пунктам с пользователем
- [x] `/register` переписан из регистрации в форму заявки (Фамилия/Имя/Телефон/способ связи Telegram-VK-MAX) → таблица `site_applications`
- [x] Ручное создание пользователя админом (`/admin/create_user`, с префиллом имени из заявки)
- [x] Список заявок в `/admin` (необработанные/обработанные, переключатель в обе стороны, ничего не удаляется)
- [x] Telegram-уведомления о новой заявке (Bot API через `urllib`, без новых зависимостей) — протестировано пользователем на реальном боте
- [x] Главная страница обновлена под новую логику в 4 местах (навбар, гостевой блок, карточка «Запишись на занятие», плавающий виджет)
- [x] Постатейная детализация прогресса ученика по урокам/теории в админке (`/admin/user/<id>/lesson|theory/<id>`)
- [x] Фикс кэширования картинок на главной (video-preview/visualizer-preview — добавлен `?v={{ app_version }}`)
- [~] `SITE_PASSWORD` намеренно не тронут по решению пользователя, хотя больше не используется в коде

### v1.12.27 — Наполнение теории заданий №19–27, завершение всего диапазона 1–27 (выполнен, 2026-07-14)

- [x] **Задания №19–21 — общая теория для связанной тройки** — по решению пользователя один и тот же текст на `/theory/19`, `/theory/20`, `/theory/21`; рассмотрен и отклонён вариант с одним физическим файлом (симлинки ненадёжны на Windows, правка `app.py` избыточно инвазивна)
- [x] **Задания №19–21 — найдены и исправлены 2 дефекта презентации** — лишний ход «+2» в примере; смешение двух формулировок Вопроса №19 (эталонные ответы презентации 34/пустой список заменены на верные 18/21)
- [x] **Задания №19–21 — практика** — 20 задач с общими id на все три задания (одна и та же игра)
- [x] **Задание №22 — найден доминирующий тип «K процессов одновременно»** (31% базы, не в презентации) — метод намеренных задержек, 2 подтипа, проверено на реальных файлах
- [x] **Задание №22 — найдена ловушка «минимальное время выполнения»** = момент завершения, не длительность (проверено: 8 vs верные 10)
- [x] **Задание №23 — все примеры презентации проверены** + решены 5 незаполненных слайдов + новый подтип «команды над цифрами числа» (3 реальные задачи)
- [x] **Задание №24 — найдена и исправлена ловушка порядка альтернатив в regex** (короткий префикс после длинного даёт заниженный ответ, проверено полным перебором)
- [x] **Задание №24 — добавлен недостающий тип «окно с N вхождениями»** (~25% базы) с исправленной ошибкой границы окна для многосимвольных подстрок
- [x] **Задание №25 — все примеры презентации проверены** + решены 5 незаполненных слайдов
- [x] **Задание №25 — найден и исправлен живой баг сайта** — `loop.parent` не существует в Jinja2, ломал рендер `answer_grid` в практике теории (`teoria_zadanie.html`)
- [x] **Задание №26 — полная переработка** — старая заглушка (2 абзаца + 8 тестовых задач) заменена на 28 абзацев (7 типов, 5 проверены на реальных файлах) + 20 новых задач; `task_id` исправлен с ошибочного `2` на `26`
- [x] **Задание №27 — построено полностью без презентации** — метод восстановлен с нуля (MST-кластеризация из-за возможного поворота прямоугольников, медиана-точка как центр, таблица классификации звёзд) и проверен на 4 парах реальных файлов (16 итоговых чисел, все совпали)
- [x] **Задание №27 — шаблон теории расширен на несколько файлов на задачу** (`task.files`) — перенесено из уже существующего паттерна `tasks_view.html`/`reshenie.html`/`exam.html`
- [x] Практика всех 9 заданий — по 20 задач возрастающей сложности из реальной базы
- [x] Полный регресс всех прошлых наборов тестов + прямая проверка рендера всех девяти страниц (включая `/theory/25` и `/theory/27` — специально из-за правок шаблона) без битых ссылок

### v1.12.18 — Наполнение теории заданий №11–18 (выполнен, 2026-07-13)

- [x] **Задание №11 — теория** — формулы презентации (`N≤2^I`, `A=I×K+P`, `S=A×G`) проверены на обоих примерах + новые разделы «составной алфавит» и «обратные задачи» (перебор N/K/G/P), 5 примеров реальной базы перепроверены
- [x] **Задание №11 — практика** — 20 задач возрастающей сложности из базы (120 задач)
- [x] **Задание №12 — теория** — из презентации использованы только последние 3 слайда (по указанию пользователя, остальные — устаревший формат); метод «обратный подсчёт» покрывал лишь ~28% реальной базы (60 задач) — добавлены «прямое преобразование» и «разворот результата», все три типа проверены полным программным симулятором Машины Тьюринга на реальных таблицах задач
- [x] **Задание №12 — практика** — 20 задач с копированием таблиц-картинок, описание задач сокращено (убран повторяющийся 6-абзацный блок правил МТ, уже разобранный в теории)
- [x] **Задание №13 — теория** — 6 методов презентации проверены кодом + новые разделы «широковещательный адрес» и «подсчёт адресов по чётности бит» (закономерность «ровно половина адресов подсети» подтверждена полным перебором 4096 адресов)
- [x] **Задание №13 — практика** — 20 задач возрастающей сложности из базы (100 задач)
- [x] **Задание №14 — теория** — 4 метода презентации проверены на всех решённых примерах + доминирующий тип «выражение минус x с известной границей» (~77% реальной базы из 128 задач, отсутствовал в презентации), проверен на 2 реальных задачах
- [x] **Задание №14 — практика** — 20 задач возрастающей сложности из базы
- [x] **Задание №15 — теория** — 4 типа презентации проверены + предикат `div()` + критичная находка про диапазон перебора x/y (маленький диапазон «на глаз» даёт неверный ответ 9802 вместо истинных 1176126 — диапазон нужно определять по константам самой формулы)
- [x] **Задание №15 — практика** — 20 задач возрастающей сложности из базы (130 задач)
- [x] **Задание №16 — теория** — критичный приём `sys.setrecursionlimit()` независимо перепроверен (подтверждено: без него реальный `RecursionError` на глубине 2024) + новый раздел «взаимная рекурсия» F(n)/G(n), проверен на реальной задаче
- [x] **Задание №16 — практика** — 20 задач возрастающей сложности из базы (118 задач)
- [x] **Задание №17 — теория** — произвольные/соседние пары и тройки из презентации + новые «тройки», «симметричные пары», «локальные минимумы/максимумы», все проверены на числовых примерах включая точное совпадение с реальной задачей базы
- [x] **Задание №17 — найден и исправлен живой баг `replace_markers()`** — коллизия литерала `a[i]`/`row[i]` с BBCode-маркером `[i]` (курсив) из whitelist-механизма экранирования; баг оказался уже живым в запущенном задании №9 — исправлен ретроактивно переименованием переменной на `k`; сплошной аудит всех 18 файлов теории на этот и на баг двойного экранирования HTML-сущностей — новых случаев не найдено
- [x] **Задание №17 — практика** — 20 задач с копированием приложенных файлов, впервые в теории использован формат ответа `answers: [count, sum]`
- [x] **Задание №18 — теория написана с нуля** (впервые без презентации) — данные скрыты в утолщённых границах ячеек (стенах), а не в значениях; написаны собственные парсеры форматов ODS и XLSX (zip+XML, без сторонних библиотек), метод — DP по клеткам поля (max/min по всем тупиковым клеткам), проверен на 3 реальных `.ods`- и 1 `.xlsx`-файле
- [x] **Задание №18 — обнаружены 3 подтипа реальной базы** — «несколько тупиковых клеток» (34%, основной метод), «фиксированный угол назначения» (32%, тот же метод без изменений — подтверждено на реальном файле), «другая механика» (разрушение робота / ВЕРХ-НИЗ, 17% — исключено из практики, см. следующий этап)
- [x] **Задание №18 — практика** — 20 задач (только стандартный + фиксированный-угол подтипы) с копированием приложенных файлов, формат ответа `answers: [max, min]`
- [x] Полный регресс всех прошлых наборов тестов после каждой темы + прямая проверка рендера всех восьми страниц (картинки/файлы/практика/прогресс-панели без битых ссылок)

### v1.12.10 — Наполнение теории заданий №6–10 (выполнен, 2026-07-09)

- [x] **Задание №6 — теория по методу презентации** — справочник команд «Черепахи» + ловушка «повторы≠стороны» + метод пересечения двух прямоугольников, проверено на реальной задаче базы (id=8, объединение 152)
- [x] **Задание №6 — 2 сгенерированные иллюстрации** — треугольник-ловушка + объединение прямоугольников (Pillow)
- [x] **Задание №6 — практика** — 20 задач (6 одна фигура → 14 две фигуры, возрастающей сложности) из базы (78 задач)
- [x] **Задание №7 — теория по формулам презентации** — изображения (`A×B×I`), звук (`K×N×I×T`), передача (`T=A/V`), приём сравнения через отношения без абсолютных величин
- [x] **Задание №7 — найден и добавлен раздел «осторожно с округлением»** — не из презентации, направление округления зависит от точной формулировки вопроса, подтверждено на паре контрастных реальных задач базы
- [x] **Задание №7 — практика** — 20 задач (8 изображение + 6 звук + 6 передача, round-robin возрастающей сложности) из базы (122 задачи)
- [x] **Задание №8 — сверка презентации с реальной базой (127 задач)** — выявлен пробел: презентация не покрывает системы счисления (36 задач, 28% базы), только перебор слов; пользователь подтвердил решение расширить теорию
- [x] **Задание №8 — новый раздел «числа в системах счисления»** — метод «цифра-разделитель», 2 примера перепроверены на реальных задачах базы (5100 — id=7, 3625 — id=12)
- [x] **Задание №8 — практика** — 20 задач (14 слова + 6 системы счисления, пропорция как в базе, round-robin возрастающей сложности) из базы (127 задач)
- [x] **Задание №9 — теория написана с нуля** (впервые без презентации) — метод программной проверки двух условий на строку файла-таблицы (Counter/порядок/разбиение на пары + суммы/средние), разница «наибольший/наименьший номер строки»
- [x] **Задание №9 — реальный `.ods`-файл разобран вручную** (zip+XML) для проверки — `pandas`/`odfpy` недоступны из-за отсутствия pip-доступа в песочнице; 4 примера перепроверены на настоящих файлах реальных задач (6322, 138, 27987, 2354)
- [x] **Задание №9 — практика** — 20 задач возрастающей сложности из базы (91 задача), с копированием приложенного файла каждой задачи
- [x] **Задание №10 — теория написана с нуля** (впервые без презентации) — метод «Найти и заменить» с флажками регистра/целого слова, разбор типичных формулировок условия
- [x] **Задание №10 — обнаружена и задокументирована сложность реальных данных** — приложенный файл почти всегда содержит больше текста, чем нужно (наивный поиск по всему файлу вместо 2 нужных глав завысил результат почти в 24 раза), в заголовках глав встречаются опечатки-омоглифы (кириллическая «Х» вместо «X», «X0» вместо «X»)
- [x] **Задание №10 — 2 примера перепроверены** на настоящих файлах реальных задач (46, 57)
- [x] **Задание №10 — практика** — 20 задач возрастающей сложности из базы (115 задач), с копированием приложенного файла каждой задачи
- [x] Полный регресс всех прошлых наборов тестов после каждой темы + прямая проверка рендера всех пяти страниц (картинки/файлы/практика/прогресс-панели без битых ссылок) + исправлен один найденный по ходу баг двойного экранирования HTML-сущностей (`&nbsp;`/`&lt;`/`&gt;`, вставленных напрямую в JSON вместо настоящих символов)

### v1.12.5 — Наполнение теории заданий №4–5 по презентациям пользователя (выполнен, 2026-07-08)

- [x] **Задание №4 — теория по методу двоичного дерева** — из презентации пользователя, дополнена случаем «весь алфавит» (резерв узла), правило подтверждено пересчётом реальной задачи из базы (id=58, без резерва — неверный ответ)
- [x] **Задание №4 — 4 сгенерированные иллюстрации** — общая схема дерева + 2 решения примера МАГИЯ + пример с резервом (Pillow, matplotlib недоступен в песочнице)
- [x] **Задание №4 — практика** — 20 задач возрастающей сложности из базы (122 задачи)
- [x] **Задание №5 — теория** — справочник функций Python, разобранный пример с кодом (перепроверен запуском), переводы в СС 3–9 и 11–36 по коду пользователя
- [x] **Задание №5 — найдена и исправлена опечатка** в присланном коде для баз 11–36 (`a //= 3` → `a //= 11`), подтверждено тестовым запуском (100 в 11-ричной: `13001` было → `91` стало)
- [x] **Задание №5 — второй вид задания переписан** по рисунку пользователя (метод общей цифры b) + сгенерированная иллюстрация (аналогичный Pillow-рендерер)
- [x] **Задание №5 — практика** — 20 задач возрастающей сложности из базы (125 задач)
- [x] Полный регресс всех прошлых наборов тестов + прямая проверка рендера обеих страниц (картинки/практика/прогресс-панели без битых ссылок)

### v1.12.3 — Наполнение теории заданий №2–3 + фикс списков/кода + единый стиль `<code>` + реальный прогресс на главной (выполнен, 2026-07-08)

- [x] **Фикс списков/кода в теории** — `replace_markers()` расширен на `<ul>/<ol>/<li>/<code>`; в 4 шаблонах (`teoria_zadanie.html`, `reshenie.html`, `tasks_view.html`, `preparation_lesson.html`) убрана лишняя `<p>`-обёртка вокруг абзацев-списков (иначе невалидная вложенность `<p><ul>...`); `exam.html` не тронут
- [x] **Единый стиль `<code>`** — синий чип в цвет акцента сайта, добавлен в `design-system.css` (все 16 страниц на `base.html`, не только теория); в конструктор теории добавлена кнопка `</>`
- [x] **Блок «Ваш прогресс» на главной — реальные данные** — новая `load_homepage_progress()` считает решённые задачи/изученные темы/лучший результат из БД вместо захардкоженных чисел; пустое состояние с CTA для пользователей без истории
- [x] **Обновлены фото-превью на главной** — секция «Видеоразборы и уроки» теперь показывает реальные скриншоты `/theory/1` и `/choose-mode/1` вместо скриншота админки и удалённого «Варианта 14»
- [x] **Задание №2 — практика пересобрана** — 20 задач возрастающей сложности из базы (115 задач); текст теории по решению пользователя не менялся (кроме опечатки, поправленной самим пользователем)
- [x] **Задание №3 — теория написана с нуля** — структура реляционной БД (главная таблица + справочные, связь по ключу), функция ВПР (синтаксис, пример, план решения) — по итогам разбора реальных `.xlsx`-файлов задач и правок пользователя
- [x] **Задание №3 — практика пересобрана** — 20 задач возрастающей сложности из базы (88 задач), включая приложенные файлы БД `.xlsx`/`.ods`/`.xls` (единственная тема из трёх с файлами в практике)
- [x] Полный регресс всех прошлых наборов тестов после каждого шага + прямая проверка рендера обеих страниц (картинки/файлы/прогресс-панели без битых ссылок)

### v1.12.0 — Конструктор теории: полная параллель со старым конструктором + наполнение задания №1 (выполнен, 2026-07-07)

- [x] **Этап 1 конструктора** — сетка выбора номера задания (вместо `<select>`) + массовая вставка текста с авторазбиением на абзацы
- [x] **Этап 2 конструктора** — тулбар форматирования абзацев (Ж/К/Подч./степень/индекс); попутно исправлен `replace_markers()` в `app.py` — whitelist расширен на `<strong>`/`<em>`/`<u>` (старый контент с этими тегами показывался как escaped-текст)
- [x] **Этап 3 конструктора** — картинки к любому абзацу теории; `/api/theory/save` переведён с JSON на `FormData`+multipart
- [x] **Этап 4 конструктора** — полный редактор практических задач: многоабзацное описание с тулбаром, картинки к абзацу задачи, прикреплённый файл, переключатель типа ответа (одно поле/два поля/таблица) со строгой взаимоисключаемостью полей, сворачиваемые карточки задач (новые раскрыты, сохранённые свёрнуты)
- [x] **Несколько видео на тему** — `theory.videos` вместо одиночного `theory.video` (как уже было у задания №26), автоконвертация старого формата при открытии в конструкторе; попутно исправлен живой баг падения при сохранении темы, уже хранящей только `videos`
- [x] **Увеличен размер полей абзаца** — `rows` 3–4 → 8, по фидбеку пользователя
- [x] **Задание №1 — правки текста теории** — исправлено 5 опечаток/грамматических ошибок (задвоенное слово, согласование рода, пробел перед скобкой, невнятная формулировка, лишний `\n` в JSON)
- [x] **Задание №1 — пересборка практики** — старые 2 тестовые задачи удалены (у одной обнаружен конфликт `answers`+`correct_answer`, оставшийся от более раннего теста конструктора — студентам показывалась не та форма ответа); добавлены 20 задач из базы задания №1, отсортированные по возрастанию сложности и визуальной сложности картинки графа
- [x] Полный регресс всех прошлых наборов тестов после каждого этапа + прямая проверка рендера `/theory/1` (все 20 картинок и прогресс-панелей резолвятся без битых ссылок)

### v1.11.0 — Подготовка сайта к роли «визитной карточки» (выполнен, 2026-07-06)

- [x] **Скрытие версии сайта** — убран видимый текст `· v{{ app_version }}` из `components/footer.html` и `start.html`; техническая инфраструктура (cache-busting) не тронута
- [x] **Редизайн `reshenie.html`** — переведён на `{% extends "base.html" %}` + новый `static/css/pages/reshenie.css`, переиспользованы готовые компоненты дизайн-системы; вся Jinja/JS-логика сохранена бит-в-бит (3 типа формы ответа, таймер, AJAX-проверка); `exam.html` осознанно НЕ тронут
- [x] **Яндекс.Метрика + Open Graph теги** — счётчик (Вебвизор, карта кликов/скроллинга, аналитика форм) и `og:title`/`og:description`/`og:image`/`og:url`/`twitter:card` в `base.html` и `start.html`; счётчик не добавлен в `exam.html` и админку. **Проверено вживую пользователем 2026-07-06** после деплоя на REG.RU — счётчик найден на странице, передача данных подтверждена.
- [ ] Цели (конверсии) в Яндекс.Метрике — обсуждались (регистрация, клик по контактам, начало решения варианта), но не настроены — отдельная задача на будущее
- [ ] UTM-конвенция для ссылок в соцсетях/видео — обсуждена (utm_source/medium/campaign), но пока не формализована документально

### v1.10.0 — 6 точечных доработок платформы (выполнен, 2026-07-06)

- [x] **Пересборка `variants/`** — все 14 старых вариантов удалены, 20 новых созданы через уже существующий `/api/variants/save`, сбалансированный подбор задач (без повторов между вариантами по каждой из 27 позиций), корректная обработка связанной тройки 19/20/21 (общий `task_id` внутри варианта)
- [x] **Связь статистики попытки с базой заданий** — `load_variant_tasks()` сохраняет `_base_task_id`; `attempt_detail.html` даёт прямую ссылку на карточку в `/tasks/<n>`; `tasks_view.html` автораскрывает и скроллит к нужной карточке по `location.hash`
- [x] **Диаграмма решаемости по номерам заданий** — на `/stats` и в `admin_user_stats.html`, 27 столбцов (правильно/неправильно), подписи количества над/под каждым столбцом
- [x] **Доступ для гостей** — найден и переработан глобальный `check_auth()` (блокировал всё для неавторизованных); теперь открыты `/tasks`, каталоги `/preparation`+`/theory`, первый урок и первая тема теории; остальное — только логин + разрешение админа
- [x] **Регистрация** — поле «Имя» обязательно (фронт+бэк), подсказка про пароль сайта («только у меня лично», без номера телефона)
- [x] **Версия сайта в футере** — `APP_VERSION` в `app.py` через `@app.context_processor`, выведена в обоих футерах
- [x] **Замена эмодзи-замков** — 🔒/🔓 нечитаемы на части мобильных экранов, заменены на эмодзи+текст («Открыто»/«Закрыто») в `preparation.html`/`teoria.html`/`admin_user_stats.html`
- [x] Финальный сквозной смоук ~25 маршрутов (публичные, админские, /solve, /exam, /choose-mode, /results) — без единого 5xx

### v1.9.1 — Возможные доработки базы задач

- [ ] **Возможное расширение Заданий 1–18** — часть заданий (12, 13) уже взяты полностью (именованный + анонимный пул); для остальных всё ещё берётся только «именованный» пул — есть анонимные задачи без источника на kompege.ru, не импортированные
- [ ] **Возможное расширение Задания 24** — в пуле источника ещё ~50 неиспользованных именованных записей (файлы ~8 МБ каждая) сверх уже импортированных 75
- [ ] **Возможное расширение Задания 27** — в анонимном пуле ещё 27 неиспользованных записей сверх уже импортированных 90

### v1.7.3 — Возможные доработки визуализатора

- [ ] **Wall-clock timeout на `/api/visualizer/trace`** — тяжёлая одна строка кода (`2**10**8` и т.п.) может надолго занять воркер; риск снижен закрытым доступом, но не устранён
- [ ] **Rate limiting на `/api/visualizer/trace`** — пока не реализован
- [x] **Реальные скриншоты/видео для `.visualizer-preview-section`** — `visualizer-preview-left.jpg`, `-right.jpg`, `visualizer-overview.mp4` добавлены пользователем, больше не плейсхолдеры

### v1.6.6 — Дальнейшее развитие платформы

- [ ] **Социальное доказательство / результаты учеников** — отзывы, примеры результатов, счётчик пользователей в Hero (`start.html`)
- [ ] **Реальная видеотека** — несколько видео в блоке видео-превью, возможность выбора
- [ ] **Mobile check остальных страниц** — `exam.html` (таблицы ответов), `reshenie.html`, проверка на реальных устройствах (375px / 768px)
- [ ] **Редизайн `exam.html` и `reshenie.html`** — перевод на `{% extends "base.html" %}` и светло-голубую тему (отдельный scope)
- [ ] **Редизайн admin-шаблонов** — опционально, по запросу

### v1.9.0 — Завершение базы задач Заданий №26–27 + финальный аудит 1–27 (выполнен, 2026-07-03)

- [x] **Задание №26** — 124 задачи (обработка чисел из файла, 2 ответа): заглушка использовала формат `answers: [a,b]`, несовместимый со специальной веткой частичного зачёта ЕГЭ (`get_points_for_task`, позиции 26/27, 0/1/2 балла), требующей `answer_grid` — заменена при импорте; 6 задач со встроенными base64-картинками декодированы; исправлена ещё одна авторская подпись с пробелом перед `</i>` (тот же класс бага, что в задании 8)
- [x] **Задание №27** — 90 задач (23 именованные + 67 анонимных, кластеризация звёзд): та же несовместимость формата ответа, плюс сам тип условия заглушки не встречается в актуальном пуле; расширена схема задачи — новое поле `"files": [...]` для задач с несколькими файлами (единственный такой случай в базе), обратная совместимость сохранена для всех остальных 26 заданий; правки в `tasks_view.html`, защищённых `reshenie.html`/`exam.html`; при расширении пула найдены и исправлены 3 новых класса багов парсера (доп. атрибут `width` после base64, внешние картинки с `kpolyakov.spb.ru`, вложенный `<div><div>` в середине текста)
- [x] **Финальный сплошной аудит базы (Задания №1–27, 2924 задачи)** — по запросу пользователя: 23/27 заданий чисты; исправлен единственный реальный дефект (необработанная LaTeX-конструкция `\left|…\right|` в задании 27); 4 находки подтверждены безопасными ложными срабатываниями (2 известны с v1.8.2, 2 новые той же природы — математические неравенства `A<B`)
- [x] Смоук-тест всех 27 страниц `/tasks/{N}` — 200 везде
- [x] Код проекта — только точечные правки трёх шаблонов (`tasks_view.html`, `reshenie.html`, `exam.html`) под поле `"files"`; `app.py`, схема `variant.json`, `load_variant_tasks()`, маршруты, БД — не менялись

### v1.8.3 — Наполнение базы задач Заданий №19–25 (выполнен, 2026-07-03)

- [x] **Задания №19–21** — по 113 задач («связанная тройка» — общее условие, три разных вопроса на реальном экзамене): выбран «Вариант A» — параллельные `id` в трёх независимых файлах базы, без изменения `variant.json`/`load_variant_tasks()`; в `constructor_from_base.html` добавлена `LINKED_TRIO = [19, 20, 21]` с зеркалированием выбора/снятия по всем трём позициям; форма ответа (1 или 2 токена) определялась по фактическому содержимому `key` каждой конкретной записи, а не по номеру задания; 3 записи с тремя токенами ответа исключены
- [x] **Задание №22** — 110 задач (67 именованных + 43 анонимных, пул расширен по решению пользователя): исправлен баг «жирный текст» — протёкшие формулировки-заглушки после вырезания встроенной таблицы, решено tag-tolerant хелперами (`tag_tolerant()`, `scaffold_regex()`); 1 пара записей с неоднозначным дублем (разные форматы приложенного файла) оставлена по решению пользователя
- [x] **Задание №23** — 119 задач (преобразование чисел): исправлен баг конвертера LaTeX — подстановки без учёта границ математического режима портили URL в авторской ссылке (подчёркивание трактовалось как нижний индекс); введён `_convert_math_span()`, ограничивающий действие рамками `\(...\)`
- [x] **Задание №24** — 75 задач (единственный случай урезания объёма против доступного пула ~148 записей): файлы аномально крупные (~8 МБ против килобайт в остальных заданиях); первая партия 25 (2 фото-связки + 23 именованных), затем ещё 2 партии по 25 сверх исходных по отдельному запросу пользователя (208.5→346.9→419.8 МБ)
- [x] **Задание №25** — 120 задач (маски чисел, полный перебор): первое задание, полностью использующее табличный формат ответа `answer_grid`; найден и обойдён дефект источника (заявленное число столбцов не совпадало с фактическим для одной записи); декодирована встроенная в текст base64-картинка (id 101) в обычный файл изображения
- [x] **Критический фикс `replace_markers()`** (`app.py`) — по репорту пользователя со скриншотами живого сайта («___IEND_I___») исправлена коллизия плейсхолдеров XSS-экранирования (`___NAME___` → непересекающиеся `\x00PH1\x00`…`\x00PH8\x00`); 17/17 регрессионных тестов + живой рендеринг контента со скриншотов
- [x] Проверка целостности после каждой партии/задания и смоук-тест через Flask `test_client()` — 0 проблем на финальных проверках
- [x] Код проекта — только точечные правки `app.py` (`replace_markers()`) и `templates/constructor_from_base.html` (`LINKED_TRIO`), плюс содержимое `tasks/task_19–25/`

### v1.8.2 — Наполнение базы задач Заданий №11–18 + аудит (выполнен, 2026-07-02)

- [x] **Задание №11** — 120 задач (кодирование, объём памяти): простое текстовое задание, без таблиц/файлов/LaTeX
- [x] **Задание №12** — 60 задач (Машина Тьюринга): новый Pillow-рендерер таблиц команд `render_mt_table()` с сохранением исходного порядка столбцов; по решению пользователя пул расширен с 10 именованных до всех 60 доступных (единственный случай в проекте, где снято правило «только именованный источник»)
- [x] **Задание №13** — 100 задач (адресация IP-сетей): пул тоже расширен сверх именованного (39→100); исправлен баг regex — альтернатива `(p|div|pre|ol|ul)` матчила `<pre>` как `<p>` из-за общего префикса, добавлен `\b`
- [x] **Задание №14** — 128 задач (системы счисления, арифметические выражения): 10 задач восстановлены из фото, включая формулу через Quill/KaTeX (`data-value`)
- [x] **Задание №15** — 130 задач (логические выражения на числовых прямых): разработан конвертер LaTeX→юникод (`\lor`,`\land`,`\neg`,`\to`,`\leq` и альтернативные `\/`,`/\`,`\&`) — 66 из 130 задач требовали конвертации
- [x] **Задание №16** — 118 задач (рекурсивные функции, 1 дубль пропущен): конвертер LaTeX расширен (`\times`,`\leqslant`,`X^{Y}`→`<sup>`); исправлен разбор Quill/KaTeX-спанов — переход на разбор по внутреннему содержимому вместо `data-value`, так как не все спаны были «мусором»
- [x] **Задание №17** — 85 задач (обработка последовательности чисел из файла): первое использование формата ответа `"answers": [a, b]`; обойдён баг вложенных одноимённых тегов (`<div><div>...`)
- [x] **Задание №18** — 110 задач (Робот на сетке со стенами): выяснено, что реальные данные всегда в приложенном файле, картинка — лишь иллюстрация; 9 задач с иным числом ответов исключены; добавлена общая предобработка снятия внешней `<div>`-обёртки
- [x] **Сплошной аудит базы (Задания №1–18, 2029 задач)** — по запросу пользователя: исправлен необработанный LaTeX в 51 задаче задания 14 и 7 задачах задания 13 (конвертер не был подключён при исходном импорте), 1 пропущенная авторская подпись в задании 8 (пробел перед `</i>`), 1 настоящий дубль в задании 2 (id 22/26, проверено визуально по картинкам), 40 пустых тегов-обёрток `<b> </b>`/`<i> </i>` по заданиям 2,4,5,6,10,13,15,16,18 (включая вложенный случай, потребовавший цикла «до неподвижной точки»)
- [x] Код проекта (`app.py`, шаблоны) не менялся — только `tasks/task_11–18/` и точечные правки `tasks/task_02,08,13,14/`

### v1.8.1 — Наполнение базы задач Заданий №4–10 (выполнен, 2026-07-02)

- [x] **Задание №4** — 122 задачи (кодирование текста): таблицы «буква→код» рендерятся в PNG через Pillow (matplotlib недоступен в песочнице), ширина колонок измеряется динамически
- [x] **Задание №5** — 125 задач (алгоритмы): без таблиц/файлов, `<sub>` из источника проходит через whitelist без изменений
- [x] **Задание №6** — 78 задач (исполнители/циклы): исправлена потеря контента внутри `<div>`/`<pre>`; исправлена разбалансировка `<b>`/`<i>` при `<br/>` внутри тега (`split_balanced()`); 3 задачи восстановлены из скриншотов по стандартному исполнителю-черепахе 2026 года
- [x] **Задание №7** — 122 задачи (кодирование звука): найден и исправлен пропуск дубля — дедупликация переведена с сырого текста на обработанный
- [x] **Задание №8** — 127 задач (комбинаторика слов): подтверждено правило «только именованный источник» без исключений для редких difficulty=«гроб» записей (все анонимны в этой партии)
- [x] **Задание №9** — 91 задача (электронные таблицы): загрузка приложенных файлов (приоритет xlsx→ods→xls→csv), исправлена потеря контента внутри `<ol>`/`<ul>`/`<li>`, дедупликация уточнена до пары (текст, ответ)
- [x] **Задание №10** — 115 задач (анализ текстовых файлов): приоритет форматов docx→doc→odt→txt→rtf→pdf
- [x] **Ретроактивные исправления** — авторские подписи убраны из заданий 1–4 (79 шт.); скругление углов картинок убрано в `tasks.css`/`reshenie.html`/`exam.html`
- [x] **Проверка целостности** после каждой партии — 0 проблем на финальной проверке во всех семи заданиях
- [x] Код проекта (`app.py`, шаблоны кроме точечной CSS-правки) не менялся — только `tasks/task_04–10/`

### v1.8.0 — Наполнение базы задач Заданий №1–3 (выполнен, 2026-07-02)

- [x] **Выбор источника** — сравнены Яндекс.Учебник (SPA, недоступен), открытый банк ФИПИ (без ответов), РЕШУ ЕГЭ (старый банк без формата 2026 г.), kompege.ru (выбран — JSON API, ответы открыты, файлы/картинки без авторизации)
- [x] **Задание №1** — 113 задач (графы дорог), автоклассификация подтипов по тексту (~17%/83%), найдены и исправлены 5 задач с ответом-заглушкой и 2 с потерянной картинкой (`<span>`-обёртка)
- [x] **Задание №2** — 116 задач (таблицы истинности): конвертер LaTeX + HTML-сущностей формул → юникод; рендерер таблиц в PNG через `matplotlib` (рамки, жирный столбец F); высота строк донастроена по фидбеку (0.55→0.4→0.3")
- [x] **Задание №3** — 88 задач (базы данных): несколько картинок на задачу на произвольных позициях (переиспользован парсер Задания 1), загрузка приложенного файла с приоритетом xlsx→ods→xls, retry-логика на сетевые сбои + промежуточное сохранение
- [x] **Проверка целостности** после каждой партии — дубли id, потерянные картинки/файлы, «осиротевшие» файлы на диске, пустые описания, нечисловые ответы, утечки разметки — во всех трёх заданиях 0 проблем на финальной проверке
- [x] **Старые тестовые задачи** и orphan-файлы (`Z1.png`/`Z1.ods`) удалены из всех трёх папок перед заменой
- [x] Код проекта (`app.py`, шаблоны, `static/`) не менялся — только `tasks/task_01–03/`

### v1.7.2 — Диаграммы функций в Block Scheme + витрина на главной (выполнен, 2026-07-01)

- [x] **Парсер** — `def` больше не пропускается вместе с телом; тело рекурсивно разбирается через `parseBlock()`, функции всплывают отдельным списком `functions[]`; новый тип узла `return` (перевёрнутая трапеция, свой цвет, тупиковый блок без исходящей стрелки)
- [x] **`layoutProgram()`** (заменил `layoutTree()`) — главная диаграмма + диаграмма на каждую функцию, столбцом справа, друг под другом; у функций нет овала «Конец»
- [x] **Анимация вызов/возврат** — карта `bsCallSiteLine` (scope_id → строка вызова); аргументы летят к «Начало (имя)», возврат — обратно к конкретному call-site; работает для рекурсии и вызова функции из функции
- [x] **Исправлен баг: источник шарика Memory/Output** — после `return` шарик летел от блока самого return, а не от call-site; переиспользована карта `bsCallSiteLine`
- [x] **Исправлен баг: наложение диаграмм** — `funcCX` теперь считается через пробный проход `layoutSeq` при `cx=0` (`leftExtent()`), а не фиксированным отступом
- [x] **Донастройка отступов/ширины** — `FUNC_GAP_X` 260→130, `.main` 1400→1850px, `.bs-code-panel` 320→390px, `#bs-svg` адаptивный (`max-width:100%; height:auto; min-width:650px`)
- [x] **`.visualizer-preview-section`** на `start.html` — видео + 2 фото (по образцу `.video-preview-section`), тег `.section-tag-cyan`, CTA на `/visualizer`
- [x] **Проверка** — вся логика (парсер/layout/анимация) прогонялась через Node напрямую на реальных данных `tracer.py` (факториал-рекурсия, две функции, `bubble_sort` — 59 шагов трассировки), 0 промахов резолва; визуальная приёмка — пользователем в браузере

### v1.7.1 — Визуальная унификация визуализатора (выполнен, 2026-07-01)

- [x] **Navbar** — собственный `<header>` визуализатора заменён на `{% include 'components/navbar.html' %}`; кнопка темы перенесена в `.page-title`
- [x] **CSS без конфликтов** — в `static/visualizer/style.css` скопированы только правила navbar (не весь `design-system.css`, чтобы не столкнуться с одноимёнными `.card`/`.btn` визуализатора)
- [x] **Светлая тема под палитру сайта** — `:root`: `--bg` → общий градиент, `--primary`/`--primary-dark`/`--primary-light` → `#2563EB`/`#1E40AF`/`#DBEAFE`, `--surface`/`--border`/`--shadow*` → токены `design-system.css`
- [x] **Тёмная тема + инверсия navbar** — `.dark .navbar` и дочерние элементы переопределены в светлый вариант; контраст проверен численно (4.63:1–17.85:1)
- [x] **Проверка** — структура HTML и валидность CSS проверены автоматически; визуальная приёмка — пользователем в браузере (headless-скриншот в этой песочнице недоступен: нет `chromium-cli`, `pip install playwright` не работает из-за SOCKS-прокси)

### v1.7.0 — Интеграция визуализатора Python-кода (выполнен, 2026-07-01)

- [x] **Backend** — `validator.py`, `tracer.py` перенесены из visual-code без изменений; `app.py` импортирует `validate()`/`trace_code()`; роуты `GET /visualizer` и `POST /api/visualizer/trace`, оба за `"user_id" not in session`
- [x] **Frontend** — `templates/visualizer.html` (автономный, без `{% extends "base.html" %}`), `static/visualizer/style.css` + `js/{editor,animator,block-scheme}.js` без изменений
- [x] **api.js** — `API_BASE` → относительный `/api/visualizer`; добавлен заголовок `X-CSRFToken` (токен из `<meta name="csrf-token">`)
- [x] **Навигация** — кнопка «⚡ Визуализатор кода» в `components/navbar.html` (десктоп рядом с dropdown через общий `.nav-center`, мобильная версия — пункт внутри dropdown) и продублирована в `start.html` (свой встроенный navbar)
- [x] **Проверка** — Flask test client: анонимный доступ заблокирован, авторизованный — рендер + CSRF + трассировка работают корректно
- [ ] Wall-clock timeout и rate limiting — отложены (см. v1.7.1)

### v1.6.5 — Редизайн пользовательских страниц (выполнен, 2026-06-19)

- [x] **Этап 0: архитектурная база** — `base.html`, `components/navbar.html`, `components/footer.html`, `components/flash.html`, `design-system.css`, `scroll-reveal.js`
- [x] **Этап 1: страницы авторизации** — `login.html`, `register.html`, `csrf_error.html` + `auth.css` (480px-only, центрированная карточка)
- [x] **Этап 2: каталоги и списки** — `variant_list.html`, `teoria.html`, `preparation.html`, `tasks_list.html` + `catalogs.css`
- [x] **Этап 3: профиль и статистика** — `profile.html` + `profile.css`; `stats.html`, `attempt_detail.html` + `stats.css`
- [x] **Этап 4A: выбор режима** — `choose_mode.html` + `variants.css`
- [x] **Этап 4B: база задач** — `tasks_view.html` + `tasks.css` (JS-фильтр, collapse-expand `max-height: 5000px`)
- [x] **Этап 4C: теория** — `teoria_zadanie.html` + `teoria.css` (`checkPracticeTask()` через `<form onsubmit>` + CSRF hidden input)
- [x] **Этап 4D: урок** — `preparation_lesson.html` + `preparation_lesson.css` (`checkTask()` через `onclick` + fetch, без `<form>`)
- [x] **Финальный аудит** — 15/15 страниц OK · 246/246 проверок OK

### v1.6.4 — Доработка главной страницы (выполнен, 2026-06-18)

- [x] **HTML5-видеоплеер** — `<video controls preload="metadata">`, источник `/static/videos/platform-overview.mp4`; директория `static/videos/` создана
- [x] **Боковые постеры видео** — реальные `<img>` вместо CSS-градиентов; `/static/images/video-preview-left.jpg` и `video-preview-right.jpg`; natural colors, без play/overlay, `pointer-events: none`
- [x] **Исправление clipping** — удалён локальный `overflow-x: clip`; защита горизонтального скролла через `body { overflow-x: hidden }`
- [x] **CTA-секция записи на занятие** — `.contact-cta-section` после «Что нового»; Telegram + MAX; reveal-анимация; адаптив 768px/480px
- [x] **Плавающая карточка связи** — `.floating-contact` position fixed, bottom-right, z-index 50; скрывается ≤768px; без JS
- [x] **`.card-stripe` для update-card** — 3 карточки с семантическими цветами: синий (видеоразборы) / фиолетовый (статистика) / зелёный (старт с нуля); hover-фикс через повышенную специфичность

### v1.5.3 — Главная страница: визуальная переработка

- [x] **Этап 1** — Hero двухколоночный: mockup платформы справа, мини-статы под mockup.
- [x] **Этап 2** — Визуальная иерархия секций:
  - [x] stats-row: карточки → горизонтальная полоса с разделителями (нет фона, нет отдельных рамок)
  - [x] "С чего начать": равнозначный grid → `.start-flow` с `.start-arrow` (`→` / `↓`), левые акцентные бордеры
  - [x] Мини-задача: синий тинт + тег «Задание» — акцент без изменения контента
  - [x] Калькулятор: фиолетовый тинт + фиолетовый `calc-btn` + тег «Калькулятор» — отличается от мини-задачи
  - [x] Преимущества: 4 отдельные карточки → единая `.advantages-panel` с внутренними разделителями
- [x] **Этап 3.1** — Feature Grid 2×2:
  - [x] `.features-grid` 3 кол. → 2 кол. (`repeat(2, 1fr)`)
  - [x] Карточка «Статистика» удалена; остались 4 карточки в симметричном grid
  - [x] Порядок карточек: Начальная подготовка / Теория / Варианты / База заданий
- [x] **Этап 3.2** — Контраст вторичного текста:
  - [x] 15 CSS-селекторов `#64748b` → `#94a3b8` (WCAG AA: контраст 3.7:1 → 5.3:1)
  - [x] Декоративные элементы mockup и uppercase-лейблы оставлены без изменений (6 шт.)
- [x] **Этап 3.3** — Interactive Zone (финальная проверка: 9/9 OK):
  - [x] Три секции (мини-задача, калькулятор, маршрут) обёрнуты в `.interactive-zone`
  - [x] CSS: `border-top/bottom` + `background: rgba(255,255,255,0.018)` + `padding: 64px 0`
  - [x] Адаптив ≤480px: `padding: 44px 0`
  - [x] `checkMiniTask()` и `checkReadiness()` не изменялись

### v1.5.4 — Главная страница: UX-доработка

- [x] **Этап 1** — Очистка и усиление navbar (финальная проверка: 8/8 OK):
  - [x] Блок «Новые варианты» полностью удалён из HTML и CSS (9 селекторов + адаптив)
  - [x] Маршруты `/choose-mode/3,4,5` в `app.py` сохранены (только скрыто отображение)
  - [x] Кнопка navbar: «Разделы» → «Разделы платформы» (desktop); «Меню» на мобильном не изменился
  - [x] CSS кнопки усилен: `font-weight: 600`, `color: #cbd5e1`, `background: rgba(255,255,255,0.04)`, `border-radius: 8px`, `white-space: nowrap`
- [x] **Этап 2** — Последовательная интерактивная зона (финальная проверка: 24/24 OK):
  - [x] Начальное состояние: видна только мини-задача; calculator и roadmap скрыты (`display:none`)
  - [x] Индикатор прогресса `div.iz-progress`: текст «Шаг N из 3» + три точки `.iz-dot`
  - [x] Шаг 1: мини-задача → при ответе появляется кнопка «Оценить свою готовность →»
  - [x] Шаг 2: калькулятор → при ответе на все вопросы появляется кнопка «Посмотреть маршрут подготовки →»
  - [x] Шаг 3: roadmap + CTA «Запишись на первое бесплатное вводное занятие» (Telegram + MAX)
  - [x] Кнопки без ответа (`no-answer`, `warn`) — кнопка перехода НЕ появляется
  - [x] Переходы только по кнопкам — авто-переходов нет; `scrollIntoView` не используется
  - [x] Fade-анимация: `opacity 0.25s` + `setTimeout(280ms)` + двойной `requestAnimationFrame`
  - [x] Backend не менялся; navbar/logout/footer/hero не тронуты
- [x] **Этап 3** — Roadmap-карточка результата + упрощение `.interactive-zone`:
  - [x] `.roadmap-section` → верхняя половина split-card: `max-width: 580px`, `border-radius: 20px 20px 0 0`, `background` + `border`
  - [x] `.iz-cta` → нижняя половина split-card: `border-top: none`, `border-radius: 0 0 20px 20px`, синхронизированный `max-width`
  - [x] Новые классы: `.iz-result-header` (иконка ✓ + заголовок), `.iz-result-check` (зелёный кружок), `.iz-result-title`
  - [x] HTML: `section-heading` внутри `roadmap-section` заменён на `iz-result-header`
  - [x] `.interactive-zone`: удалены `background`, `border-top`, `border-bottom` — только логическая обёртка и отступ (`padding: 64px 0`)
  - [x] JS-функции, roadmap/CTA, backend — не менялись

### v1.6.0 — Визуальный редизайн главной страницы (светлая тема)

- [x] **Этап 1** — Глобальные токены:
  - [x] `body { color }`: `#e2e8f0` → `#0f172a`
  - [x] `hero-tagline`, `hero-desc`: `#94a3b8` → `#475569`
  - [x] `section-heading h2`: `#e2e8f0` → `#0f172a`
  - [x] `section-heading p`: `#94a3b8` → `#475569`
  - [x] `section-title`, `features-heading`, `progress-header-title`: `#64748b` → `#475569`; рамка → синяя
  - [x] `hero-btn-secondary`: прозрачный → `rgba(255,255,255,0.75)` + синяя рамка; hover-цвет `#0f172a`

- [x] **Этап 2** — Hero mockup и мини-статистика:
  - [x] `hero-mockup`: `rgba(0.06)` → `rgba(255,255,255,0.96)` + синяя рамка + тень
  - [x] `mockup-topbar`: чёрный → `rgba(237,245,255,0.9)`
  - [x] `mockup-choice-correct`: неоновый зелёный → пастельный `#15803d` на `rgba(220,252,231,0.8)`
  - [x] `mockup-result-correct`: `#4ade80` → `#16a34a`
  - [x] `mockup-progress-track`: white-rgba → `rgba(37,99,235,0.1)`
  - [x] `hero-mini-stat`: `rgba(0.04)` → `rgba(255,255,255,0.92)` + тень; числа `#0f172a`, лейбл `#475569`

- [x] **Этап 3** — Start-flow карточки:
  - [x] `start-card`: `rgba(0.05)` → `rgba(255,255,255,0.92)` + синяя рамка + `box-shadow`
  - [x] `start-card-title/desc`: белый/серый → `#0f172a`/`#475569`
  - [x] `start-card-btn-1/2/3`: неоновые → дневные синий/фиолетовый/зелёный (`#2563eb`/`#4f46e5`/`#16a34a`)
  - [x] `start-arrow`: `#334155` → `#94a3b8` (мягкая стрелка на светлом фоне)

- [x] **Этап 4** — Интерактивная зона:
  - [x] `iz-dot`: white-rgba → `rgba(37,99,235,0.18)`; active → `#2563eb`
  - [x] `iz-step-connector`: white-rgba → `rgba(37,99,235,0.2)`
  - [x] `iz-next-btn`: прозрачный → `rgba(255,255,255,0.85)` + синяя рамка
  - [x] `minitask-card`: синий-прозрачный → белый + синяя тень; опции — синий тинт hover/selected
  - [x] `minitask-option-label`: `#e2e8f0` → `#0f172a`
  - [x] результаты minitask correct/incorrect/no-answer: неоновые → пастельные + тёмный текст
  - [x] `calculator-card`: фиолетовый-прозрачный → белый + фиолетовая тень
  - [x] `calc-option/selected`: тёмная тема → `rgba(237,233,254,…)` + `#4338ca`
  - [x] результаты calc low/mid/high/warn: неоновые → пастельные + тёмный текст
  - [x] `roadmap-section`: `rgba(0.04)` → `rgba(255,255,255,0.96)` + тень
  - [x] `roadmap-circle`: синий-прозрачный → `rgba(219,234,254,0.85)` / `#2563eb`
  - [x] `roadmap-circle-goal`: зелёный-прозрачный → `rgba(220,252,231,0.85)` / `#16a34a`
  - [x] `roadmap-step-title/desc`: белый/серый → `#0f172a`/`#475569`
  - [x] `iz-cta`: прозрачный → `rgba(255,255,255,0.96)` + тень; текст тёмный
  - [x] `iz-result-check`: неоновый зелёный → `rgba(220,252,231,0.9)` / `#16a34a`

- [x] **Этап 5** — Advantages / Features / Updates / News:
  - [x] `advantages-panel`: только рамка → + белый фон + тень; разделители → синие
  - [x] `advantage-item-title/desc`: белый/серый → `#0f172a`/`#475569`
  - [x] `feature-card`: `rgba(0.05)` → `rgba(255,255,255,0.92)` + тень; hover → голубой тинт
  - [x] `feature-title/desc`: белый/серый → `#0f172a`/`#475569`
  - [x] `update-card`: `rgba(0.04)` → `rgba(255,255,255,0.92)` + тень
  - [x] `update-title/desc`: белый/серый → `#0f172a`/`#475569`
  - [x] `news-item` border: `rgba(sky,0.25)` → `rgba(37,99,235,0.38)`
  - [x] `news-item.important` border: → `rgba(220,38,38,0.5)`
  - [x] `news-title-text`: `#cbd5e1` → `#0f172a`; `news-description`: `#94a3b8` → `#475569`

- [x] **Этап 6** — Progress / Guest CTA:
  - [x] `progress-card`: голубой-прозрачный → `rgba(255,255,255,0.95)` + синяя тень
  - [x] `progress-num`: `#e2e8f0` → `#1e40af` (синий акцент)
  - [x] `progress-label`: `#94a3b8` → `#475569`
  - [x] `guest-cta-card`: голубой-прозрачный → `rgba(255,255,255,0.95)` + синяя тень
  - [x] `guest-cta-title/desc`: белый/серый → `#0f172a`/`#475569`

- [x] **Этап 7** — Глобальная проверка (~55 CSS-селекторов, OK):
  - [x] `section-tag-blue/purple`: неоновые → дневные `#1d4ed8`/`#4338ca` на пастельных фонах
  - [x] `minitask-question`: `#e2e8f0` → `#0f172a` (пропущен в этапе 4)
  - [x] `footer-border` и `footer-bottom-border`: white-rgba → `rgba(37,99,235,0.08–0.1)`
  - [x] `advantage-item` (media query 768px): white-rgba → синяя рамка
  - [x] navbar: не тронут — все `#e2e8f0` и `rgba(255,255,255,…)` оставлены

- [x] **Этап 8** — Выполнен в рамках v1.6.1:
  - [x] Усиление теней при hover — `translateY` + усиленная `box-shadow` на всех карточках
  - [x] Плавные CSS-анимации для карточек — `transition: border-color 0.22s, box-shadow 0.22s, transform 0.22s`
  - [ ] Проверка на реальном desktop и мобильном (375px / 768px / 1280px) — перенесено в v1.6.2
  - [ ] Возможная корректировка navbar под светлую тему — перенесено в v1.6.2

### v1.5.6 — Персонализация воронки главной страницы

- [x] **Этап 1** — Перенос блоков прогресса и регистрации в воронку (13/13 OK):
  - [x] `progress-section` вырезан из позиции после Hero
  - [x] `guest-cta-section` вырезан из позиции после Hero
  - [x] Оба блока вставлены между `features-section` и «Что нового на платформе»
  - [x] Jinja2-условия сохранены дословно: `session.get('user_id')` / `not session.get('user_id')`
  - [x] CSS, содержимое блоков, JS, backend, navbar/logout/CSRF — не тронуты
- [x] **Этап 2 и далее** — включены в v1.6.0

### v1.5.5 — Структура и конверсия главной страницы

- [x] **Этап 1** — Устранение дублирования статистики (10/10 OK):
  - [x] `stats-row` полностью удалён: HTML + весь CSS (5 селекторов × 3 брейкпоинта)
  - [x] `hero-mini-stats` сохранён без изменений (числа 1200+, 27, 90+)
  - [x] Backend, app.py, маршруты не менялись
- [x] **Этап 2** — Усиление блока преимуществ (7/7 OK):
  - [x] 4 преимущества переписаны по модели «боль ученика → решение платформы»
  - [x] Новые заголовки: «Готовый маршрут от нуля до ЕГЭ» / «Все 27 тем — без пробелов» / «Видишь, где проваливаешься» / «Экзамен без страха»
  - [x] Структура, CSS, количество карточек — не менялись
- [x] **Этап 3** — Именованный индикатор шагов интерактивной зоны (12/12 OK):
  - [x] Старый индикатор «Шаг N из 3 + три точки» заменён на `.iz-steps-row` с именами шагов
  - [x] Шаги: «Задача» / «Готовность» / «Маршрут»; `.iz-step-connector` между ними
  - [x] CSS `:has(.iz-dot-active)` — подсветка активного лейбла без доп. JS
  - [x] `updateIzProgress()`: удалена 1 строка (`izProgressLabel`), логика точек не изменена
  - [x] `goToStep2()`, `goToStep3()`, `checkMiniTask()`, `checkReadiness()` — не менялись

---

## Ближайшие задачи

### v1.4.0 — Расширенный редактор задач

- [x] **Этап 1: форматирование текста** — панель B/I/sup/sub в `admin_task_form.html`.
  Кнопки `type="button"`, toolbar на всех абзацах (существующих и новых).

- [x] **Этап 2: изображения в редакторе** — загрузка, хранение, удаление. Полностью завершён.
  - Сохранение в `tasks/task_XX/images/` (автосоздание директории)
  - Имя файла: `t{task_num}_{task_id}_{timestamp}_{secure_filename}`
  - JSON `images[]`: `path`, `after_paragraph`, `size`, `alt`
  - Проверка: extension (`allowed_file`) + magic bytes (`is_valid_image`)
  - `MAX_CONTENT_LENGTH = 5 МБ`
  - Исправлено: orphaned files при дублировании `task_id`
  - Исправлено: path traversal в `delete_image` (`safe_to_delete = to_delete & existing_paths`)
  - Исправлен баг: `img-small/img-medium/img-large` не давали визуального эффекта в `reshenie.html` и `exam.html` — CSS-правила отсутствовали; добавлены в оба шаблона; размеры работают во всех шаблонах

- [x] **Этап 3: прикрепление файлов** — вложения `xlsx`, `xls`, `csv`, `txt`, `zip`, `db`, `sqlite`, `ods`, `odt` к задачам.
  - Файлы сохраняются в `tasks/task_XX/files/` (автосоздание директории)
  - Имя файла: `f{task_num}_{task_id}_{timestamp}_{secure_filename}`
  - JSON `file`: `path`, `name`, `description`; один файл на задачу
  - Whitelist расширений `allowed_task_file()`; `secure_filename`; path traversal при удалении заблокирован
  - Orphaned файл при замене: старый удаляется с диска до сохранения нового
  - Маршрутизация скачивания исправлена в `reshenie.html` и `exam.html`:
    `_source_task_num is defined` → `task_files`; иначе → `variant_files`
  - `tasks_view.html` уже использовал `task_files` корректно — изменений не потребовал

### Высокий приоритет

- [x] **Контент variant_14** — снято в v1.10.0: все 14 старых вариантов (включая variant_14-плейсхолдер) удалены и заменены 20 новыми, полностью наполненными через базу заданий.

- [x] **Аудит SQL-запросов на инъекции** — 72 запроса проверено, 0 инъекций,
  все используют `?`-плейсхолдеры. Закрыто в v1.3.0 (2026-06-16).

- [x] **Аудит XSS** — XSS-1 (admin_panel), XSS-2 (replace_markers), XSS-3
  (preparation_lesson) — все три исправлены. Закрыто в v1.2.2–1.2.4 (2026-06-15).

- [ ] **Проверка загрузки аватаров** — добавить проверку MIME-типа файла
  (сейчас проверяется только расширение), ограничить максимальный размер.

### Средний приоритет

- [ ] **Синхронизация между двумя ПК** — настроить git remote на GitHub,
  добавить `CLAUDE.md`, `DEVELOPMENT_LOG.md`, `TODO.md` в репозиторий.

- [x] **Наполнение базы задач — Задания №1–27, весь диапазон закрыт** — 2924 задачи импортировано с kompege.ru
  (v1.8.0: 317 = 113+116+88; v1.8.1: 780 = 122+125+78+122+127+91+115; v1.8.2: 851 =
  120+60+100+128+130+118+85+110; v1.8.3: 763 = 113+113+113+110+119+75+120; v1.9.0: 214 =
  124+90). Финальный сплошной аудит всей базы проведён в v1.9.0 — 23/27 чисто, 1 дефект исправлен.

- [ ] **Наполнение теории** — `theory/task_01`…`task_05` (v1.12.0–v1.12.5) и
  `task_26` (наполнен раньше, но требует переработки) реально наполнены,
  остальные 21 тема (6–25, 27) пустые. Конструктор полностью готов
  (v1.12.0–v1.12.3, включая списки/код) — дополнить содержимым.

- [ ] **Добавить уроки** — сейчас только `urok_01` и `urok_02`. Расширить.

---

## Технический долг

### Stage 3 (из плана аудита)

- [ ] **Унификация логики подсчёта баллов** — дублирующийся код подсчёта
  `score` / `secondary_score` в обработчиках `save_results` и `finish_exam`.
  Вынести в единую функцию.

- [ ] **Централизованное управление соединениями БД** — сейчас `get_db()`
  открывает новое соединение на каждый запрос и закрывается вручную.
  Использовать `g` объект Flask или контекстный менеджер для гарантированного
  закрытия соединений.

- [ ] **Логирование** — добавить структурированные логи для:
  - ошибок авторизации
  - CSRF-ошибок
  - ошибок записи в БД
  - попыток доступа к чужим данным

- [ ] **Базовый шаблон (base.html)** — все 27 шаблонов самостоятельны,
  CSS и часть структуры дублируются. Рефакторинг на наследование сократит
  объём и упростит поддержку. ⚠️ Большое изменение — только после полного
  согласования.

### Мелкий долг

- [ ] **`admin_task_delete` не удаляет файлы и изображения с диска** — при удалении задачи
  из базы через `/admin/tasks/<task_num>/delete/<task_id>` связанные файлы
  (`tasks/task_XX/files/`) и изображения (`tasks/task_XX/images/`) остаются на диске как orphaned.
  Нужно добавить вызов `_delete_task_file_from_disk` и цикл удаления изображений
  аналогично тому, как это сделано в `admin_task_edit`.

- [ ] `constructor_editor.html` — нет fetch-запросов, проверить актуальность
  функционала (шаблон, возможно, устарел).

- [ ] `/api/variant/preview` (app.py:2044) — маршрут существует, но не вызывается
  ни из одного шаблона. Выяснить назначение: удалить или подключить.

- [ ] `admin_task_form.html` — форма без `action`, принимает текущий URL.
  Проверить корректность редиректов после сохранения задачи.

- [x] `attempt_detail.html` — шаблон создан, маршруты `/stats/attempt/<attempt_id>`
  и `/admin/user/<user_id>/attempt/<attempt_id>` работают. Задача выполнена.

---

## Известные проблемы

| Проблема | Приоритет | Описание |
|---|---|---|
| Нет проверки MIME аватаров | Средний | Проверка только по расширению файла |
| Дублирование логики баллов | Низкий | В save_results и finish_exam |
| Нет логирования ошибок | Низкий | Тихие сбои не фиксируются |
| Отсутствие rate limiting | Низкий | Форма входа без ограничений попыток |

---

## Идеи для улучшения

### UX и функционал
- [ ] **Email для восстановления пароля** — сейчас нет механизма сброса.
- [ ] **Прогресс-бар на главной** — показывать % выполненных заданий.
- [ ] **Рейтинг пользователей** — таблица лучших результатов по вариантам.
- [x] **Мобильная адаптация главной страницы** — `start.html` полностью адаптирован
  для ≤480px: компактный hero, dropdown navbar, горизонтальные карточки прогресса,
  уменьшены padding всех блоков. Завершено в v1.5.0.
- [ ] **Мобильная адаптация остальных страниц** — `exam.html` (таблицы ответов),
  `reshenie.html`, `stats.html` плохо работают на телефоне.
- [ ] **Экспорт результатов** — PDF или CSV для педагогов.
- [x] **Светлая тема** — все 15 пользовательских страниц переведены на светло-голубую тему в v1.6.5. `exam.html`, `reshenie.html`, admin-шаблоны — вне scope. Переключатель тем — не реализован.

### Техническое
- [ ] **Кэширование JSON-файлов** — `variants/*.json`, `tasks/*.json`, `theory/*.json`
  перечитываются с диска при каждом запросе. Добавить простой in-memory кэш.
- [ ] **Health check endpoint** — `GET /healthz` для мониторинга.
- [ ] **Автоматическое создание папок** при добавлении нового варианта/задания
  через конструктор (сейчас папки создаются вручную).

---

## Безопасность (следующий аудит)

### Выполнено

- [x] Аудит прав доступа к API-маршрутам — исправлены `/api/variants/save` и `/api/variant/preview` (добавлена проверка is_admin, 403 для обычных пользователей)
- [x] CSRF при выходе — `GET /logout` переведён на `POST`, CSRF-формы добавлены в `start.html` и `profile.html`
- [x] Stored XSS #1 (критический) — `admin_panel.html:193`: `username` вынесен из JS-литерала в `data-username` атрибут, читается через `this.dataset.username`
- [x] XSS-2 (высокий) — `replace_markers` в `app.py`: уязвимая ветка `Markup(text)` удалена; реализован whitelist-pipeline (placeholder → escape → restore) для `sup`/`sub`/`b`/`i`; `script`/`img`/`iframe`/`onerror` и любые другие теги теперь экранируются; 12/12 тестов прошли, регрессий в реальном контенте нет.
- [x] XSS-3 (средний) — `| safe` в `preparation_lesson.html:357,390` заменён на `| replace_markers`; `<b>` в urok_01.json сохраняется; `script`/`img`/`iframe`/`svg`/`onerror` экранируются; 6/6 XSS-векторов безопасны; регрессий нет.

**XSS-аудит закрыт: XSS-1 ✅ XSS-2 ✅ XSS-3 ✅**

### Ожидает выполнения

#### P3 Этап 1 — выполнено (2026-06-16)

- [x] **`POST /clear_stats`** — добавлен явный auth-guard (`if "user_id" not in session → 401`);
  `str(e)` заменён на generic-сообщение; добавлен DELETE из `user_task_answers` перед
  DELETE из `user_results` (устранены осиротевшие записи).

- [x] **`POST /save_results/<variant_num>`** и **`POST /finish_exam/<variant_num>`** —
  добавлен явный auth-guard; добавлена проверка `if not tasks → 404` для несуществующих
  вариантов; в `finish_exam` `user_id` вынесен в переменную.

#### P3 Этап 2 — ожидает выполнения

- [ ] **Double-submit защита** для `/save_results` и `/finish_exam` —
  пользователь может вызвать эндпоинты произвольное число раз, каждый вызов
  создаёт новую строку в `user_results` и `user_task_answers`.

#### Остальное

- [x] Полный аудит SQL-запросов на параметризацию — 72 запроса проверено, 0 инъекций,
  все используют `?`-плейсхолдеры, пользовательский ввод не попадает в ORDER BY / LIMIT.
- [ ] Rate limiting на `/login` и `/register`
- [ ] Проверка загрузки файлов (MIME, размер, имя файла)
- [ ] Content Security Policy (CSP) заголовки
- [ ] Аудит прав доступа к файловым маршрутам (`/variant_files/`, `/theory_videos/`)

---

## Целостность данных (из SQL-аудита 2026-06-16)

- [x] **D-1: `delete_user()` не удаляет связанные записи** — исправлено в v1.2.6.
  `POST /admin/delete_user/<user_id>` теперь удаляет все данные пользователя из 7 таблиц
  в правильном порядке: `user_task_answers` → `user_results` → `user_lesson_progress` →
  `user_theory_progress` → `user_lesson_access` → `user_theory_access` → `users` (последней).

- [x] **D-2: `task_id` из JSON не валидируется как `int`** — исправлено в v1.3.0.
  Строгая проверка типов: `int` и digit-строка `"5"` принимаются; `float`, `bool`,
  `null`, `list`, `object` отклоняются с 400 до записи файлов на диск.
  Атомарность восстановлена: при ошибке папка варианта не создаётся. 9/9 сценариев проверены.

- [x] **D-3: `str(e)` в двух admin-only маршрутах** — исправлено в v1.3.0.
  `/api/theory/save` и `/api/variants/save` возвращают generic-сообщение при исключении.
  `str(e)` не раскрывается даже администратору.
