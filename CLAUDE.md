# CLAUDE.md — Руководство по проекту для Claude Code

## Описание и назначение

EGEvolution — учебная платформа для подготовки к ЕГЭ по информатике.
Сайт: egevolution.ru

Пользователи проходят варианты ЕГЭ, изучают теорию по заданиям 1–27,
решают практические задачи в уроках. Администратор управляет контентом
через встроенную панель.

**Текущая версия: v1.16.0** — наполнены теорией уроки №2–6 учебного
плана «Python с нуля» (ввод/вывод/типы/переменные, строки, условия, циклы,
списки и множества), из них урок №3 (строки) написан полностью
самостоятельно — презентация пользователя перескакивает с переменных сразу
на условия, раздела про строки в ней нет вовсе. Там, где материал был по
презентации, отдельно проверялся на смысловые пробелы против того, что
реально нужно для практики, и по подтверждению пользователя дополнялся:
урок 3 — срез с шагом/разворот строки `s[::-1]`, `.join()`, перебор строки
в `for`, `isdigit()`/`isalpha()`; урок 4 — комбинирование условий
`and`/`or`; урок 5 — `range()` с одним аргументом и `break`/`continue`;
урок 6 — `sum()`, оператор `in`, `set()` для удаления дубликатов, плюс
исправлена неточность самой презентации («`add()` добавляет элемент в
конец множества» — у множества нет порядка, значит и «конца» нет). По
ходу тестирования новой фичи (см. ниже) в собственном же контенте урока
№6 найден и исправлен живой баг — `print(A[i])` совпадал с BBCode-маркером
курсива `[i]` в `replace_markers()` (тот же баг класса, что и в v1.12.18),
исправлено переименованием индекса `i → k`.

Помимо контента — новая визуальная фича: блок кода `<pre>` для
многострочных примеров. Раньше такие примеры (if/elif/else, while, for,
break/continue) хранились построчно — по одной строке на `<code>`-абзац с
ручными отступами через NBSP, и на живой странице выглядели как
разрозненные «пилюли» с зазором между ними (замечено пользователем по
скриншоту) — `.theory-content p` даёт `margin-bottom`/`text-indent`,
рассчитанные на прозу, а не на код. Из трёх рассмотренных вариантов
(настоящий `<pre>`-блок / склейка через `<br>` внутри `<code>` / группировка
на уровне шаблона без изменения whitelist) пользователь выбрал первый как
самый полный. `<pre>`/`</pre>` добавлены в whitelist `replace_markers()` по
той же безопасной схеме (непересекающиеся `\x00PH23\x00`/`\x00PH24\x00`,
как и у остальных 11 тегов); `preparation_lesson.html` — условие «абзац не
оборачивать в `<p>`» (раньше работало только для `<ul>/<ol>/<li>`) расширено
на целые `<pre>...</pre>` абзацы. Контент теперь хранит весь многострочный
пример одной строкой массива с настоящими `\n` — NBSP-костыль для новых
блоков больше не нужен. 7 примеров в уроках 4–6 переведены на `<pre>`;
списки операторов/методов (не единый код, а перечень раздельных примеров)
оставлены как есть. Визуальный стиль — тёмная Darcula-тема (`#2b2b2b`,
текст `#a9b7c6`, в тон PyCharm из урока №1) с декоративной «шапкой
редактора» (три кружка через `pre::before`/`box-shadow`, без разметки в
HTML) — по прямому желанию пользователя, вместо в цвет остального сайта.
Попутно — стиль текстовых полей абзацев в конструкторе уроков
(`constructor_uroki.html`, новый класс `.paragraph-textarea`; раньше поля
были вообще без CSS — голый `<textarea>` без обёртки `.form-group`, отсюда
мелкий нечитаемый вид на скриншоте пользователя), применён только в этом
файле.

v1.15.1 — точечный патч сразу после v1.15.0, найден
пользователем при собственном тестировании гостевого доступа (незалогиненный
посетитель). Картинки не отображались у гостя сразу в трёх местах: база
заданий (`/tasks/<n>`, любой номер 1–27), тема теории №1 (`/theory/1`) и
урок №1 (`/preparation/1`) — везде страница открывалась нормально, а каждая
картинка на ней превращалась в битую иконку. Причина — глобальный
`before_request`-гейт `check_auth()` открывает гостю сами страницы (список
`allowed` + исключения для `task_num==1`/`lesson_id==1`), но не файловые
маршруты, которые реально отдают картинки/видео/файлы (`task_images`,
`theory_images`, `theory_videos`, `theory_files`, `preparation_images`,
`preparation_videos`, `preparation_files`) — каждый `<img src="...">` на
открытой странице был отдельным запросом, который тот же гейт молча уводил
на `/login`. Баг жил с v1.10.0 (тогда открыли сами страницы гостю), но
оставался незамеченным, пока в уроке №1 не появился реальный контент с
картинками (v1.15.0). Исправлено точечно: `task_images`/`task_files`
добавлены в `allowed` безусловно (база задач открыта гостю для всех 27
номеров), `theory_images`/`theory_videos`/`theory_files` и
`preparation_images`/`preparation_videos`/`preparation_files` — по
аналогичному с `theory_task`/`preparation_lesson` условию (открыты гостю
только для `task_num==1`/`lesson_id==1`, чтобы не открыть картинки закрытых
тем/уроков сверх того, что открыто на самих страницах). `variant_images`/
`variant_files` сознательно не тронуты — их родительские страницы
(`reshenie`/`exam`) гостю не открыты вообще, поэтому там та же проблема
пока не проявляется.

v1.15.0 — первое реальное содержимое урока начальной
подготовки: пользователь самостоятельно, через новый конструктор уроков
(v1.14.0), наполнил урок №1 «Установка Python и PyCharm» — 33 абзаца и 17
картинок-скриншотов пошаговой установки. По запросу пользователя проведён
независимый обзор контента (без правок): найдены и по отдельному
подтверждению исправлены 5 опечаток/грамматических ошибок и один
содержательный пробел — абзац «За что отвечает каждый пункт:» анонсировал
объяснение чекбоксов установщика PyCharm, но самого объяснения не было;
добавлены 4 коротких абзаца с описанием (Desktop Shortcut, "Open Folder as
Project", .py-ассоциация, добавление в PATH) — по общим знаниям об
установщике JetBrains, с явной пометкой пользователю сверить с реальным
скриншотом. Отдельно добавлен ещё один абзац про альтернативный способ
установки Python — через Microsoft Store (скриншот прислан пользователем
прямо в чат; у Claude Code нет инструмента для сохранения вложений чата
на диск, поэтому саму картинку пользователь прикрепил самостоятельно
через конструктор — сработало без замечаний).
Попутно найден и исправлен живой баг вёрстки: `templates/preparation_lesson.html`
показывал карточки теории и практики полностью пустыми у пользователя,
хотя `Flask test_client()` рендерил их корректно и в консоли браузера не
было ошибок JS — причина в общей анимации появления блоков
(`static/js/scroll-reveal.js`): элемент с классом `.reveal` остаётся
на `opacity: 0`, пока не наберёт 12% видимой площади в вьюпорте
(`IntersectionObserver`, `threshold: 0.12`); для очень высокой карточки
(37+ абзацев и множество картинок) это условие практически никогда не
выполняется при обычной прокрутке. Класс `reveal` убран у карточек теории
и практики урока — по прецеденту, уже применённому ранее для длинного
блока практики в `teoria_zadanie.html` (короткие карточки — видео,
шапка — анимацию сохранили).

v1.14.0 — первый шаг наполнения уроков начальной подготовки
(следующий фронт пункта 4 из v1.11.0 после завершения всей теории в v1.12.27):
согласован и структурно реализован учебный план из 12 уроков «Python с нуля»
(установка Python/PyCharm → ввод/вывод, типы, переменные → строки → условия →
циклы → списки и множества → функции → понимание и исправление ошибок → работа
с визуализатором кода → двумерные списки/таблицы-матрицы → чтение файлов кодом →
работа в Excel напрямую), составленный по присланной пользователем презентации
(разделы 1–5, независимо перепроверенной запуском кода) и расширенный по итогам
обсуждения. Старое содержимое `urok_01`/`urok_02` (обе — тестовая тема «Системы
счисления», одна из них незаконченная заглушка) полностью удалено и заменено на
12 пустых уроков-скелетов (только заголовки, без текста теории/практики —
наполнение контентом — следующий шаг). Одновременно с этим построен админский
конструктор уроков — `templates/constructor_uroki.html`, самостоятельный клон
конструктора теории (не единый мультирежимный инструмент — разница в схеме
слишком существенная), доведённый до полной функциональной параллели: выбор
урока из списка, массовый ввод текста с разбиением на абзацы, тулбар
форматирования, картинки к любому абзацу теории и практики, полный редактор
практических задач (заголовок/описание/картинки/файл/переключатель типа ответа
один-два-таблица). По ходу построения конструктора схема урока обновлена по
явному запросу пользователя: видео заменено с одиночного `video` на массив
`videos` (несколько видео на урок, как уже было у теории), добавлено новое поле
`description` (короткий блурб для каталога `/preparation`, которого нет у
теории), и впервые заведена папка `practice_files/` для прикреплённых файлов
практических задач урока (раньше не существовала вообще). `preparation_lesson.html`
обновлён под оба новых поля — рендерит несколько видео (с обратной
совместимостью на старый одиночный формат) и заголовок/файл практической
задачи, которые раньше в шаблоне не отображались, даже если бы были заданы
вручную в JSON. Новые backend-роуты: `GET/POST /api/uroki/*`,
`GET /preparation_files/<lesson_id>/<filename>`, `GET /constructor_uroki`;
третья карточка в `constructor_gate.html`. Весь новый функционал (конструктор,
сохранение, отображение) проверен сквозным сценарием через `Flask test_client()`
— 37 проверок, включая реальную загрузку картинки и прикреплённого файла с
последующей проверкой их на диске и на отрендеренной странице урока.

v1.13.0 — после завершения всего диапазона теории (v1.12.27)
проведён аудит сайта глазами незарегистрированного нового посетителя, по
итогам которого закрыт главный найденный барьер конверсии: самостоятельная
регистрация требовала пароль сайта, который можно было узнать только лично
у владельца (Telegram/MAX/звонок) — для органического посетителя с
рекламы/соцсетей это был полный стоп, прямо противоречащий роли сайта как
«визитной карточки» (см. v1.11.0). Страница `/register` полностью
переработана из формы регистрации в форму заявки (Фамилия, Имя, Телефон,
способ связи на выбор — Telegram/VK/MAX с подстраивающимся под выбор полем
контакта): теперь она не создаёт аккаунт напрямую, а сохраняет заявку в
новую таблицу `site_applications` и уведомляет администратора в Telegram
(через Bot API одним HTTP-запросом на `urllib` из стандартной библиотеки,
без новых зависимостей; отправка не критична для самой заявки — сбой
Telegram не мешает её сохранению). Поскольку самостоятельная регистрация
исчезла, в админ-панели добавлены два новых инструмента: список заявок
(`/admin` — необработанные заявки таблицей с кнопками «Создать аккаунт» и
«Обработана»; обработанные уходят в свёрнутый список с кнопкой «Вернуть в
необработанные» — переключатель в обе стороны, ничего не удаляется) и
ручное создание пользователя (`/admin/create_user` — админ сам вводит
логин и пароль, видит его в момент ввода; поддерживает переход с
предзаполненным именем прямо из заявки). Пароль администратор теперь не
«смотрит» у существующих пользователей (хэши необратимы), а сам
устанавливает при создании аккаунта — обсуждали более раннюю идею
«показать пароль», но остановились на этом безопасном варианте. Главная
страница обновлена под новую логику в четырёх местах (навбар, блок для
гостей, карточка «Запишись на первое занятие», плавающий виджет «Есть
вопросы?») — везде добавлена или заменена кнопка «Оставить заявку» рядом с
прямыми контактами Telegram/MAX. Помимo этого в рамках того же аудита:
админ-панель получила постатейную детализацию прогресса ученика по каждому
уроку/теме теории (`/admin/user/<id>/lesson/<id>`,
`/admin/user/<id>/theory/<num>` — какие именно задачи решены верно, сколько
попыток), и исправлен баг с кэшированием картинок на главной (видео-превью
и превью визуализатора отдавались браузером из 45-дневного кэша без
версионирования URL — добавлен `?v={{ app_version }}`, как уже было сделано
для CSS/JS в v1.10.2). `SITE_PASSWORD` по прямому решению пользователя пока
оставлен нетронутым в коде и `.env`, хотя в новой логике заявок он уже не
используется.

v1.12.27 — завершение пункта 4 из v1.11.0 (наполнение
теории контентом): наполнены теория и практика заданий №19–27 — этим закрыт
весь диапазон теории №1–27, включая переработку задания №26. Задания №19–21
(выигрышная стратегия, игра с кучами камней) — связанная тройка с ОБЩЕЙ
теорией (по явному решению пользователя один и тот же текст размещён на всех
трёх страницах `/theory/19–21`, а не по три разных); презентация проверена
кодом и содержала два реальных дефекта — лишний ход `+2` в примере с кучей
и путаница между двумя разными формулировками Вопроса №19 («известно, что
Ваня выиграл… когда это возможно» — простой перебор, и «при любом ходе
Пети» — стратегия any/all), из-за чего эталонные ответы презентации (34 и
пустой список) оказались неверны (правильно — 18 и 21, проверено на обоих
примерах). Задание №22 (анализ процессов): помимо стандартного расписания
«старт как можно раньше» найден и проверен на реальных файлах доминирующий
тип вопроса (31% базы) — «отрезок с K одновременными процессами», требующий
НЕ стандартного расписания, а намеренных задержек независимых процессов
(два подтипа: свободная задержка и задержка с сохранением минимального общего
времени через метод критического пути); также найдена формулировка-ловушка
(«минимальное время выполнения процесса» = момент завершения, а не
длительность из таблицы). Задание №23 (перебор программ исполнителя): все
примеры презентации проверены кодом, добавлен не описанный в презентации
подтип — команды работы с цифрами числа (замена цифры, перестановка разрядов),
проверен на 3 реальных задачах. Задание №24 (регулярные выражения): найдена
и исправлена ловушка порядка альтернатив в regex (короткий вариант-префикс
после длинного даёт заниженный ответ) и добавлен целый недостающий тип —
«окно с N вхождениями подстроки» (~25% реальной базы, есть в базе, но не в
презентации), с найденной и исправленной ошибкой на границе окна для
многосимвольных подстрок. Задание №25 (делители и маски чисел): все примеры
презентации проверены кодом; попутно найден и исправлен реальный баг сайта —
`teoria_zadanie.html` использовал несуществующий в Jinja2 атрибут
`loop.parent` для рендера табличных ответов (`answer_grid`), из-за чего
страница падала с ошибкой 500 при первом же использовании этого формата
в практике теории. Задание №26 (жадные алгоритмы) — впервые переработано:
старая двухабзацная заглушка и 8 тестовых практических задач заменены на
28 абзацев теории (7 типов задач — вместимость в двух вариантах, «наоборот»,
матрёшка, минимум групп, ячейки по времени, два окна с очередью) и 20 новых
задач практики, все алгоритмы проверены на реальных файлах базы. Задание №27
(кластеризация звёздного неба) впервые построено полностью без презентации:
восстановлен и проверен на 4 парах реальных файлов (16 отдельных чисел, все
совпали) метод кластеризации точек через минимальное остовное дерево (так как
прямоугольники кластеров могут быть повёрнуты — простой bounding box не
работает), нахождение центра кластера как точки с минимальной суммой
расстояний (не среднего), и полная таблица классификации звёзд по цвету и
типу светимости, нигде не данная текстом явно и восстановленная только по
численному совпадению с эталонными ответами; попутно расширен шаблон
`teoria_zadanie.html` на поддержку нескольких файлов на одну практическую
задачу (`task.files`), перенесено из уже существующего паттерна в
`tasks_view.html`/`reshenie.html`/`exam.html` для этого же задания. Итого
теория реально наполнена по всем 27 темам.

v1.12.18 — продолжение пункта 4 из v1.11.0 (наполнение
теории контентом): наполнены теория и практика заданий №11–18, из них №11–17 —
по презентациям пользователя (PDF; для №12 — только последние 3 слайда, первые
относились к старому формату задания), №18 — впервые без презентации, по
итогам изучения реальной базы задач. Задание №11 (вычисление количества
информации): теория по формулам презентации, расширена на составной алфавит
(мощность = сумма частей списка символов) и обратные задачи (дан объём
памяти — найти N/K/G/P через перебор, как в задании №5). Задание №12 (Машина
Тьюринга): презентация покрывала только «обратный подсчёт» (сколько было
исходных символов по итоговому счётчику); сверка с реальной базой (60 задач,
у каждой своя уникальная таблица) выявила ещё два типа вопроса — прямое
преобразование числа через таблицу и «разворот» результата к исходному числу
— оба добавлены и проверены полным программным симулятором Машины Тьюринга
на настоящих таблицах реальных задач. Задание №13 (адресация в сетях): все 6
методов презентации проверены кодом; расширено на широковещательный адрес
(`IP | ¬маска`, плюс формат ответа «сумма октетов») и подсчёт адресов в
подсети по условию на биты (с найденной закономерностью — для условий на
чётность единиц ответ всегда ровно половина подсети). Задание №14 (кодирование
чисел, системы счисления): 4 метода презентации проверены запуском; добавлен
доминирующий реальный тип (~77% базы) — «большое выражение минус x с
границей», решается тем же способом (прямое вычисление в Python + перебор x).
Задание №15 (преобразование логических выражений): 4 типа переменной из
презентации проверены кодом; добавлен предикат `div()` и — самое важное —
предостережение про размер диапазона перебора x/y: на реальной задаче показано,
что маленький диапазон «на глаз» даёт неверный ответ (9802 вместо истинных
1176126), если в самой формуле встречаются большие числа. Задание №16
(рекурсивные алгоритмы): проверен критически важный приём презентации —
без `sys.setrecursionlimit()` пример с глубиной рекурсии 2024 действительно
падает с `RecursionError` (лимит Python по умолчанию — 1000), с увеличенным
лимитом считается верно; добавлена взаимная рекурсия двух функций F/G.
Задание №17 (обработка числовой последовательности): произвольные пары из
презентации и соседние пары/тройки из её же практики проверены кодом;
добавлены симметричные пары и локальные экстремумы. По ходу работы найден и
исправлен реальный баг `replace_markers()` — запись вида `a[i]` в тексте
теории случайно совпадает с BBCode-маркером `[i]` (курсив) из whitelist'а,
из-за чего скобка «съедалась»; тот же баг уже был живым в **запущенном**
задании №9 (`row[i]`) — исправлен и там (переименована индексная переменная).
Задание №18 (Исполнитель Робот на квадратном поле) впервые в проекте построено
целиком без презентации и потребовало отдельных парсеров ODS/XLSX для чтения
не значений, а утолщённых границ ячеек (стен) — готовых библиотек в песочнице
нет; метод (перебор стен + двумерное динамическое программирование) проверен
на 4 реальных файлах базы (включая находку и исправление собственного бага
наследования стиля ячейки от колонки по умолчанию). Итого реально наполнены
темы теории №1–18 (задание №26 наполнено ранее, но требует переработки и в
счёт не идёт). Остальные 8 тем — следующий шаг пункта 4.

v1.12.10 — продолжение пункта 4 из v1.11.0 (наполнение
теории контентом): наполнены теория и практика заданий №6–10, из них №6–8 —
по презентациям пользователя (PDF), №9–10 — впервые полностью самостоятельно,
без презентации, по итогам изучения реальной базы задач. Задание №6
(результаты работы простейших алгоритмов, исполнитель «Черепаха»): теория по
методу презентации (справочник команд + разбор ловушки «число повторов ≠
число сторон многоугольника» + метод пересечения двух прямоугольников через
поднятие/опускание хвоста), 2 сгенерированные иллюстрации (Pillow). Задание №7
(кодирование/передача изображений и звука): теория по формулам из
презентации, плюс найденный на реальной базе (не в презентации) раздел
«осторожно с округлением» — направление округления зависит от точной
формулировки вопроса, подтверждено на паре контрастных реальных задач.
Задание №8 (перебор слов и системы счисления): презентация покрывала только
перебор слов (71% реальной базы); обнаружен и по решению пользователя закрыт
пробел — 28% базы составляют задачи на системы счисления, для них
самостоятельно построен и перепроверен раздел теории (метод «цифра-разделитель»
для условия «ровно одна цифра X», лежащий в основе двух новых проверенных
примеров). Задания №9 («Работа с большим набором данных» — файлы-таблицы на
20–30 тыс. строк) и №10 (поиск слов/сочетаний букв в тексте) впервые в проекте
целиком построены с нуля по изучению реальной базы задач, без презентации;
оба содержат каждый по нескольку численно перепроверенных прямым запуском
кода примеров на настоящих приложенных файлах задач, включая находки, которых
не было бы видно без такой проверки (в задании №10 — файл почти всегда
содержит больше глав, чем нужно по условию, и в заголовках глав встречаются
опечатки, ломающие наивный поиск границ). Итого реально наполнены темы теории
№1–10 (задание №26 наполнено ранее, но требует переработки и в счёт не идёт).
Остальные 16 тем — следующий шаг пункта 4.

v1.12.5 — продолжение пункта 4 из v1.11.0 (наполнение
теории контентом): наполнены теория и практика заданий №4 и №5 — впервые по
готовым презентациям пользователя (PDF), а не с нуля. Задание №4 («Условие
Фано», кодирование): теория опирается на метод двоичного дерева из
презентации пользователя, дополнена двумя случаями (ограниченный алфавит /
весь алфавит — во втором нужно резервировать узел дерева под неперечисленные
буквы, правило подтверждено пересчётом реальной задачи из базы — без резерва
получается неверный ответ); 4 иллюстрации дерева сгенерированы программно
(Pillow — matplotlib недоступен в песочнице), а не взяты сканом из
презентации. Задание №5 (алгоритмы для исполнителей): теория включает
справочник функций Python из презентации, разобранный пример с кодом
(независимо перепроверен запуском), перевод в системы счисления 3–9 и 11–36
по коду пользователя (найдена и исправлена опечатка — `a //= 3` вместо
`a //= 11` в примере для баз 11–36, из-за которой перевод в непроверенном
виде давал неверный результат) и альтернативный «второй вид» задания
(арифметика над цифрами числа) — переработан по присланному пользователем
рисунку (метод с общей цифрой b), с собственной сгенерированной
иллюстрацией. Практика обоих заданий — по 20 задач из базы, возрастающей
сложности. Итого реально наполнены темы теории №1–5 (задание №26 наполнено
ранее, но требует переработки и в счёт не идёт). Остальные 21 тема —
следующий шаг пункта 4.

v1.12.3 — продолжение пункта 4 из v1.11.0 (наполнение
теории контентом): наполнены теория и практика заданий №2 и №3, плюс два
точечных исправления конструктора/сайта. (1) Живой баг: списки (`<ul>/<ol>/<li>`)
и инлайн-код (`<code>`) в тексте теории показывались как escaped-текст —
`replace_markers()` в `app.py` расширен на эти теги, а в 4 шаблонах
(`teoria_zadanie.html`, `reshenie.html`, `tasks_view.html`,
`preparation_lesson.html`) убрана лишняя `<p>`-обёртка вокруг абзацев, которые
сами являются списочной разметкой — иначе `<ul>`/`<li>` вложились бы в `<p>`
некорректно; `exam.html` осознанно не тронут. (2) Единый стиль `<code>`
(синий чип в цвет акцента сайта) добавлен в `design-system.css` — теперь
действует на всех 16 страницах, наследующих `base.html`; в тулбар
конструктора теории добавлена кнопка `</>`. (3) Блок «Ваш прогресс» на
главной странице (`start.html`) вместо захардкоженных чисел (145 / 9 из 27 / 90,
одинаковых для всех пользователей) теперь считает реальные данные —
`load_homepage_progress()` в `app.py` суммирует верные ответы по вариантам,
практике теории и урокам; для пользователей без истории — мотивационная
плашка вместо нулей. Заодно заменены два фото в секции «Видеоразборы и уроки»
на реальные скриншоты сайта. (4) Задание №2: теория (синтаксис Python +
таблица перевода логических связок в операторы) не менялась по содержанию
(лишь пробел в тексте — правка пользователя), практика пересобрана на 20 задач
из базы возрастающей сложности. (5) Задание №3 — теория написана с нуля по
итогам изучения реальных задач и вложенных файлов баз данных (структура
«главная таблица операций + 1–2 справочные таблицы, связанные по ключу»,
обязательный разбор функции ВПР с синтаксисом и примером, план решения);
практика пересобрана на 20 задач из базы (единственное задание из трёх, где
практика включает не только картинки, но и сами приложенные файлы баз
данных .xlsx/.ods). Итого реально наполнены темы теории №1, №2, №3 (задание
№26 уже было наполнено ранее, но по решению пользователя не учитывается в
этом тезисе — потребует переработки). Остальные 23 темы — следующий шаг
пункта 4.

v1.12.0 — первый шаг по пункту 4 из v1.11.0 (наполнение
теории контентом): конструктор теории (`constructor_theory.html`) доведён до
полной функциональной параллели со старым конструктором вариантов, плюс
реально наполнена теория задания №1. Пять частей за сессию: (1) 4-этапная
доработка конструктора теории (согласована и выполнена по этапам с
подтверждением после каждого) — этап 1: выбор номера задания сеткой кнопок
вместо `<select>` + массовый вставка текста с авторазбиением на абзацы; этап
2: тулбар форматирования абзацев (Ж/К/Подч./степень/индекс), попутно
исправлен `replace_markers()` — whitelist расширен на `<strong>`/`<em>`/`<u>`
(старый контент с этими тегами показывался как escaped-текст на экране); этап
3: картинки к любому абзацу теории (загрузка/удаление, `/api/theory/save`
переведён с JSON на `FormData`+multipart); этап 4: практические задачи теории
получили полный редактор — многоабзацное описание с тем же тулбаром, картинки
к любому абзацу конкретной задачи, прикреплённый файл, переключатель типа
ответа (одно поле/два поля/таблица) со строгой взаимоисключаемостью полей
(`correct_answer`/`answers`/`answer_grid`), сворачиваемые карточки задач
(новые — раскрыты, уже сохранённые — свёрнуты, т.к. на тему может быть ~20
задач); (2) конструктор переведён на несколько видео на тему (`theory.videos`
вместо одиночного `theory.video`, как уже было у задания №26) — старый формат
при открытии в конструкторе автоматически переводится в массив, при этом
попутно исправлен живой баг: сохранение темы, уже хранящей только `videos`
(например, задание 26), падало с `TypeError`, т.к. код безусловно писал в
`theoryData.video.path`; (3) увеличен начальный размер текстовых полей абзаца
(`rows` 3–4 → 8) — раньше приходилось растягивать вручную; (4) исправлены 5
опечаток/грамматических ошибок в тексте теории задания №1 (задвоенное слово,
согласование рода, пробел перед скобкой, невнятная формулировка, лишний `\n`
в JSON); (5) практический блок задания №1 пересобран: старые 2 тестовые
задачи (одна из них — с конфликтующими полями ответа, оставшимися от более
раннего теста конструктора) удалены вместе с файлами, вместо них подобраны и
добавлены 20 задач из базы задания №1 (113 задач), отсортированные по
возрастанию сложности (12 лёгких → 6 средних → 2 сложных) и по возрастанию
визуальной сложности картинки графа внутри каждого уровня. Блок теории
(текст/картинки/видео задания №1, отдельно от практики) и `app.py`-бэкенд
`/api/theory/save` в части (2)–(5) не менялись — только конструктор и
содержимое `theory/task_01/`. Остальные 26 тем теории всё ещё не заполнены —
следующий шаг пункта 4.

v1.11.0 — смена приоритета платформы: подготовка сайта к
роли «визитной карточки» для привлечения новых клиентов (обзорные видео,
продвижение в соцсетях), функционал решения вариантов временно отложен. Три
пункта за сессию: (1) убран видимый текст версии сайта из публичного футера
(`components/footer.html`, `start.html`) — случайному посетителю `vX.Y.Z` в
футере читается как «сайт в разработке»; техническая инфраструктура версии
(cache-busting статики) не тронута; (2) `reshenie.html` (обычное решение
варианта) переведён на `{% extends "base.html" %}` + новый `reshenie.css`, с
переиспользованием готовых компонентов дизайн-системы; вся Jinja/JS-логика
(3 типа формы ответа, таймер, AJAX-проверка) сохранена без изменений;
**`exam.html` осознанно не тронут** — по требованию пользователя; (3)
подключены Яндекс.Метрика (Вебвизор, карта кликов/скроллинга) и Open
Graph/`twitter:card` теги в `base.html`/`start.html` — счётчик сознательно не
добавлен на `exam.html` и в админ-шаблоны. Пункт про наполнение теории/уроков
контентом перенесён на следующую сессию (v1.11.1).

v1.10.2 — два независимых патча по репорту пользователя со
скриншотов живого сайта. (1) Диаграмма статистики по заданиям отображалась на
egevolution.ru без стилей — деплой был корректным (CSS на сервере побайтово
идентичен локальному), но у статики выставлен `Cache-Control: max-age=3888000`
(45 дней) без версионирования URL, поэтому браузеры, посещавшие `/stats` до
деплоя, показывали закэшированный старый CSS. Добавлен `?v={{ app_version }}`
ко всем 20 ссылкам на CSS/JS в 16 шаблонах сайта — теперь при каждом повышении
версии статика гарантированно перекачивается заново. (2) Ссылка-эмодзи «🔗» на
карточку в базе заданий из разбора попытки (`attempt_detail.html`) заменена на
кнопку «Перейти к заданию» в отдельном столбце таблицы.

v1.10.1 — точечный патч поверх v1.10.0: список тем теории
(`/theory`) показывал тему №1 как «Закрыто» для залогиненных пользователей без
явного разрешения администратора, хотя доступ к ней должен быть открыт всем.
Причина — `theory()` дублировал прямой SQL-запрос к `user_theory_access` в обход
`check_theory_access()`, где было добавлено исключение для `task_num==1`;
исправлено вызовом самой функции вместо дублирующего запроса. Гостей баг не
касался.

v1.10.0 — шесть точечных доработок платформы за одну сессию
(первая версия после завершения базы задач в v1.9.0, не связанная с наполнением
контента): полная пересборка `variants/` (14 старых удалены, 20 новых созданы
через уже существующий `/api/variants/save`, сбалансированный подбор задач без
повторов между вариантами); `load_variant_tasks()` сохраняет реальный `task_id`
базы заданий, детальная статистика попытки (`attempt_detail.html`) даёт прямую
ссылку на карточку в `/tasks/<n>`; диаграмма решаемости по каждому из 27 номеров
заданий на `/stats` и в админке; обнаружен и переработан глобальный
`before_request`-гейт `check_auth()` — база заданий и каталоги уроков/теории
открыты гостям, первый урок и первая тема теории открыты всем без исключения;
страница регистрации — обязательное поле «Имя» + подсказка про пароль сайта;
версия сайта (`APP_VERSION`) выведена в футер; эмодзи-замки 🔒/🔓, нечитаемые на
части мобильных экранов, заменены на эмодзи+текст. Подробности — см.
DEVELOPMENT_LOG.md.

v1.9.0 — база задач завершена: заполнены последние два
номера, Задания №26–27 (214 новых задач: 124 + 90), чем закрыт весь диапазон
1–27 (2924 задачи суммарно). Оба задания используют частичный зачёт ЕГЭ
(0/1/2 балла) — специальную ветку `get_points_for_task()` в `app.py` для
позиций 26/27, которая требует формат `answer_grid`; устаревшие
задачи-заглушки использовали несовместимый с этой логикой `answers: [a, b]`
и при реальном использовании засчитывались бы в 0 баллов независимо от
ответа — заменены на `answer_grid` при импорте. Задание 27 потребовало
**расширения схемы задачи**: каждая запись требует два разных входных файла
(файл A и файл B), тогда как прежняя схема поддерживала только одно поле
`"file"` — добавлено новое опциональное поле `"files": [...]` (список),
с обратной совместимостью для всех остальных 26 заданий (используется
generic-проверка `{% if task.files %}…{% elif task.file %}…{% endif %}` в
`templates/tasks_view.html`, `templates/reshenie.html`, и JS-контейнер
`#task-file-links-container` в `templates/exam.html`, включая два защищённых
шаблона — правки согласованы отдельно). По запросу пользователя пул Задания
27 расширен с 23 именованных до 90 задач (+ 67 анонимных), что вскрыло три
новых класса багов парсера (доп. атрибуты после base64 в `<img>`, внешние
картинки с `kpolyakov.spb.ru` вместо base64, вложенный `<div><div>…</div></div>`
в середине текста — не как обёртка всего условия). После завершения всех 27
заданий проведён финальный сплошной аудит всей базы: найден и исправлен
последний реальный дефект (необработанная LaTeX-конструкция `\left|…\right|`
в одной задаче Задания 27), остальные 4 находки — подтверждённые безопасные
ложные срабатывания (см. DEVELOPMENT_LOG.md). Единственное изменение кода в
этой версии — три шаблона выше; `app.py` не менялся (генерическая логика
раздачи файлов уже не завязана на схему).

v1.8.3 — база задач наполнена реальным контентом с kompege.ru для Заданий №19–25
(763 новые задачи: 113 + 113 + 113 + 110 + 119 + 75 + 120). Задания 19–21
образуют на реальном экзамене «связанную тройку»
(общее условие, три разных вопроса) — реализовано как параллельные id в трёх
файлах базы (`tasks/task_19/`…`task_21/`) без изменения схемы вариантов, плюс
отражение выбора по всем трём позициям в конструкторе
(`templates/constructor_from_base.html`, `LINKED_TRIO`). Задание 22 расширено
до 110 задач (67 именованных + 43 анонимных по решению пользователя) с
исправлением бага «жирный текст» — протёкшие формулировки-заглушки
(«Типовой пример…» и т.п.), остававшиеся после вырезания встроенной таблицы.
Задание 24 импортировано в урезанном объёме — 75 задач вместо изначально
доступных ~125 — из-за аномально крупных приложенных файлов (~8 МБ каждый,
против килобайтов в остальных заданиях); довезено в две партии по 25 сверх
исходных 25. Задание 25 впервые в базе полностью использует табличный формат
ответа `answer_grid` (числа + остаток/делитель), включая декодирование
встроенной в условие base64-картинки в обычный файл изображения. Помимо
данных, точечно исправлен критический баг в `replace_markers()` (`app.py`) —
коллизия плейсхолдеров XSS-экранирования, при которой однобуквенное
содержимое тега могло случайно собрать подстроку, совпадающую с плейсхолдером
другого тега (обнаружено пользователем на живом сайте как текст
«___IEND_I___»); плейсхолдеры переведены с `___NAME___` на непересекающиеся
`\x00PH1\x00`…`\x00PH8\x00`. Задания 11–18 (v1.8.2, 851 задача) не
расширялись.

v1.8.2 — база задач наполнена реальным контентом с kompege.ru для Заданий №11–18
(851 задача: 120 + 60 + 100 + 128 + 130 + 118 + 85 + 110), включая новый
рендерер таблиц-программ для Машины Тьюринга (Задание 12), конвертер
LaTeX-формул в юникод (`\lor`, `\cdot`, `\times`, `X^{Y}` и др. — Задания
14–16), двухчастный формат ответа `answers: [a, b]` (Задания 17–18) и
приоритетную загрузку файлов. По итогам сплошного аудита всех 2029 задач
Заданий №1–18 исправлены точечные баги парсера прошлых версий (необработанный
LaTeX в Заданиях 13/14, пропущенная авторская подпись в Задании 8, дубль в
Задании 2, пустые теги-обёртки с пробелом по всей базе). Код проекта не
менялся — только содержимое `tasks/task_11/`…`tasks/task_18/` и ретроактивные
правки `tasks/task_02/`, `tasks/task_08/`, `tasks/task_13/`, `tasks/task_14/`.

v1.8.1 — база задач наполнена реальным контентом с kompege.ru для Заданий №4–10
(780 задач: 122 + 125 + 78 + 122 + 127 + 91 + 115), включая приложенные файлы
(аудио-описания, таблицы, документы) и точечные исправления парсера
(`<div>`/`<pre>`/`<ol>`/`<li>` внутри условия, сбалансированное разбиение
`<b>`/`<i>` по `<br/>`). Заодно ретроактивно убраны авторские подписи из
Заданий №1–4 и убрано скругление углов у картинок-таблиц. Код проекта не
менялся — только содержимое `tasks/task_04/`…`tasks/task_10/` и точечные CSS-правки
(`static/css/pages/tasks.css`, `templates/reshenie.html`, `templates/exam.html`).

v1.8.0 — база задач наполнена реальным контентом с kompege.ru для Заданий №1–3
(317 задач: 113 + 116 + 88), включая рендеринг таблиц истинности в картинки и
загрузку файлов баз данных. Код проекта не менялся — только содержимое
`tasks/task_01/`, `tasks/task_02/`, `tasks/task_03/`.

v1.7.2 — Block Scheme визуализатора научился строить отдельные диаграммы для
пользовательских функций (`def`), с анимацией перелёта аргументов/возврата
между диаграммами (в т.ч. рекурсия), плюс блок-витрина визуализатора на
главной странице и точечные исправления (наложение диаграмм, источник шарика
Memory/Output после `return`, отступы).

v1.7.1 — визуальная унификация визуализатора Python-кода с остальным сайтом:
собственный navbar визуализатора заменён на общий `components/navbar.html`,
светлая тема визуализатора перекрашена под палитру сайта (`--primary: #2563EB`,
общий градиентный фон), тёмная тема визуализатора сохранена, но при её
включении navbar инвертируется в светлый вариант.

v1.7.0 — интеграция визуализатора Python-кода (отдельный проект
[visual-code](https://github.com/Artyomka12/visual-code)), встроен как модуль:
`validator.py` + `tracer.py` рядом с `app.py`, маршруты `GET /visualizer` и
`POST /api/visualizer/trace`, доступ только для авторизованных пользователей.

Редизайн пользовательских страниц (v1.6.5): 15 страниц, светло-голубая тема,
`base.html` + компоненты + 8 page-CSS файлов. Финальный аудит: 246/246 ✅

---

## Стек технологий

| Компонент | Версия |
|---|---|
| Python | 3.x |
| Flask | 3.0.0 |
| Werkzeug | 3.0.1 |
| Flask-WTF | 1.2.1 |
| python-dotenv | 1.0.0 |
| SQLite | встроен |
| Jinja2 | встроен в Flask |
| JavaScript | vanilla, без фреймворков |

---

## Структура проекта

```
webpl with base/
├── app.py                  # Весь backend (~2629 строк) и все Flask-маршруты
├── validator.py            # AST-валидатор кода визуализатора (whitelist-подход, из visual-code)
├── tracer.py               # Трейсер выполнения кода визуализатора (sys.settrace, из visual-code)
├── requirements.txt
├── users.db                # SQLite база данных
├── .env                    # Секреты (НЕ в git)
├── .env.example            # Шаблон переменных окружения
├── make_admin.py           # Утилита назначения прав администратора
├── Procfile                # Для деплоя (gunicorn)
│
├── variants/               # Варианты ЕГЭ (JSON + медиафайлы)
│   ├── variant_01/
│   │   ├── variant.json
│   │   └── images/
│   └── ...
│
├── tasks/                  # База задач по номерам заданий
│   ├── task_01/
│   │   ├── tasks.json
│   │   └── images/
│   └── ... (task_01 — task_27)
│
├── theory/                 # Теоретические материалы
│   ├── task_01/
│   │   ├── theory.json
│   │   ├── images/
│   │   └── videos/
│   └── ...
│
├── uroki/                  # Подготовительные уроки
│   ├── urok_01/
│   │   ├── lesson.json
│   │   └── images/
│   └── ...
│
├── templates/              # 31 HTML-файл (Jinja2)
│   ├── base.html           # Базовый шаблон: блоки title, extra_head, content, scripts
│   ├── components/
│   │   ├── navbar.html     # Sticky тёмный navbar, dropdown, logout POST+CSRF
│   │   ├── footer.html     # Футер со ссылкой на Telegram
│   │   └── flash.html      # Flash-сообщения (success/error/warning)
│   ├── start.html          # Главная страница — автономная, не наследует base.html
│   ├── exam.html           # Экзаменационный режим — автономный, вне scope редизайна
│   ├── visualizer.html     # Визуализатор Python-кода — автономный (visual-code), только для авторизованных
│   ├── [12 admin/constructor-шаблонов] — автономные, вне scope редизайна
│   │   # admin_panel, admin_task_form, admin_tasks_list, admin_tasks_view,
│   │   # admin_user_stats, admin_create_user, admin_practice_detail,
│   │   # constructor_gate, constructor_from_base, constructor_theory,
│   │   # constructor_uroki, constructor_editor
│   └── [16 пользовательских страниц — {% extends "base.html" %}]
│       # login, register, csrf_error, variant_list, teoria, preparation,
│       # tasks_list, profile, stats, attempt_detail, choose_mode,
│       # tasks_view, teoria_zadanie, preparation_lesson, results, reshenie
│
├── static/
│   ├── favicon.png
│   ├── css/
│   │   ├── design-system.css           # Дизайн-токены: цвета, отступы, .card, .btn, .form-input
│   │   └── pages/                      # Page-specific CSS (9 файлов)
│   │       ├── auth.css                # login, register, csrf_error
│   │       ├── catalogs.css            # variant_list, teoria, preparation, tasks_list
│   │       ├── profile.css             # profile
│   │       ├── stats.css               # stats, attempt_detail
│   │       ├── variants.css            # choose_mode
│   │       ├── tasks.css               # tasks_view
│   │       ├── teoria.css              # teoria_zadanie
│   │       ├── preparation_lesson.css  # preparation_lesson
│   │       └── reshenie.css            # reshenie (добавлен в v1.11.0)
│   ├── js/
│   │   └── scroll-reveal.js            # IntersectionObserver-анимация .reveal → .reveal.visible
│   ├── visualizer/                     # Статика визуализатора Python-кода (из visual-code, без изменений)
│   │   ├── style.css
│   │   └── js/                         # api.js (адаптирован под относительный путь + CSRF), editor.js, animator.js, block-scheme.js
│   ├── images/                         # video-preview-left.jpg, video-preview-right.jpg
│   ├── videos/                         # platform-overview.mp4
│   ├── uploads/avatars/                # Аватары пользователей
│   └── tutors/                         # Файлы преподавателей
```

---

## Дизайн-система и архитектура шаблонов (v1.6.5)

### Светлая тема (пользовательские страницы)

Все 15 пользовательских страниц переведены на единую светло-голубую тему:
градиент фона `#eaf6ff → #f5fbff → #eef8ff`, белые карточки, синий акцент `#2563eb`,
тёмный текст `#0f172a`, sticky тёмный navbar.

### Файлы дизайн-системы

| Файл | Назначение |
|---|---|
| `static/css/design-system.css` | CSS-токены: `--color-bg-page`, `--color-accent-blue`, `--radius-md`, `--transition-base`; классы `.card`, `.btn`, `.form-input`, `.section-heading`, `.page-wrap` |
| `static/css/pages/auth.css` | `login.html`, `register.html`, `csrf_error.html` |
| `static/css/pages/catalogs.css` | `variant_list.html`, `teoria.html`, `preparation.html`, `tasks_list.html` |
| `static/css/pages/profile.css` | `profile.html` |
| `static/css/pages/stats.css` | `stats.html`, `attempt_detail.html` |
| `static/css/pages/variants.css` | `choose_mode.html` |
| `static/css/pages/tasks.css` | `tasks_view.html` (collapse/expand через `max-height: 5000px`) |
| `static/css/pages/teoria.css` | `teoria_zadanie.html` |
| `static/css/pages/preparation_lesson.css` | `preparation_lesson.html` |
| `static/js/scroll-reveal.js` | IntersectionObserver: `.reveal → .reveal.visible`, threshold 0.12 |

### Компоненты (templates/components/)

| Компонент | Особенности |
|---|---|
| `navbar.html` | Sticky тёмный navbar; dropdown раздела; logout через `POST` + CSRF hidden input |
| `footer.html` | Футер со ссылкой на Telegram |
| `flash.html` | Flash-сообщения трёх категорий: `success`, `error`, `warning` |

Примечание: `.card` в `design-system.css` не имеет собственного `padding` — каждый page-CSS добавляет его.
При hover `.card:hover { transform }` переопределяется в page-CSS для интерактивных карточек,
где transform нежелателен (`.task-progress-panel`, `.lesson-practice-card` и др.).

### Автономные шаблоны (не наследуют base.html)

| Шаблон | Причина автономности |
|---|---|
| `start.html` | Собственная дизайн-система v1.6.0–1.6.4; navbar встроен напрямую |
| `exam.html` | Специально вне scope редизайна v1.6.5; встроенный CSS |
| Admin/constructor-шаблоны (9 шт.) | Вне scope редизайна v1.6.5 |

`reshenie.html` переведён на `base.html`/`design-system.css` в v1.11.0 — больше не автономный.

### CSRF в шаблонах

- HTML-формы: `<input type="hidden" name="csrf_token" value="{{ csrf_token() }}">`
- JSON fetch: заголовок `'X-CSRFToken': '{{ csrf_token() }}'`
- `preparation_lesson.html` не имеет `<form>` — ответы только через `onclick` → `checkTask()` → fetch.
  Любые `<input>` внутри `.task-block` попадают в `querySelectorAll('input')` — скрытые поля недопустимы.

---

## Визуализатор Python-кода (v1.7.0–v1.7.2)

Интегрирован отдельный проект [visual-code](https://github.com/Artyomka12/visual-code) —
интерактивный визуализатор выполнения Python-кода (Classic View: scope-блоки и «шарики»
данных; Block Scheme: анимированная блок-схема/AST). Перенесён как модуль внутрь
существующего Flask-приложения, без отдельного процесса/порта.

**Backend:**

| Файл | Назначение |
|---|---|
| `validator.py` | Whitelist-валидация AST (перенесён из visual-code без изменений). Разрешены: переменные, `if/elif/else`, `for`/`while`, функции, списки/словари/comprehension, ~20 builtin-функций. Запрещены: `import`, `class`, `lambda`, `try/except`, `global/nonlocal`, `async/await` |
| `tracer.py` | Трассировка выполнения через `sys.settrace` (перенесён без изменений). Лимит 600 line-событий (`MAX_STEPS`). Wall-clock timeout не реализован — при необходимости решать отдельно |
| `app.py` | Роуты `GET /visualizer` и `POST /api/visualizer/trace`, импортирующие `validate()`/`trace_code()`. Оба защищены проверкой `"user_id" not in session` |

**Frontend:**

- `templates/visualizer.html` — автономный шаблон, не наследует `base.html` (как `start.html`/`exam.html`), но с v1.7.1 использует общий `{% include 'components/navbar.html' %}` вместо собственного header
- `static/visualizer/style.css` — с v1.7.1 содержит собственную копию CSS navbar (не подключает `design-system.css` целиком — во избежание конфликта одноимённых классов `.card`/`.btn` с классами самого визуализатора); светлая тема (`:root`) перекрашена под палитру сайта (`--primary: #2563EB`, фон — общий градиент `#eaf6ff→#f5fbff→#eef8ff`); тёмная тема (`.dark`) сохранена как есть, но `.dark .navbar` и дочерние элементы инвертированы в светлый вариант — единственный на сайте пример navbar с двумя цветовыми состояниями
- `static/visualizer/js/{editor,animator}.js` — перенесены без изменений; кнопка переключения темы (🌙, `editor.js`) с v1.7.1 расположена в `.page-title` рядом с заголовком страницы, а не в header
- `static/visualizer/js/block-scheme.js` — с v1.7.2 существенно доработан (см. ниже); `editor.js`/`animator.js` (Classic View) не затронуты
- `static/visualizer/js/api.js` — единственная адаптированная часть самого визуализатора (не считая `block-scheme.js` с v1.7.2): `API_BASE` переведён на относительный путь `/api/visualizer`, добавлен заголовок `X-CSRFToken` (токен читается из `<meta name="csrf-token">` в `visualizer.html`)

**Точки входа в навигации:**

- `templates/components/navbar.html` — кнопка «⚡ Визуализатор кода» рядом с выпадающим списком «Разделы платформы» (десктоп); на `≤768px` уходит внутрь выпадающего списка
- `templates/start.html` — идентичная кнопка продублирована во встроенном navbar главной страницы (свой `<style>`, не наследует `components/navbar.html`); с v1.7.2 на главной странице также есть блок-витрина `.visualizer-preview-section` (между «Что есть на платформе» и «Ваш прогресс») — по образцу `.video-preview-section`, видео + два скриншота, CTA на `/visualizer`

**Block Scheme — диаграммы функций (v1.7.2):**

Изначально `block-scheme.js` строил только одну диаграмму и полностью игнорировал тела `def` (пропускались парсером). Теперь:

- Парсер (`parseBlock`/`parseFunctionDef`) распознаёт `def` отдельно от обычной последовательности — тело рекурсивно разбирается и «всплывает» через `functions[]`, а не остаётся в основном потоке; новый тип узла `return` (форма — перевёрнутая трапеция, свой цвет `--bs-return-*` в `style.css`, тупиковый блок без исходящей стрелки)
- `layoutProgram()` (заменил `layoutTree()`) строит главную диаграмму + отдельную диаграмму на каждую функцию, колонкой справа, друг под другом в порядке определения; диаграммы функций не имеют овала «Конец» — обрываются на последнем блоке
- Позиционирование колонки функций — не фиксированным отступом, а измерением: пробный проход `layoutSeq` при `cx=0` для каждой функции (`leftExtent()`), чтобы узнать её реальную левую границу с учётом вложенности циклов/условий, и только потом разместить с гарантированным зазором (`BS.FUNC_GAP_X`). Фиксированный отступ раньше приводил к наложению диаграмм при глубокой вложенности (обнаружено и исправлено в v1.7.2)
- Анимация вызова/возврата: карта `scope_id → строка вызова` (`bsCallSiteLine`, строится один раз из полной трассировки) — при `call`-шаге аргументы летят по невидимой прямой от блока-вызова к «Начало (имя)» нужной диаграммы; при `return`-шаге значение летит обратно именно к тому блоку, откуда был сделан этот конкретный вызов. Работает одинаково для рекурсии (вызов из той же диаграммы) и вызова одной функции из другой
- Источник шарика Memory/Output (`srcEl` в `bsStepNext()`) переопределяется на call-site по той же карте, когда текущий шаг — `return`: иначе шарик летел от строки самого `return`, а не от блока, где функция была вызвана (баг, найденный после того, как диаграммы функций стали реально рисоваться — раньше просто молчаливо не анимировался)
- `#bs-svg` — адаптивный (`max-width:100%; height:auto; min-width:650px`), сжимается под контейнер через свой `viewBox`; ниже `min-width` включается скролл (`#bs-canvas { overflow:auto }`) вместо дальнейшего сжатия

**Известные ограничения (сознательно отложены):**

- Нет wall-clock timeout на трассировку — тяжёлая одна строка (`2**10**8` и т.п.) может надолго занять воркер. Риск снижен закрытым доступом (только авторизованные), но не устранён
- Нет rate limiting на `/api/visualizer/trace`
- Block Scheme: возврат из функции с полностью «мёртвым» if/else внутри цикла (обе ветки — return) может оставить висящую стрелку от несуществующего входа — редкий случай, не входил в текущий scope

---

## Архитектура контента

### Варианты (variants/)

Поддерживаются два формата JSON:

**Старый формат** — список полных объектов задач:
```json
[
  {
    "id": 1,
    "title": "Задание 1",
    "description": ["..."],
    "correct_answer": "42",
    "images": [{"path": "img.png", "after_paragraph": 0}]
  }
]
```

**Новый формат** — словарь ссылок на базу задач:
```json
{
  "1": {"task_id": 5},
  "2": {"task_id": 12}
}
```

Функция `load_variant_tasks(variant_num)` обрабатывает оба формата.
- Старый формат: задачи берутся из JSON варианта напрямую.
- Новый формат: задачи подтягиваются из `tasks/task_XX/tasks.json`.
  При загрузке устанавливается `task["id"] = task_num` и
  `task["_source_task_num"] = task_num` для корректной маршрутизации изображений.

### Маршрутизация изображений

- Новый формат: `url_for('task_images', task_num=task._source_task_num, filename=...)`
- Старый формат: `url_for('variant_images', variant_num=variant_num, filename=...)`

Проверка в шаблонах: `{% if task._source_task_num is defined %}`.

### База задач (tasks/)

`tasks/task_XX/tasks.json` — список объектов задач для задания №XX.
Используется конструктором вариантов и просмотром базы администратором.

### Теория (theory/)

`theory/task_XX/theory.json` — материал по заданию №XX:
- `theory.theory.description` — список абзацев
- `theory.theory.images` — изображения с привязкой к абзацам
- `theory.videos` — массив видео (новый формат)
- `theory.video` — одно видео (старый формат, обратная совместимость)
- `theory.practice.tasks` — практические задания

### Уроки (uroki/)

`uroki/urok_XX/urok_XX.json` — подготовительный урок (номер id встроен в имя файла,
в отличие от `theory/task_XX/theory.json`, где имя файла фиксировано, а номер лежит
внутри JSON как `task_id`). Формат (с v1.14.0, зеркально теории):
- `lesson_id` — номер урока, дублируется в самом JSON (зеркально `task_id`)
- `description` — короткое описание для карточки в каталоге `/preparation` (поля
  нет у теории)
- `theory.description` / `theory.images` — абзацы теории и картинки к ним
- `videos` — массив видео (`{path, title}`), несколько на урок — старый одиночный
  `video` поддерживается в шаблоне только для обратной совместимости
- `practice.tasks` — практические задания (заголовок, абзацы, картинки,
  прикреплённый файл в `uroki/urok_XX/practice_files/`, один из
  `correct_answer`/`answers`/`answer_grid`)

Доступ управляется через таблицу `user_lesson_access` (админ открывает/закрывает).

### Система сложности задач (DIFFICULTY_INFO)

Каждая задача в `tasks.json` может иметь поле `"difficulty"` с одним из четырёх значений:

| Ключ | Метка | Цвет |
|---|---|---|
| `easy` | Лёгкое | зелёный `#4ade80` |
| `medium` | Среднее | жёлтый `#facc15` |
| `hard` | Сложное | красный `#ef4444` |
| `grave` | Гроб | фиолетовый `#a855f7` |

Словарь `DIFFICULTY_INFO` в `app.py` содержит эти значения и используется
при отображении задач в базе и статистике. Если поле отсутствует — задача
считается `medium` по умолчанию.

---

## База данных (users.db)

### Таблицы

| Таблица | Назначение |
|---|---|
| `users` | Пользователи: id, username, password_hash, name, avatar, is_admin, created_at |
| `user_results` | История попыток решения вариантов: variant_num, score, secondary_score, time_spent, date, total_tasks |
| `user_task_answers` | Ответы на задания в вариантах: variant_num, task_id, user_answer, is_correct, points |
| `user_lesson_progress` | Прогресс в уроках: lesson_id, task_id, attempts, is_correct |
| `user_theory_progress` | Прогресс по теории: task_num, practice_task_id, attempts, is_correct |
| `user_lesson_access` | Доступ к урокам: user_id, lesson_id, is_unlocked |
| `user_theory_access` | Доступ к теории: user_id, task_num, is_unlocked |
| `task_usage` | Статистика использования задач в вариантах |
| `site_applications` | Заявки с формы `/register`: last_name, first_name, phone, contact_type, contact_value, is_processed, created_at |

### Подключение

`get_db()` — открывает новое соединение при каждом запросе.
`db.row_factory = sqlite3.Row` — доступ к полям по имени.
Соединения закрываются вручную в конце каждого обработчика.

---

## Основные маршруты

### Публичные
| Маршрут | Описание |
|---|---|
| `GET /` | Главная страница |
| `GET /login`, `POST /login` | Вход |
| `GET /register`, `POST /register` | Заявка на доступ (не создаёт аккаунт — пишет в `site_applications`, уведомляет админа в Telegram) |
| `POST /logout` | Выход (защищён CSRF; GET → 405) |

### Пользовательские (требуют авторизации)
| Маршрут | Описание |
|---|---|
| `GET /variants` | Список вариантов |
| `GET /solve/<variant_num>` | Решение варианта (обычный режим) |
| `POST /check/<variant_num>/<task_id>` | Проверка ответа на задание |
| `POST /save_results/<variant_num>` | Сохранение результатов варианта |
| `GET /results/<variant_num>` | Страница результатов |
| `GET /exam/<variant_num>` | Экзаменационный режим |
| `POST /finish_exam/<variant_num>` | Завершение экзамена |
| `GET /choose-mode/<variant_num>` | Выбор режима (обычный/экзамен) |
| `GET /theory` | Список заданий теории |
| `GET /theory/<task_num>` | Теория по заданию |
| `POST /check_theory_answer` | Проверка ответа теории |
| `GET /preparation` | Список уроков |
| `GET /preparation/<lesson_id>` | Урок |
| `POST /check_lesson_task` | Проверка ответа урока |
| `GET /tasks` | Публичный список 27 номеров заданий с базы |
| `GET /tasks/<task_num>` | Просмотр задач из базы по номеру задания |
| `GET /stats` | Личная статистика |
| `GET /stats/attempt/<attempt_id>` | Детальная статистика конкретной попытки |
| `GET /profile` | Профиль |
| `POST /profile/upload_avatar` | Загрузка аватара |
| `GET /visualizer` | Визуализатор выполнения Python-кода (только для авторизованных) |
| `POST /api/visualizer/trace` | Трассировка выполнения Python-кода для визуализатора (JSON) |

### Административные (требуют is_admin=1)
| Маршрут | Описание |
|---|---|
| `GET /admin` | Панель администратора |
| `POST /admin/delete_user/<user_id>` | Удаление пользователя |
| `GET /admin/tasks` | Список всех номеров заданий в базе |
| `GET /admin/tasks/<task_num>` | Список задач конкретного номера |
| `GET/POST /admin/tasks/<task_num>/add` | Добавление задачи |
| `GET/POST /admin/tasks/<task_num>/edit/<task_id>` | Редактирование задачи |
| `POST /admin/tasks/<task_num>/delete/<task_id>` | Удаление задачи |
| `GET /admin/user/<user_id>` | Статистика пользователя |
| `GET /admin/user/<user_id>/attempt/<attempt_id>` | Просмотр попытки пользователя |
| `GET /admin/user/<user_id>/lesson/<lesson_id>` | Постатейная детализация прогресса по уроку |
| `GET /admin/user/<user_id>/theory/<task_num>` | Постатейная детализация прогресса по теории |
| `POST /admin/user/<user_id>/toggle_lesson/<lesson_id>` | Доступ к уроку |
| `POST /admin/user/<user_id>/toggle_theory/<task_num>` | Доступ к теории |
| `GET/POST /admin/create_user` | Ручное создание аккаунта (логин/пароль вводит админ; поддерживает `?name=` префилл из заявки) |
| `POST /admin/application/<application_id>/toggle` | Переключение статуса заявки (обработана / не обработана) |

### API и конструктор
| Маршрут | Описание |
|---|---|
| `GET /api/tasks/<task_num>` | Список задач из базы (JSON) |
| `GET /api/theory/<task_num>` | Данные теории (JSON) |
| `POST /api/theory/save` | Сохранение теории (JSON) |
| `GET /api/uroki/<lesson_id>` | Данные урока (JSON) |
| `POST /api/uroki/save` | Сохранение урока (JSON) |
| `POST /api/variants/save` | Сохранение нового варианта (JSON) |
| `POST /api/variant/preview` | Предпросмотр варианта (JSON; не используется в шаблонах) |
| `GET /constructor_gate` | Выбор типа конструктора |
| `GET /constructor_from_base` | Конструктор из базы задач |
| `GET /constructor_theory` | Редактор теории |
| `GET /constructor_uroki` | Редактор уроков начальной подготовки |
| `GET /constructor_editor` | Редактор варианта |

### Файловые маршруты (раздача статики контента)
| Маршрут | Источник |
|---|---|
| `GET /task_images/<task_num>/<filename>` | `tasks/task_XX/images/` |
| `GET /task_files/<task_num>/<filename>` | `tasks/task_XX/` |
| `GET /variant_images/<variant_num>/<filename>` | `variants/variant_XX/images/` |
| `GET /variant_files/<variant_num>/<filename>` | `variants/variant_XX/` |
| `GET /theory_images/<task_num>/<filename>` | `theory/task_XX/images/` |
| `GET /theory_videos/<task_num>/<filename>` | `theory/task_XX/videos/` |
| `GET /theory_files/<task_num>/<filename>` | `theory/task_XX/` |
| `GET /preparation_images/<lesson_id>/<filename>` | `uroki/urok_XX/images/` |
| `GET /preparation_videos/<lesson_id>/<filename>` | `uroki/urok_XX/videos/` |
| `GET /preparation_files/<lesson_id>/<filename>` | `uroki/urok_XX/practice_files/` |

---

## Переменные окружения

Файл `.env` (не в git). Шаблон: `.env.example`.

| Переменная | Обязательна | Описание |
|---|---|---|
| `SECRET_KEY` | ДА | Секретный ключ Flask для подписи сессий |
| `SITE_PASSWORD` | ДА | ⚠️ С v1.13.0 нигде не используется в коде (регистрация заменена формой заявки) — оставлена по решению пользователя, не трогать без отдельного согласования |
| `ADMIN_USERNAME` | ДА | Логин администратора (при первом запуске) |
| `ADMIN_PASSWORD` | ДА | Пароль администратора (при первом запуске) |
| `WTF_CSRF_SECRET_KEY` | НЕТ | Ключ CSRF (по умолчанию = SECRET_KEY) |
| `FLASK_DEBUG` | НЕТ | Режим отладки: `1` = включён, `0` = выключен |
| `TELEGRAM_BOT_TOKEN` | НЕТ | Токен Telegram-бота для уведомлений о новых заявках (v1.13.0). Без него отправка молча пропускается |
| `TELEGRAM_CHAT_ID` | НЕТ | Chat ID администратора для уведомлений в Telegram (v1.13.0) |

Приложение **не запустится** без первых четырёх переменных.

---

## Правила запуска

```bash
# 1. Создать .env из шаблона
cp .env.example .env
# Заполнить значения в .env

# 2. Установить зависимости
pip install -r requirements.txt

# 3. Запустить
python app.py
# или для продакшена:
gunicorn app:app
```

База данных `users.db` и администратор создаются автоматически при первом запуске.

---

## Деплой на продакшен (REG.RU)

Сайт egevolution.ru развёрнут на виртуальном хостинге REG.RU через Passenger.
GitHub — единственный источник актуального кода; на сервер файлы вручную
практически никогда не копируются (исключение — ручная загрузка больших
видеофайлов, которые не попадают в git из-за `.gitignore`).

**Рабочий каталог на сервере:** `~/www/egevolution.ru`
(полный путь: `/var/www/<логин>/data/www/egevolution.ru`) — это тот же
git-репозиторий, работа ведётся прямо в нём.

**Схема обновления:**

```
локальный ПК → git push origin main → GitHub (origin/main)
                                            ↓
                              SSH на сервер REG.RU
                              cd ~/www/egevolution.ru
                                            ↓
                              git pull origin main
                                            ↓
                              touch tmp/restart.txt
                                            ↓
                        Passenger отслеживает mtime этого файла
                        и сам перезапускает Flask-приложение
```

- `touch tmp/restart.txt` обязателен после любого изменения Python-кода,
  HTML-шаблонов, JS, CSS, а также `.env`. Если менялись только статические
  файлы (картинки, видео) — перезапуск обычно не нужен.
- `.env` существует **только на сервере**, в git не хранится. После правки
  `.env` на сервере тоже нужен `touch tmp/restart.txt`, чтобы Flask
  перечитал переменные окружения.
- Крупные видеофайлы (например, `static/videos/*.mp4`) исключены из git
  через `.gitignore` — их нужно заливать на сервер вручную отдельно от
  `git pull`.
- Никакие другие способы перезапуска (`systemctl restart`, `gunicorn`,
  Docker, CI/CD, Supervisor) в этой схеме не используются — переход на них
  не планируется без отдельного явного запроса.

---

## Правила работы Claude Code

### Обязательно перед изменениями
1. Прочитать файл полностью, найти все затронутые места.
2. Показать план изменений перед внесением.
3. Дождаться подтверждения пользователя.

### Запрещено без явного подтверждения
- Менять схему базы данных (`CREATE TABLE`, `ALTER TABLE`).
- Менять логику авторизации и проверки паролей.
- Менять формат хранения вариантов в JSON.
- Создавать новые таблицы в `users.db`.
- Менять `load_variant_tasks()` — критическая функция совместимости.
- Переименовывать маршруты (сломает закладки и внешние ссылки).
- Менять структуру директорий `variants/`, `tasks/`, `theory/`, `uroki/`.
- Добавлять зависимости в `requirements.txt` без согласования.
- Делать git push без явной просьбы пользователя.
- Изменять `.env` (содержит реальные секреты).

### Стиль кода
- Шаблоны: 16 пользовательских страниц наследуют `base.html`; `start.html`, `exam.html`
  и все admin-шаблоны — автономные HTML-файлы со встроенным CSS.
- JS — только vanilla, встроен в шаблоны.
- Все секреты — через `os.environ`, не хардкодить.
- Паттерн чтения переменных: `os.environ.get(key) or default` (не `get(key, default)`).
- CSRF: HTML-формы — hidden input, JSON fetch — заголовок `X-CSRFToken`.

---

## Что нельзя менять без подтверждения пользователя

1. **Схема БД** — любые изменения таблиц затрагивают существующих пользователей.
2. **`load_variant_tasks()`** — обеспечивает совместимость старых и новых вариантов.
3. **Маршруты** — изменение URL сломает существующие ссылки и закладки.
4. **Пароль сайта** — механизм закрытой регистрации.
5. **Форматы JSON контента** — variants, tasks, theory, uroki.
6. **Логика подсчёта баллов** — `secondary_score`, `score` в `user_results`.
7. **Права доступа** (`is_admin`) — механизм безопасности.
8. **CSRF-инфраструктура** — не добавлять `csrf.exempt` без аудита.
9. **`exam.html`** — автономный шаблон, специально не переведён на `base.html`;
   содержит встроенный CSS и JS для экзаменационного режима. Требует отдельного решения.
   (`reshenie.html` переведён на `base.html` в v1.11.0 — больше не в этом списке.)
10. **`start.html`** — автономная главная страница со своей дизайн-системой (v1.6.0–1.6.4);
    navbar встроен напрямую. Любые изменения — только отдельным согласованием.
11. **Admin-шаблоны** (12 файлов: `admin_panel`, `admin_task_form`, `admin_tasks_list`,
    `admin_tasks_view`, `admin_user_stats`, `admin_create_user`, `admin_practice_detail`,
    `constructor_gate`, `constructor_from_base`, `constructor_theory`, `constructor_uroki`,
    `constructor_editor`) — автономные, вне scope редизайна v1.6.5.
12. **`design-system.css`** — изменение токенов затронет все 16 пользовательских страниц одновременно.
13. **`base.html` и `components/`** — изменение затронет все 16 пользовательских страниц.

---

## Работа между несколькими ПК

### Принцип

Код и документация хранятся в git-репозитории на GitHub.
Секреты (`.env`) хранятся локально на каждом ПК — не синхронизируются.
Контекст для Claude Code восстанавливается через `CLAUDE.md`, `DEVELOPMENT_LOG.md`, `TODO.md`.

### Структура файлов контекста

| Файл | Назначение | Обновлять |
|---|---|---|
| `CLAUDE.md` | Статичное описание проекта и правила | При изменении архитектуры |
| `DEVELOPMENT_LOG.md` | История изменений, статус безопасности | После каждой рабочей сессии |
| `TODO.md` | Текущие задачи и известные проблемы | После каждой рабочей сессии |

### Порядок работы через GitHub

**Первичная настройка (один раз):**
```bash
git remote add origin https://github.com/<логин>/<репо>.git
git push -u origin main
```

**Начало работы на любом ПК:**
```bash
git pull origin main
# Убедиться, что .env существует и заполнен
# Если .env нет — скопировать из .env.example и заполнить
```

**Завершение работы на любом ПК:**
```bash
# 1. Обновить DEVELOPMENT_LOG.md и TODO.md (см. промпты ниже)
# 2. Закоммитить изменения
git add .
git commit -m "описание изменений"
git push origin main
```

### Регламент обновления документации

**После каждого крупного изменения кода:**
- Обновить соответствующий раздел в `DEVELOPMENT_LOG.md` (что изменено, почему)
- Закрыть выполненные пункты в `TODO.md`

**Перед переключением на другой компьютер:**
- Обновить `DEVELOPMENT_LOG.md` — текущее состояние, что сделано
- Обновить `TODO.md` — что осталось, что в процессе
- Сделать коммит и `git push`

**Перед коммитом:**
- Проверить, что `.env` не попал в `git add`
- Обновить `DEVELOPMENT_LOG.md` если были значимые изменения

**Перед новым релизом:**
- Добавить новую версию в `DEVELOPMENT_LOG.md` с полным списком изменений
- Обновить статус безопасности
- Закрыть все выполненные задачи в `TODO.md`
- Обновить версию в заголовке `DEVELOPMENT_LOG.md`

### Как восстановить контекст на новом компьютере

1. `git pull origin main`
2. Убедиться, что `.env` заполнен
3. Открыть Claude Code и использовать промпт начала сессии (ниже)
4. Claude прочитает `CLAUDE.md`, `DEVELOPMENT_LOG.md`, `TODO.md` и восстановит контекст

### Промпт начала новой сессии

Использовать после `git pull` на новом компьютере или после долгого перерыва:

```
Привет. Продолжаем работу над проектом EGEvolution.

Прочитай эти файлы по порядку и восстанови контекст:
1. CLAUDE.md — архитектура и правила проекта
2. DEVELOPMENT_LOG.md — что уже сделано и текущий статус
3. TODO.md — что нужно сделать

После прочтения:
- кратко подтверди текущее состояние проекта;
- напомни, что было сделано последним;
- укажи, какая задача следующая по приоритету из TODO.md.

Не вноси никаких изменений до моей команды.
```

### Промпт завершения рабочей сессии

Использовать перед `git commit` и переключением на другой ПК:

```
Рабочая сессия завершается. Обнови документацию.

Задача 1 — обнови DEVELOPMENT_LOG.md:
- добавь запись о сегодняшних изменениях (дата: сегодня);
- опиши что было сделано в этой сессии;
- обнови статус проверок если проводились;
- не удаляй историю предыдущих версий.

Задача 2 — обнови TODO.md:
- закрой [x] выполненные задачи;
- добавь новые задачи если они появились в ходе работы;
- обнови приоритеты если изменились.

После обновления файлов — покажи краткий список изменений,
чтобы я мог написать сообщение для git commit.

Код проекта не трогай.
```
